# 原批次正式调度活性修复

用户已确认：发现问题修复代码，原批次完整创建并回读前持续处理；不靠临时提队列、改业务状态绕过。

## 不变边界

仅新加坡测试环境；保留原提交的租户、冻结 BC 路由、18 剧 × 10 账户、预算和 ENABLE 意图。已成功对象不重建；未知发送先核查，不冒充未发送。素材与封面新鲜度、Worker 并发和外部额度不放宽。沿用 main，不另开分支或并行代理。

## 证据与实施顺序

1. 旧目标素材消息在来源分支之后才核对 revision/claim，过期观察消息可建立新读取代次。先用真实 PostgreSQL/Redis、SDK 传输替身复现，再将快速校验前移并保留持锁复核。
2. MATERIAL 等待视频/封面仍每 15 秒排新消息。改为持久依赖等待：现有结果恢复定时器仅在对应依赖已落定时唤醒原步骤一次，正常执行器继续核验权限、视频和封面。旧投递自然消费，不搬移或删除队列。
3. 有界组合准入，避免所有未来素材同时刷新、排队时间超过 15 分钟新鲜窗口；后续组合由完成/恢复机制自动进入。测试等待跨过新鲜窗口、重复消息、失败与未知分支及多组合连续完成。
4. 运行相关真实 PostgreSQL/Redis 回归、静态检查，聚焦提交并推送。每次发布按手册完整备份、隔离恢复、保留配置并核实所有服务版本。
5. 仅使用只读监控观测正式 Worker 推進；验收为原 180 个组合完整创建和远端回读，不以健康检查或局部成功替代。

## 当前状态

- 旧 revision/claim、素材依赖持久等待和单组合窗口已发布 f0698e38；服务器隔离回归 25 项通过。
- 补充复现封面来源等待 5 秒轮询、未来未发送封面不遵守窗口，修复后 81 项相关回归和 167 项已发送/双通道/批量容量回归通过，尚未发布该补充修复。
- 2026-09-17 16:07 UTC 原批次仍只有 2/180 完整成功；来源封面 559、目标封面 249，当前组合 14 项等待封面。161 项封面因库存分页不完整阻断，继续沿正式恢复与证据核查；未完成整批验收。
