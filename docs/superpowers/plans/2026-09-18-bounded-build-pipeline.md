# 批量搭建流水线 Implementation Plan

> 执行方式：按用户要求在当前会话持续执行，使用 executing-plans；不启动子代理、不等待重复确认。用户明确要求原批次完成前继续。

**Goal:** 完成原180组合，同时用有界并行与结果独立执行解决大批量队头阻塞。

**Architecture:** PostgreSQL持久依赖与窗口控制活跃图，准备和结果各保留一个素材槽。旧消息经正式任务重投递正确路由，原任务栅栏及外部额度保持。

**Tech Stack:** Python/SQLModel/PostgreSQL、Celery/Redis、systemd。

**Spec:** ../specs/2026-09-18-bounded-build-pipeline-design.md

## Global Constraints

仅新加坡测试服；原冻结租户/BC/账户/预算/素材/ENABLE意图不变。活跃素材槽总数2，未知发送不盲重发，不使用运维清队列/改状态/提优先级。

## Task 1：统一窗口与批量准入

Files: app/modules/builds/execution_window.py、app/modules/materials/cover_sharing.py；tests/modules/builds/test_execution_tasks.py、test_execution.py（均位于backend）。

- [x] 三账户实际冻结图复现规划未来封面后领取失败，原代码报cover_claim_lost。
- [x] cover_admission_condition()供规划与单领取复用；当前批次不再选入未来成员，新回归通过。
- [x] window_units()以row_number按tenant/submission分区、drama/unit排序选前10；material_unit_admitted用集合包含而非仅第一行。
- [x] 单槽故障用例显式设置测试槽1；新增默认10槽、超过一波的完整执行和10000组合准入容量回归。关键断言：`len(admitted)==10`，补位后仍10，等待图投递不随总量增长。

## Task 2：结果独立执行与旧路由正式交接

Files: backend/app/jobs/tasks.py、backend/app/modules/materials/cover_tasks.py；deploy/staging-worker.service、新deploy/staging-results.service；backend/tests/jobs/test_worker_queue_isolation.py和queue_isolation_worker.py。

- [x] 新测试先让所有准备槽阻塞，未释放时投递结果，断言3秒内完成；旧两槽混用必须失败。
- [x] prepare_cover回归resources；staging准备1槽、results1槽。旧prepare_cover在结果队列的任务使用`task.retry(queue=dispatch_queue(task.name), countdown=0, max_retries=None)`交接并保留原身份；本地路由/参数通过，真实Linux prefork留发布验收。
- [ ] 测试正确路由不交接、旧路由同ID交接、重复不产生远端重复写、交接故障可由原投递恢复；测试模板总素材槽为2。
- [ ] 扩展实际Redis消费者测试覆盖独立结果服务和10000个准备积压，Linux验证不依赖释放准备槽。

## Task 3：一体回归与正式发布

Files: docs/implementation-progress.md、docs/runbooks/staging-singapore.md、docs/validation/2026-09-17-build-pipeline-liveness.md；私有发布bundle不提交。

- [ ] 运行完整执行/恢复/封面吞吐/双通道/outbox/消费者隔离回归、Ruff/格式/ty app，记录真实PG/Redis容量结果。
- [ ] 聚焦提交并推送；发布bundle覆盖新增results服务的停止、备份、恢复、启动、版本与配置核验，不改变额度和功能开关。
- [ ] 六服务统一版本；素材准备/结果各1、广告/控制各1；备份及独立恢复全部通过，Linux真实消费者测试通过后继续业务验收。

## Task 4：持续业务验收

- [ ] 通过原产品RETRY/RECONCILE恢复确定未发送和未知只读任务，回读恢复进度，不直接调用执行器或改表。
- [ ] 观察源准备、结果核验、窗口补位、广告创建、回读五段真实进度；卡住则记录具体依赖和租约，补回归修代码。
- [ ] 原180组合全部成功并核对无重复创建，再结束目标；未知13来源未取得充分证据前不当作未发送。

## Task 5：业务观察发现的恢复筛选遗漏

- 原等待步骤关联已BLOCKED、确定未发送封面时，RETRY仅查FAILED导致漏选；步骤先失败再重试会重复分轮。将明确三种持久等待状态且满足原COVER_RETRY/权限/无外部效果证据的步骤纳入显式批次重试。不修改冻结数据；已armed/receipt/活动claim仍禁止重传，未来任务仍走10组合准入。
- [x] 新六项回归先验证未发送等待遗漏、已发送仍禁止；修复SQL并跑原恢复/容量/权限回归：51 passed，Ruff/format/ty app通过。
- [ ] 完整备份发布后使用原产品批次恢复，继续180组合验收。

## Task 6：完整封面候选查询的规划开销

- 23e版本已备份部署，RETRY累计调度416项；服务器新增六项恢复回归通过。继续观察发现封面候选完整SELECT在17时段有12次5秒SQL超时，单独窗口查询的4ms证据不能覆盖三重嵌套候选查询。
- 实际服务器只读EXPLAIN：旧查询8.85秒，JIT编译8.62秒；仅物化候选降至3.88秒仍不足。候选物化一次，绑定依赖改为排除NULL的tenant/job集合反连接，取消相关EXISTS的高估成本。新查询32.189ms且无JIT，不修改数据库配置/超时。
- 同一只读REPEATABLE READ快照比较旧/新查询，55条完整行及顺序完全相同，7.22957秒→0.02815秒；预算0.5秒通过。万组合测试同时构造1万封面/资产依赖并执行完整候选SQL，仍10个候选。
- [ ] 完整执行/封面准备与未绑定素材库回归通过后发布，核对真实超时消失并持续完成原180组合。

发布前证据：完整执行与容量15 passed /103.90秒；封面吞吐/独立素材库/封面创建和恢复98 passed /177.56秒；Ruff/格式/ty app通过。

## Task 7：历史未发送封面批次准入

- [x] 复现历史已分批任务不受窗口约束：future用例领取非空而失败；以未完成成员为准整批准入，保持成员和发送账本不变。mixed允许接续、armed允许核查，future暂停并由正式repair在任一成员进入窗口时唤醒，wake自身不必当前。
- [x] 三项新增及原窗口规划/领取共5项通过。实际服务器只读完整任务准入SQL执行19.036ms、无JIT。吞吐测试来源查询计数原把200次准入查询误算为来源读取，补充SOURCE参数识别，保留20次来源读取上限。
- [ ] 执行/容量/封面回归、完整备份发布、服务器测试与持续原180组合业务验收；不以局部通过结束目标。

## Task 8：已恢复依赖与失败步骤重新衔接

- 服务器一个FAILED素材步骤关联未发送PENDING封面，既不能进窗口又不属原BLOCKED重试候选。仅在原严格未发送/无回执/无活动领取投递栅栏下，显式RETRY允许失败步骤接回PENDING依赖；不再给该封面投递新消息，保留原批次唯一wake。
- 新4项先复现recovery_no_candidates，修复后与恢复/容量/封面创建共76项通过93.26秒；armed、claimed、已有dispatch仍拒绝。

## Task 9：图片分页固定边界永久漏项

- 真实只读证据：账户总数156，100条两页得到100+56条但3条重叠，唯一数153；额外用71条页的第2页获得71条，其中3条新ID，唯一数达到156。另一账户141总数两页唯一140，重复第一页无新增；不同MID导致相同image_id的假设已排除。
- 正式共享扫描保留最多三轮；第二/第三轮改用71/53条页移动边界，实际响应页大小/数量/页数严格校验。同一总数跨轮唯一ID达到总数后即可结束扫描；库存变化仍最多两次重启，达到10000搜索上限仍不能形成负向结论，已发送批次绝不重发。
- SDK/MCP共用读取合同增加可选page_size（默认100），仍在官方1～100范围内，游标查询范围不超过10000。续跑状态持久化实际页大小，不修改已有发送账本。
- 新8项边界、缺项、已发送和双通道页大小回归先失败后通过；整组回归与发布验收继续。

## Task 10：共享后及源账户自身的精确正向回读

- 真实已共享批次按4个源MID查询目标，一次返回4条，签名、尺寸和实际目标图片证据全部匹配。现有每目标完整库存扫描重复读取141～530条，不是必要的成功证明。
- 已armed批次或目标为实际来源账户时，每次核查代数先按最多100个原MID读取一次；复用同一严格内容校验并发布实际目标image_id。只接受匹配正证据，空/部分响应不构成不存在证明，继续原完整分页核查；不重发共享、不自共享。分页总数变化重启时保留已探测标记。
- 新共享正证据/空结果两项及来源自身两项断言先失败后通过。原边界、库存变化及部分拒绝测试补齐精确探测后的完整查询响应，保留原无重复发送、UNKNOWN和拒绝状态断言；完整回归75 passed /144.71秒，Ruff/格式/ty app通过。
- 发布不改配置、额度、迁移或冻结广告参数，继续以原180组合真实回读作为最终验收。

## Task 11：同账户有界连续分页

- 实际批次40余个小批次交错逐页，期间其他共享使同账户总数变化，造成多次重启。真实只读测量建立0.978秒、首次读2.288秒、同连接第二次读0.654秒；同会话连续读取可减少重复初始化和相邻页排队间隔。
- 同一目标连续至多4页，每页仍受原配额与权限复核；账户完成、总数重启、精确探测或剩余预算少于20秒即让出，保留持久游标。完整核查后发送前预算少于15秒则另一次正式任务发送。没有上调超时/并发，没有改队列优先级。
- 图片传输合同仍严格验证每页；跨页重叠由持久扫描器统一做唯一库存计数及三轮补齐，不由会话内另一套逻辑提前拒绝。视频原分页合同不变。
- 新真实PG/Redis连续四页补齐和页数上限两项先失败，预算两项通过；完整回归与后续发布验收继续。
- 完整回归166 passed /1 skipped /222.61秒（SDK不适用MCP文本载体），含原分页恢复、默认4页上限、预算让出及下一次只发送一次；Ruff/格式/ty app通过。三个逐页断点用例族显式将页数缩为1，默认4页由新独立完整用例验证。

## Task 12：首次共享的库存查询限定本次源MID

- 连续分页部署后真实MATERIAL仍仅249→255，进一步确认首次共享前的全账户库存负向核查是根本的请求放大来源。官方共享合同限定20源MID×10目标账户，目标同名素材会拒绝；不把这种拒绝当成功。已验证真实源MID筛选返回目标实际图片及相同MID。
- 新首次共享只查询本次冻结源MID集合，并严格检查返回的有限集合是否完整；匹配者复用实际目标ID，缺项走原账本仅首次共享一次。不以全图库同内容/不同MID/不同名称的未登记图片作为首次共享的前置依赖；本地已有可信映射仍走原只读复用。
- 这不是对UNKNOWN补传授权：已armed的空筛选、部分筛选及来源自身目标未匹配仍进行完整核查；同名拒绝也必须通过内容证据回读，不换名或重发。库存查询不完整时仍不能因空/缺项发送。
- 无新接口、配置、额度、迁移、账户或广告参数变化。保留原复核链路与每请求权限检查，用完整回归验证首次缺项、部分复用、未知不重发和来源不自共享。
- 新首次/部分复用两项先失败；旧已复用且无POST的只读续跑先失败为PENDING，修复任务路由与批次READY重置；每个旧完成库存目标仍须定向探测。最终84 passed /158.92秒，Ruff/格式/ty app通过。原多页用例用不完整筛选响应进入全库存核查，未删除缺项/重叠/无重复发送的安全断言。

## Task 13：SOURCE未知上传的分页补齐

- 原批次2个SOURCE均已armed且无回执/MID/候选，分别在531总数第4页及118总数第2页终止；它们仍使用单封面旧扫描分支，之前仅修共享批次没有覆盖此处。各引用原批次10个素材步骤，禁止重新上传。
- 保留原核查代数与逐页持久证据，next_page作为内部读取序号：首轮100条边界保持已有数据含义，后续71/53条补齐，最多三轮。数据库按同一代数的唯一image_id计数；只有同总数完整集合且唯一文件名候选才读取详情发布，缺项/歧义/总数变化/10000可见性上限仍UNKNOWN。
- 不增加字段或迁移，不改写历史页，不把重复图片计为新增，不延长任务时间。新增成功补齐/三轮仍缺项先失败；原重复页测试改为三轮后仍阻断，原总数变化与不可重发断言保留，完整回归进行中。
- 最终105项通过169.64秒，Ruff/格式/ty app通过；仍须正式发布和原批次业务验收。

## Task 14：完整定向查询在原预算内接续目标

- 实际旧小批次每个账户即使一次MID查询即完成也退出，再进入准备队列等待；一次会话建立约1秒，任务约5秒而只做一个有效查询。保留四次读取总上限与20秒预算门槛，只有本次新MID查询完整落定目标时才接续下一账户。不完整/UNKNOWN空结果仍让出或后续全库存核查，所有逐请求权限、额度、账本不变。
- 新4/5账户用例均先失败（实际只查询1个而非4个）；覆盖四次上限、剩余账户持久续跑与一次共享。完整回归及原断点用例验证进行中。
- 已读到正证据立即持久发布，不能因下一账户读取失败丢掉本次成果；保留原partial-publish用例默认4次验证。三个历史跨任务断点用例显式1次读取，新独立用例覆盖默认4次。最终86项通过155.51秒，Ruff/格式/ty app通过。

## Task 15：无效原件的无限自动校验

- 真实19:47诊断：7个未关联原提交的原件已标invalid_video，却沿同一dispatch累计投递2289～2468次，仍每60秒重新下载/ffprobe。19:35起的窗口内40次validate_original累计408.76秒，占用唯一准备槽；不能只优化广告任务而忽略此持续争用。
- 原代码把所有校验错误都当瞬断重排，repair也无终止条件。修复为仅存储不可用/期限/校验器缺失可自动重试，沿同一dispatch最多3次投递；无效内容/元信息错误等明确失败解除该文件的活动dispatch引用，保留对象、失败记录和已存证据。历史终止错误在下载前退出，repair不再补投；新代数重传仍走已有产品入口，不清队列/不删原件。
- 五项真实PG新回归中四项先失败：坏字节仍有dispatch、两种历史永久错误被repair重投、第三次瞬断仍重排；第一次瞬断正常保留退避用例通过。完整导入/恢复/原件/HTTP边界回归进行中，无迁移/新开关。
- 最终86 passed /1 skipped（Linux prefork）/98.91秒，Ruff/格式/ty app通过；准备完整备份发布及服务器Linux验证。

## Task 16：未知目标结果的空库存扫描终止条件

- 目标分发在完整分页未找到名称/摘要匹配后仍重置第一页并每60秒调度，再被repair补投，无法结束一轮核查。13项URL未知仍无补传授权，反复查询不是发送结果证据。
- 完整空查仅登记本轮核查结束，保留result_unknown，不推断definite_no_effect。旧消息、批读候选和repair不能重新自动扫描；正式显式只读核查建立新代次，仍不允许上传。非末页继续持久分页，找到候选仍走详情验证，未改变错误/库存变化的原处理。
- 新真实PG/Redis用例先因缺少终止状态失败，覆盖旧消息重放、repair不补投、正式只读再次核查及分页中途续跑。广泛分发/恢复回归进行中；与Task15一起发布，不修改额度、并发、冻结意图和新鲜度。
- 扩展169项通过，另一个新增分页夹具非末页空列表被schema守卫拒绝，修正真实响应结构后分发/批读70项全通过177.20秒；Ruff/格式/ty app通过。完整空查之外的库存变化/歧义/读取异常未在此轮扩大修改。

## Task 17：来源图片库存变化的有限重新核查

- SOURCE完整库存核查在同账户并发入库导致总数变化时仍立即终止（真实535…变化），此前只补了跨页重叠边界。沿现有连续故障预算允许重新开始，第三次故障终止；清本轮候选并换search_round，旧页留存且不拼接，不新增发送。
- 两个真实SDK边界回归覆盖变化后稳定可完成、持续变化保持UNKNOWN且无活动dispatch。最初夹具尾页数量不足，被原schema守卫拒绝；按真实数量修正后，移除新分支再次验证两个用例确实失败，再恢复实现通过32项。旧显式核查测试扩展到三次总数变化，回归进行中。
- 无迁移/开关/并发变更。20:24UTC原批次6/180完整、AD13/360，7个无效原件活动dispatch引用均已解除，原生共享一项空查已结束；13项URL仍沿剩余分页核查，继续监控不冒充整批完成。
- 最终来源/封面/吞吐/创建恢复91项通过177.85秒，Ruff/格式/ty app通过，准备发布。

## Task 18：已知共享封面的刷新不再退回库存扫描

- 当前窗口视频已过900秒而封面仍VERIFYING，已知目标ID因share_batch_id路由到共享搜索。只对同租户、BC、actor、冻结路由且原成员全部仍有目标ID的批次复用既有视频/图片批读；历史UNKNOWN不抹掉已知ID，不修改共享账本、不重发。缺失ID或拆离成员仍保留原批次wake。
- 新回归覆盖READY/UNKNOWN账本、图片缺项、缺ID及拆离成员；原实现先失败。完整四文件100项通过314.32秒；最终候选查询改为按批次ID相关EXISTS后继续复跑新用例与100/1000/10000项容量。Ruff/格式/ty app通过。容量测试不代表平台真实创建吞吐。
- 20:51UTC仍8/180完整、AD16/360、素材齐20，13项URL未知完整空查已结束但补传尚未获确认。继续完整备份发布、正式恢复和真实批次验收。

## Task 19：批量封面与广告刷新的封面行锁顺序

- 21:03:55UTC实际PG日志deadlock，两端均等待material_cover_job；同刻46个已知ID任务被本地资源错误终止。批读成员列表从首消息开始，后续HTTP前检查与结果持久化依此持锁；广告刷新按job.id排序，两者顺序相反。批读统一按job.id排序，不改变图片绑定或外部参数。
- 真实PG记录每个批读事务的实际锁序，新用例先失败，再修复；相关44项通过90.40秒。另Redis传输层故障用例证明第一次本地瞬断被直接UNKNOWN，第三次正确终止。仅只读阶段、明确retryable的本地资源错误允许沿原job三次有界退避，不扩展上传重试；已知/未知ID路径均增加覆盖，完整回归中。
- 21:10UTC13/180完整、AD29/360、素材齐25，1357版本原产品RETRY已COMPLETED/17。不将死锁重试当根治，必须同时统一锁序并通过服务器验证。
