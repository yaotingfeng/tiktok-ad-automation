## 2026-09-18：变化中封面库存有界重查（已发布，整批仍未完成）

- 真实同一目标连续只读分页总数 121→122，且两项跨页重复；其他共享正在改变库存。旧程序任何总数变化即阻断，当前组合也已被该错误阻断，不能仅以队列下降报告完成。
- 新增真实 PG/SDK 传输回归先复现 2 failed；变化时丢弃该目标旧分页证据，从首页重查，最多重新开始两次。持续变化仍 BLOCKED/UNKNOWN，不拼接不同总数推断不存在，不因重新核查重复发送已 armed 请求。
- 未发送稳定/不稳定两分支和已发送稳定/不稳定两分支 4 项通过（11.14 秒），初轮吞吐 47 项通过（103.13 秒）；扩展吞吐/双通道/租户素材库/来源回归 136 项通过（275.00 秒），Ruff/格式/ty app 通过。旧“显式重试清游标”测试保留，改为超过有界重查后才走产品重试。
- 为完整备份重新核对四套归档 SHA、解压内容及 950 历史，确认零会话后清理临时恢复库 tt_ada_tenant_library_2467c7f8_test 和四份已验收解压副本 B2PzXP3B/igzGRiA7/JyhK5WRU/kZ6IB80Y；均可从对应保留备份重建。业务库、全部备份、版本未删，可用 1512620 KiB；发布前仍需再核对容量。
- 第一次发布被磁盘余量守卫拦截，未停服或切换；另核验 20260917T132835Z 五归档及项目/配置逐文件一致后移除临时副本 HzXVdoGw，可由备份重建。保留守卫，重新发布成功。
- 6fd9c5e2897e11dab8078e830ad816856d54c0d7 已推送并部署，完整备份 20260917T162520Z、五归档/1252 文件/21 表/950 历史独立恢复一致。五服务九进程、三个 Worker、双身份隔离/HTTPS/前端与原配置验收通过；服务器新增 7 项回归通过（70.24 秒），专用角色恢复、空测试库移除。
- 原提交 RETRY 请求 f0200fe8-f1f2-533b-834c-532977ec83ae / 任务 82060b4a-ef65-4744-ab37-4799446433ec 完成 2 项正式恢复调度；RECONCILE 请求 2dffc97a-dff2-5998-a2fe-77cdc6a401cc / 任务 884689c2-7464-4b3b-96f7-5c6856312cc7 已受理。16:26 UTC 完整仍 2/180，素材 158，结果队列 8873；不是搭建完成。继续只读分析正式消费者耗时，未清理或搬移业务队列。

## 2026-09-18：封面依赖持久等待（已发布，整批仍未完成）

- 原正式素材窗口已将 builds 队列降至 0，但 BUILD 封面等待来源时仍每 5 秒投递，和来源核验争用资源 Worker；旧未发送未来封面也未遵守素材窗口。两个新增真实 PG 回归分别先失败再修复，不动业务状态和队列。
- 目标等待来源改为持久等待，由原 repair 在来源落定后投递；未来未发送封面等待原组合窗口，窗口接续后自动恢复。共享任务任一当前组合可准入，未绑定搭建的素材库操作不限制；来源、已发送、显式只读及已绑定批次保持原核查。
- 81 项封面/来源/租户素材库/执行回归通过（147.85 秒），覆盖正式恢复唤醒和后续共享；已发送/双通道/批量容量（含 10000 项队列）补充 167 项通过（354.04 秒）。Ruff、格式、ty app 通过。测试排除“重试未清分页游标”的错误假设，未改分页逻辑。
- 16:07 UTC 原批次完整仍 2/180；SOURCE READY 559、BUILD READY 249、结果队列 9502。161 个未发送封面库存分页失败继续核查，13 个未知来源上传没有盲目补传。上一版服务器隔离 25 项回归通过；临时 f0698e38 恢复测试库已核验备份和零会话后移除，可从 dump 重建，业务与备份保留。
- 已推送并发布 2467c7f8dd5a3b371c55fd05b37372f3b9ed9961；完整备份 20260917T161519Z、五归档/1251 文件/21 表/950 历史独立恢复一致。五服务九进程、三个 Worker、配置/额度/前端/定时器一致；服务器独立回归 22 项通过（66.75 秒），角色恢复 NOLOGIN/NOCREATEDB、临时空测试库移除。16:18 UTC 完整仍 2/180，资源结果 9081，封面 BLOCKED 271，继续修复库存变化的有界重新核查。

## 2026-09-17：原批次正式调度活性（已发布，整批仍未完成）

- 用户明确要求修代码使原批次完整执行，不再用临时提队列、手工改状态代替修复。按 [实施计划](superpowers/plans/2026-09-17-build-pipeline-liveness.md) 继续，不创建新提交或变更账户、预算、冻结路由。
- 实测 15:18 UTC 仍为完整 2/180、MATERIAL 158/4770、结果队列 15556、builds 7430。定位旧来源观察消息在 revision/claim 校验前可建立新读取代次；新增两例真实 PG 回归均先失败，再修为快速退出并持锁复核。
- MATERIAL 等待视频/封面改为持久依赖等待，不每 15 秒新排步骤和空唤醒组合。结果落定后由现有 repair 定时器排原执行器一次，仍检查完整权限、冻结路由、视频/封面新鲜度及未知结果安全边界。
- 每提交只推进一个可执行组合的素材，完成后自动接续；UNKNOWN/明确失败保留原核查并让出窗口。旧版本已排队未来步骤沿正式执行器转为等待，不清队列、不搬移、不手工提优先级。无迁移、配置、额度、Worker 并发或新鲜度修改。
- 素材/广告/封面/批量与锁序相关 107 项通过（273.02 秒）；追加旧队列接管及视频过期回归 2 项通过。调度、MCP、素材恢复及完整步骤补充 40 项通过（298.55 秒，含重叠）。包含两组合完整创建与回读、单次创建保证，Ruff/格式/ty app 通过。随后执行完整发布备份及隔离恢复。
- 为完整发布预留空间：复核 20260917T144302Z 五归档 SHA 和 RELEASE_COMPLETE、确认零会话后，仅删除上一轮临时恢复库 tt_ada_tenant_library_5c16ce11_test（约 176 MiB，可由同批 dump 重建）；业务库、全部备份、运行与历史版本保留，可用空间约 1.55 GiB。
- 已推送并部署 f0698e38b763353ebf0824896f060780a7f4a808；完整备份 20260917T154301Z、五归档/1248 文件/21 表/950 加密响应独立恢复一致。五服务九进程、三 Worker、双身份隔离及前端/配置保持通过。15:47 UTC 正式代码已将约 1360 个未来素材转入持久等待，builds 降至 4362；完整仍 2/180。服务器回归和业务持续验收见 [记录](validation/2026-09-17-build-pipeline-liveness.md)。

## 2026-09-17：跨共享批次核验锁序（已发布，整批仍未完成）

- 14:27:12 UTC服务器再次记录40P01，双方等待的是material_share_batch，不是已修复的广告素材锁。批量核验按素材顺序逐项更新不同共享批次；不同账户窗口可用相反顺序持有批次锁。另外共享收口先锁批次再锁素材，与核验路径反序。
- 统一结果落账锁序为全部素材、全部操作、按ID排序的全部共享批次；共享收口也先锁成员素材/操作。保留逐项claim、冻结路线、摘要、VID和有效期校验，没有扩大重试权限或重发未知上传。
- 新增两个真实PG并发回归：跨批次等待低ID时不能持有高ID，以及共享收口等待素材时不能持有批次。正确复现2 failed后修复为2 passed（3.72秒）；批量共享/双通道/未知结果/并发及广告锁序56项通过（180.80秒），Ruff、格式、语法和ty app通过。
- 已验收的2ddeff79临时恢复测试库在重核五归档SHA、确认零会话后删除，约171MiB；业务、备份和版本不动，临时库可从20260917T142416Z的dump恢复。
- 5c16ce11c84b37cd3f86df9a31451a4bdde396b1已推送并于14:43 UTC发布，完整备份20260917T144302Z的五归档/1247文件/21表/949响应独立恢复一致；head、配置、额度和前端不变。五服务九进程、三Worker、登录隔离/HTTPS、索引1项/Linux3项和服务器锁序2项（32.56秒）通过。专用测试角色按手册临时启用后已恢复NOLOGIN/NOCREATEDB，专用测试空库移除；恢复库历史复核不变。
- 原提交本次RETRY无可安全重发候选，未创建恢复任务；RECONCILE请求d87ebfe2-0aef-576c-8915-da407b7354b8/任务8a365af7-daa7-41ae-9a83-b1dd561e76d6完成14项只读恢复调度。不是14项业务成功，也不是13项未知上传已补传。
- 截至14:51:22 UTC连续监控，完整仍2/180、MATERIAL158/4770、AD4/360、READBACK8/720。131个MATERIAL UNKNOWN（130项源上传依赖及1封面）、封面图库分页和深队列仍阻塞批量推进；资源准备队列0不等于全部完成，结果队列15358、builds6679。六轮精确当前消息优先级操作已结束，未清队列、未增加Worker或留下临时消费者。13项无回执上传仍需用户对有限补传作明确选择。

## 2026-09-17：广告准备与素材批读锁序死锁（已发布，第二单元完整成功）

- 第二个原单元 `29a9c0d9-c032-41a3-8227-089a51e833dd` 已完成28素材、计划、广告组及两项回读，但两条未发送广告再次出现通用准备失败。14:09:59和14:11:37 UTC PostgreSQL日志确认 `40P01 / deadlock detected`：广告按冻结素材排列逐项加锁，与批读按素材ID加锁相反；不是原跨BC约束问题复发。
- 广告准备先按同租户素材ID锁定整个冻结组，再按原排列编译；不改账户、素材、预算、路线、证据有效期或发送语义。真实PG并发回归先失败后通过，并断言创意顺序不变；相关创建、双通道、封面及素材执行51项通过（271.31秒），Ruff/格式/语法与 `ty check app` 通过。无参数全目录ty另报3911项诊断，未将其报告为通过。
- 14:21 UTC整批仍为完整1/180、MATERIAL158/4770、AD2/360；新单元计划/组虽成功，不能视为完整创建。原13项无回执上传仍未重传。图库重复分页及出队等待仍在，按源MID的只读探测有效，但不以查不到同MID冒充全库不存在，也未变更图片共享规则。
- 经确认无活动会话并重新校验全部备份归档，仅清理本轮四个临时恢复测试库（218c64f5、b6462b09、5d5b6550、3ff86a3b），约599MiB；业务库、全部备份和版本保留，测试副本可从对应dump重建。磁盘可用从约1.2GiB恢复到1.9GiB。
- 已推送并发布2ddeff79d53764ce6c971459038097e6d56b94ab；完整备份20260917T142416Z，五归档/1246文件/21表/949响应独立恢复一致，head、配置、额度、前端不变。五服务九进程、登录隔离、服务器索引1项/Linux3项及真实28素材组锁序回滚验证通过。
- 原提交RETRY请求56ba5572-7c68-53b2-bd43-cbc187559393，恢复任务657937a3-1863-48f5-ab00-09c94c87fc0b已完成5项安全调度。14:31 UTC第二单元全部完成，整批完整2/180、AD4/360、READBACK8/720；已成功计划/组未重建。14:36仍2/180，131个UNKNOWN素材步骤和队列积压尚未解决；不是整批验收成功。

## 2026-09-17：排队中的已有VID复核未进入批量（已发布，持续恢复）

- 原批次实查1,236条分发为queued，但已有VID且操作verifying/read_only。批量入口只接收verifying/result_unknown分发，导致这些正常新建复核任务始终逐条读取。已知VID现在包含queued分发；无VID共享发现范围不变，操作状态/代数/租户/账户/冻结路由/逐项摘要和响应检查不变。
- API/MCP新增queued回归先失败，再修复；含50VID查询预算、缺失成员、并发与撤权的素材批量53项通过（177.59秒），队列/outbox45项通过，Ruff/format/ty通过。
- 实际35,220条唯一排队消息完整只读索引1.63秒，100次深部LPOS1.39秒；原1秒快照稳定漏尾部、逐项深扫描挤占outbox。短期索引预算调为3秒，保持40,000消息上限、5秒缓存、完整身份与实时LPOS；受控时间测试先失败后通过。
- `3ff86a3`已推送并部署，完整备份 `20260917T132835Z`、五归档/1,246文件/21表/948加密响应独立恢复、五服务九进程/三Worker及双身份登录隔离验收通过；配置、额度、head及前端未改变。服务器隔离索引1项、Linux队列3项通过，补充本地调度drain4项通过。
- 原提交RECONCILE请求 `b9422ebb-9f09-54a2-9305-d7bbab95b3b8` / 恢复 `99bec1b2-3c1e-4647-8ffa-09b454f67db9`，按既有处理器完成17项恢复调度，无直接平台调用。仅有明确未发送证据的任务允许原流程新准备；13项无回执上传未补传。
- 13:41 UTC原批次MATERIAL146、AD2、完整1/180，仍未整批完成。IMAGE共享已出现一个7成员READY批次。只读核对约2.48万结果消息中约1.98万属于旧revision/claim，工作进程已开始自然消费并在55秒内从25,859降至24,090；未删除或隔离消息，原消费者每轮恢复。
- 40项原生共享在发送前读取失败，持久证据为FAILED/definite_no_effect；重读其中一批20源VID全部匹配，可由原准备流程安全恢复，但不能将此证据套用到13项未知URL转存。索引在高并发下仍可能不完整，保留有界补查；没有宣称队列根因已全部消除。

## 2026-09-17：封面共享候选窗口与重叠分页（已发布，继续恢复）

- 已知封面回读恢复后，49 项真实源封面转 READY，但共享准备仍扫描全部候选。现在每次只处理包含原锚点的20素材×10账户窗口，未选任务保留原投递；共同授权复用扩展到领取和结果发布，来源、图片、账户边界不变。
- 平台真实总数118的第一页100项、第二页18项有3个重复ID。原实现立即阻断；现在只有总数稳定且去重库存计数完整，才允许完成负向库存检查，最多三轮。总数改变、重复页内ID、超出10000及三轮仍缺项继续阻断，不盲重发已发送请求。
- 真PG 31素材×10账户规划回归原查31源、修复查20源并领取原锚点在内200项；相邻页重叠的成功/缺项用例先失败。规划相关115项通过，分页补充后相关71项通过；Ruff/ty通过。真实只读补查第三轮达到118唯一ID，无远端写入。
- `5d5b655` 已推送并部署；完整备份 `20260917T131020Z`，五归档/1,246文件/21表/948加密响应独立恢复一致；五服务九进程、配置/配额/前端字节、timer及双身份登录隔离通过。服务器隔离恢复库的候选窗口与重叠分页增量3项通过（115.62秒），无真实平台调用。
- 原批次仍未整批完成，13:16 UTC MATERIAL130/4770、AD2/360、完整单元1/180；新计划失败为未发送的 `dependency_failed`，依赖恢复后由原调度器处理。未发送封面通过既有安全retry，已发送只核实；按原身份有界补发未发布封面outbox/优先消费续页，不创建新业务任务、不改变广告意图。
- 续页消息仍被旧核实/补投积压延迟，正在有限轮次推进和监控；13项无回执上传仍待用户有限补传确认。真实业务未验收，不将局部测试/服务健康视为整批完成。

## 2026-09-17：封面批量核验的重复授权查询（已发布，业务继续恢复）

- 继续监控原提交发现已知图片只读批量核验反复超时。真实 PostgreSQL 回归中 50 项的 BC 绑定查询：API 920、MCP 2,168 次；先失败后修复。
- 只在同一次短事务复用共同账户/冻结路由/租户动作授权，claim、摘要、VID/图片身份、映射和回执仍逐项检查。每次真实 HTTP 回调、响应发布都新建核查范围，不跨请求或事务缓存。
- 封面与广告依赖相关 158 项通过；查询预算、跨事务禁止复用及图片响应后撤权增量 5 项通过（含重叠）。Ruff/ty 通过，无迁移、配置、额度或前端变化。
- `b6462b0` 已推送并于 12:46 UTC 发布，备份 `20260917T124524Z`、1,246 文件/21 表/948 份归档独立恢复、五服务九进程及 Web/三 Worker 验收通过；服务器隔离回归 5 项、Linux 队列 3 项通过。原配额和配置不变。
- 12:50 UTC 原批次 MATERIAL 成功 111、分发 READY 聚合 2,277（含历史代数），AD 仍 2。13 项旧上传未知结果没有上传响应归档，也未在冻结名称查询找到，仍待用户有限补传确认，不伪造无副作用结论。详见[连续验收](validation/2026-09-17-material-queue-recovery.md)。

## 2026-09-17：积压消息补投放大与素材核实饥饿

- 后续完整真实链路已通过：原提交一个单元的 32 素材、1 CTA、1 Campaign、1 广告组、2 广告、4 回读全部成功；原 1,339 失败均实际重试、原错误为 0。09:38 整批 MATERIAL 成功 58、封面 58；其余队列与 130 个未知结果步骤未完成，未清队列或重发未知上传。有限优先级操作已结束，没有残留临时消费者；详见同一[验收记录](validation/2026-09-17-material-queue-recovery.md)。

- 最新运行 `d22bde3`，完整备份 `20260917T091246Z`、五服务九进程/三 Worker/Linux 三项及 Web 验收通过。09:14 outbox 到期待发归零，连续观察无长时间恢复扫描；09:17 READY 素材 820、封面 25，仍在核验完整广告链路，未声称整批完成。

- `eef056c` 已推送并部署，备份 `20260917T085826Z` 独立恢复通过；21 表/947 响应、五服务九进程/三消费者/登录隔离及 Linux 队列三项验收通过。17,465 条旧结果消息原字节搬移、总数不变；READY 226 → 519，尚未整批完成。
- 后续查出恢复 SELECT 在 Worker 退出后仍运行数分钟：UUID 主键转字符串导致相关扫描。改用有非法值保护的消息 UUID 转换，限定本事务 SQL/锁期限并关闭 JIT；新旧计划/畸形消息/恢复代数回归 9 项通过。隔离恢复库新查询 100 候选、3.840 秒，全部回滚；扩展旧测试 7 项已证明基线同样失败，详见[本轮验收](validation/2026-09-17-material-queue-recovery.md)。

- 真实队列中既有相同 ID 重复消息，也有不同核实代数；消息数不等于素材数。真实 PG/Redis 复现三次补投把一个仍排队消息变为四份。outbox 现在按 ID、任务、租户、操作者、完整参数核对队列，实时 LPOS 确认才抑制补投；丢失消息及错误身份不被缓存吞掉。
- 核实/封面使用 `resource-results`，同一资源 Worker 交替消费两个队列，并发仍为 2。发布窗口在完整备份后按原字节搬移旧结果消息，前后 SHA-256 多重集合必须相等；不清队列、不删除重复副本。
- 50 个视频每次发送前的共同权限查询原为 1,550 次；保留逐成员冻结边界和每次物理请求前的新鲜检查，共同账户授权只查一次。真实 PG 回归要求不超过 220 次并全部 READY。
- jobs 与批量素材回归 131 passed / 1 skipped（Linux 专属），Ruff/ty 通过；消息搬移和跨 BC 增量验证及服务器部署结果继续记录于本次验收。无迁移、新开关或上游额度变更；生产未部署。

## 2026-09-17：跨 BC 素材准备与共享后验证阻塞（两项修复已部署，继续观察业务）

- 新加坡测试服当前批次 18 剧 × 10 账户、180 组合；素材步骤出现 `execution_prepare_failed`。回滚事务重放 `ensure_target_asset`，确认实际异常为 PostgreSQL `23514 / material route connection scope mismatch`，发生在首次跨 BC 转存任务入库，未进入素材远端发送。
- 根因：`material_source_bc` 调整来源字段约束后遗漏通用 `mcp_material_route_guard`；该触发器仍按目标 `NEW.bc_id` 验证来源连接。两份独立授权因此被误拒绝，原测试复用同一连接绑定两个 BC，未覆盖真实差异。
- 新增前向迁移 `material_route_scope`，按各字段已冻结的 BC 校验连接绑定，保留租户、通道、来源字段 CHECK 和路由不可变规则，不改历史业务行。存在跨 BC 冻结证据时禁止降回旧校验。
- 测试先复现 3 failed / 2 passed，修复后相关 64 项通过；补充历史升级/降级保护专项通过情况见本次验证记录。发布须完整备份并恢复验证；恢复使用原提交的 `RETRY` 请求，禁止清空队列或重复提交广告。
- 用户明确授权推送、部署并监控后，已推送及发布 `9599fd7b2f8915b3d24125a1af6dbbf6733c62e5`。备份 `20260917T065027Z` 完成独立恢复和迁移演练，21 表/944 份归档历史一致；head 为 `material_route_scope`，五服务九进程、三 Worker ping、双身份登录隔离通过，配置和前端产物保持。
- 已为原批次登记幂等 RETRY，初始 1,339 个安全候选；恢复任务分页推进，观察到原错误不再新增、跨 BC 任务已成功入库并进入真实上传/核验。后续统计见[验证记录](validation/2026-09-17-cross-bc-material-route.md)，不得把恢复登记数量当成业务完成数量。
- 持续监控确认原 RETRY 完成，但共享后 MID 发现一次传入最多 50 个，真实只读平台回执为 `40002 / maximum number of items is 20`。新增 21 个成员的真实 PG/Redis + MCP 传输回归，修复为 20+1 分批核实，不重复共享；已知 VID 详情 50 条上限保持。队列重复投递和未知上传仍须继续跟踪，不能把 CTA 成功当成广告完成。
- 补充修复的批量分发/并发/跨 BC 约束 55 项通过，Ruff、格式、语法和 ty 检查通过；后续发布仍执行完整备份和恢复验证，沿原任务恢复。
- 补充修复已推送并部署 `c85cca1cfd4905e0430ecbf530c2602672269ffd`，完整备份 `20260917T080601Z` 独立恢复验证通过，1,240 文件、21 表和 946 份加密响应一致；head、配置、并发和前端不变。五服务九进程、三 Worker、登录隔离通过；08:07 UTC CTA 180/180 完成，广告仍等待素材，继续按真实结果验收。
- 08:11 UTC 已核实分发从 49 增至 112，平台小批查询 12/12 返回并匹配真实内容；原数据库错误未复发。但仍有 19 个素材 UNKNOWN、验证超时和 resources 14,431 条积压，Campaign/AD 尚未创建。临时抽查消费者已排空撤下，原任务和未知回执保留；后续须处理队列恢复放大与平台结果核实，不能标记整批完成。

## 2026-09-17：单条素材失败隔离与预算显示（已发布新加坡测试服）

- 线上只读定位：22 剧×10账户中 4 剧无素材、6 剧被 7 条 invalid_video 入库失败素材拖累，旧预览仅 120 个可提交 Campaign。重查 60 个受影响组合均有可用素材，按新规则预计 180 个；未修改或重新生成线上预览。
- 按账户冻结跳过的素材、文件名和原因；保留其他有效组，空组不生成、全空才阻断。预览、提交、AD 请求、封面最后核验、恢复及任务展示共用排除规则；权限/场景错误不降级为警告。新迁移 `preview_skipped_materials` 有复合外键和冻结写保护，旧预览历史不改写。
- shadcn 页面展示跳过计数与分页详情，中文原因可追查；Decimal 指数按精确字符串展开，`0E-12` 正常显示 `0`，未修改实际策略预算。
- 最终后端整合 122 项、前端准备/预览 106 项通过，预算与策略另 79 项通过（有重叠）；构建、ty、改动 Ruff/Biome、Alembic check 通过。详见[验证及发布边界](validation/2026-09-17-preview-partial-materials.md)。
- 用户授权后已推送并部署 `06bd724b15bddf62f96e5a8a81d658a52b7f8046`；完整备份 `20260917T062358Z`、隔离恢复与迁移演练通过，18 表/920 份加密响应历史一致。head 升至 `preview_skipped_materials`，五服务九进程、三个 Worker、登录隔离、新排除接口及前端产物验收通过；配置不变。旧冻结预览不改写，需用户返回调整并重新生成，未代提交广告。

## 2026-09-17：冻结预览整段提速与动态进度（已发布新加坡测试服）

- 用户确认继续压缩整批耗时，并要求明显的 loading/进度。短批次复用公共权限、只读主账户与同账户/Mini 场景；每轮结束重新核实最新事实，下一轮重读，保持链接/素材及租户、BC 边界。强制刷新 MiniTarget 以识别其他会话变更，冻结路由每轮只加载一次。
- 进度接口固定分母并只统计完成组合；前端 shadcn 加载动画、阶段、真实进度、计时、停滞/网络/权限提示，汇总结束前不冒充完成。默认计算预算 3 秒，单步及最终检查可能额外耗时，无迁移、依赖和开关变更。
- 同服隔离恢复库完整后台重放 220 组合：37.8566 秒/36,074 次读取降至 13.1762 秒/7,882 次，120 可提交、100 阻断及原因一致；不含消息排队/浏览器轮询。所有测试事务回滚，没有真实广告操作。
- 较广后台回归 211 项、最终专项 106 项、前端准备/预览 100 项通过；构建、Ruff/Biome、ty 通过。独立复核问题已修复，详见[验证记录](validation/2026-09-17-preview-batch-progress.md)。
- 用户授权后已推送并部署 `219f51e054789ecbff0e2078b3c9fb84a85c5fa4`。完整备份 `20260917T050329Z` 与 PG/Redis/项目/配置隔离恢复通过，18 表/845 份加密归档保持一致；五服务九进程、三 Worker ping、登录隔离、前端资源和真实历史预览进度合同通过。配置、依赖、数据库 head 不变；没有代用户生成预览或创建广告，线上新批次端到端耗时尚待实际使用观察。

## 2026-09-17：预览小程序前置校验与素材检查提速（已发布新加坡测试服）

- 用户要求修复未选择小程序仍可生成预览，并排查优化实际耗时。前后端拦截缺少/冲突的推广目标，保留原预览恢复；生成页面展示服务端已完成组合数。
- 跨 BC 素材的主账户、权限与配置检查改为有界批次内一次；25 项查询从 1,088 次降到 80 次，本地环节约 317ms 降到 23–38ms，不作为线上整批耗时承诺。
- 后端主回归 68 项及补充 31 项通过，浏览器准备页 65 项、预览页 28 项及新增进度/小程序专项通过，Ruff/Biome/语法/前端构建通过。详见[验证记录](validation/2026-09-17-preview-prerequisites-performance.md)。
- 用户随后明确要求推送并部署：已推送并发布 `badccc2fa6c750f3922524f62aaca72a7dfb3487`。完整备份 `20260917T033621Z` 及隔离恢复、18 表/561 份归档核对、五服务九进程、三 Worker、登录隔离与浏览器页面验收通过；配置/依赖/数据库 head 不变。线上同组 30 素材检查由 1,178 次查询/1.0–1.3 秒降到 71 次/0.07–0.20 秒，结果一致，不代表整批耗时。用户原草稿现已选择 LemonShow，本轮没有替用户生成或提交广告。

## 2026-09-16：授权设置归入授权管理 Tab

- 根据用户反馈，将当前 BC 的使用授权卡片、设置和更换入口移到“授权管理”Tab；广告账户页只展示账户目录，搭建入口和错误提示同步指向授权管理。
- 授权记录默认名称为空，改为统一展示已有名称及通道；缺少名称时显示“官方 MCP/API 授权 · 编号后八位”，卡片、选项和列表复用同一规则。授权列表状态更新时同步刷新当前 BC 卡片，不改变后端默认路由或历史任务。
- 账户授权页面 104 项通过；搭建通道、准备和素材页面本轮另 94 项通过。8 个改动文件 Biome、TypeScript/Vite 构建通过。修正旧素材库无 BC 测试假设及新增同页查询后的轮询/作用域断言。
- 已提交并推送 `6e7045a`，沿用用户授权发布新加坡测试服；完整备份 `20260916T102006Z` 与恢复、5 服务/9 进程、真实两个 BC 页面验收通过，详见[发布记录](validation/2026-09-16-staging-authorization-tab.md)。

## 2026-09-16：统一素材库已推送并发布新加坡测试服

- 按用户授权，将账户授权界面、统一租户素材库与跨 BC 恢复修复一并推送 origin/main，实际部署 `6a84fb9c075c1269f5ee2ce8080987eec9d609b8`，旧版为 `9744ccb`。没有遗漏的本地功能改动；实际广告搭建留给用户手动测试。
- 来源 BC `7678608005688066065` 的 681 个可用副本对应 546 份素材；目标 BC `7683817908149272592` 的 30 个账户均通过新代码准备检查。发布前后各抽查 3 个真实源视频及 B 候选主账户读取成功，无平台上传、共享或广告创建。
- Linux 首轮 28 通过/1 失败，旧硬终止测试被 1 秒软期限提前中断；仅修正测试期限、实际准入键和诊断。最终固定版本 Linux 29 项通过，原先在 Mac 跳过的 4 项 Linux 检查已补验；本地全量 Ruff/ty、前端构建通过。
- 完整备份 `20260916T095817Z` 及独立 PG/Redis/项目/配置恢复通过；1,194 项旧文件、18 表历史、501 份加密响应核对通过。迁移至 `material_seed_generations`，旧 681 副本保留，初始 seed 为 0；未删除或改写历史失败。
- 5 服务/9 进程同版同配置、3 Worker ping、双管理员登录及隔离、IP/sslip.io 入口和真实统一素材库接口通过（553 条目录项，A/B 切换一致）。导入/清理 true、容量 0、域名白名单和额度保持原值，备份/证书 timer 恢复正常。完整边界见[发布记录](validation/2026-09-16-staging-tenant-library.md)。

## 2026-09-16：租户素材库与搭建恢复复审（本地，未发布）

- 统一租户素材库及单 BC 首次转存的既有实现为 `a723f69`，设计/计划为 `76ca073`。本轮复审修复明确失败 seed 永久阻断后续准备、目录不可用代表隐藏真实库存、发送前撤权留下 pending、延迟别名错误 ready，以及封面恢复使用默认连接/过期后无核实唤醒。
- 准备代数保留原主账户、旧消费者和 UNKNOWN；恢复复用已有执行器及 outbox，自动/手动选材共享判断。新增迁移 head `material_seed_generations`，不删除历史、不增加开关，已有第二代时拒绝破坏性降级。
- 最终类型收口后跨 BC/API/MCP/并发/封面组合 35 项、批量共享/迁移/小程序/恢复组合 71 项、目录/匹配 37 项、封面/场景/接口/迁移组合 43 项、准备代数迁移 8 项通过；1,000 记录/500 内容目录完整无漏项，5 页本机查询 0.912 秒。分组有重叠，不累计为全仓总数。
- 前端七组 233 个场景均已有通过结果，TypeScript/Vite、Biome、24 文件 Ruff/格式、11 个生产模块 mypy、全量 CI Ruff/ty 及 Alembic check 通过。扩展搭建/队列分段执行并修正旧测试后，729 项有通过结果、4 项 Linux 专属跳过；详细结果与 Linux 专属待验项见[本轮验证](validation/2026-09-16-material-liveness-hardening.md)。
- 独立复审提出的 SQL 别名引用、封面路由/恢复问题均已关闭；最后恢复增量未发现新的确定性 P1/P2。旧测试按已确认的权限持续有效、历史 schema 和租户接口契约纠正，未引入生产兼容分支。
- 本轮聚焦提交主题 `materials: recover failed preparation and unblock build workflows`；不推送、不发布、不进行真实广告写入。发布仍需完整备份/恢复、同版迁移与 Linux 硬超时/队列及真实平台验收，不能把本地通过视为已上线。

## 2026-09-16：素材集中上传与封面共享（已发布新加坡测试服）

- 已提交 `924150a`：新素材固定在租户/BC 的主素材账户，取消不同文件之间的单账户在途一项门槛；实际历史来源、同文件幂等和真实权限不变。2,000 文件集中测试及来源专项已通过独立审查。
- 新增主账户迁移历史保留验证，修正历史 schema 测试夹具只播种旧 BC 字段；8 项迁移回归通过。夹具处理仅用于迁移测试，不增加运行时代码兼容分支。
- `57b745c`、`e9d0cc1`：源封面提前准备、真实 IMAGE MID、20×10 图片共享、持久分页/单唤醒与原子恢复；历史源候选先过滤真实资格再限量。移除目标重传路径，UNKNOWN 只读，明确拒绝逐目标/MID 保留。
- 根整合接通视频成功与源封面 outbox，同事务确认已处理事件；VIDEO 部分失败回执持久化不可变。URL relay 响应丢失后严格核实使用独立所有权证据，不伪造上传回执。独立审查 Important 均已修复并复验。
- 根整合 272 passed/549.47s；封面双通道集中 221 passed/1 条条件跳过，后续专项 64、28、32 及候选 15 项分别通过，迁移 8 项、搭建衔接 38 项通过。详细边界与命令见[本轮验证](validation/2026-09-16-material-throughput.md)。
- 根整合 `fad9464`；首份独立发布快照 64 项通过但未部署。真实只读确认 INFO/Search 同 MID+签名+尺寸可返回不同 tos ID；`85e3a5c` 补 SOURCE 历史候选的账户库存正证据，以及直接/自动提升后的只读续跑。封面组合 166 passed、最后自动续排专项 16 passed，独立复审通过；没有重传或新旧兼容路径。
- 已独立发布 `fe759b54ff94356131329d5c0d684fd13c48d82d`，最终快照 84 passed/1 deselected；未带入并行分页/前端改动。完整备份 `20260915T182117Z`、PG/Redis/项目/配置隔离恢复、501 份归档解密摘要及 18 表历史摘要比较均通过，head 为 `material_share_receipts`。
- 9 个进程同版本/同配置、3 个 Worker ping/新任务注册、双管理员登录与隔离、42 账户可用及真实 MCP 图片 MID/库存/实际 ID 回读通过。权限持续可用和 R2 不限量不变，资源 Worker 仍 2 个；原批次 477 available/7 failed、138 份 READY 封面不变。生产及原失败视频未动；本轮没有真实上传/共享/广告写入，下一批真实吞吐仍待观察。

## 2026-09-15：账户授权持续可用与 R2 原件不限量

- 用户要求取消 24 小时权限失效并恢复现有账户可用，将 R2 原件总量设为不限制；视频格式问题不在本轮范围。
- 账户目录/连接展示、单次及批量素材操作、搭建恢复和历史回读均取消已确认授权的年龄门槛，已完成权限结果持续复用。保留真实撤权、身份/BC 绑定变化和未完成目录同步的执行期限。
- 全局和租户原件窗口以 `0` 表示不限制，模板、Compose、配置校验和 R2 检查脚本一致；原件统计、校验、清理及请求并发保持原规则。
- 基础红绿回归、目录补全专项和 10 个模块 mypy 通过；扩展测试的旧故障已在原提交副本复现。验收、发布及历史批次恢复见[本轮记录](validation/2026-09-15-permissions-storage.md)。
- 已隔离未部署分页改动，发布 `ca69cc3c7239f9440d4091f81a5b68c8337aa810`；发布快照专项 40 项通过。完整备份及恢复批次 `20260915T151342Z`，9 进程同版本/同配置、双管理员登录与三 Worker ping 通过；42 账户均可用，租户/全局原件预算均为 0（不限量）。旧上传队列自动恢复，已观察到真实账户入库成功；366 个原件需浏览器继续上传，3 个无效视频不处理，未推送。

## 2026-09-15：任务详情视觉收拢与连接误报修正

- 用户复验反馈布局松散及已完成任务出现连接告警。确认前端误将 `recovery_mode=BLOCKED` 当作授权失效；实际无核查候选的正常完成态也返回该值。删除推断式告警，具体权限阻断仍由恢复原因显示，未知请求及回执不受影响，连接入口移至技术详情。
- 复用现有 shadcn 卡片，将版权方、策略、预算和三级创建计数收拢为任务概览，压缩空白并统一页头按钮；1920/1440/390px 长名称和计数对齐纳入回归。仅展示层修改，无后端、迁移或广告/授权写入。
- 验证：任务页、提交配置及双通道页面 87 项通过；TypeScript/Vite 构建、3 文件 Biome 和差异检查通过。桌面及手机截图人工检查完成，均为合成数据。初轮新增链接断言的参数顺序问题已纠正并重跑通过。
- 聚焦提交 `c604407 builds: restore compact task overview and remove false connection alarm` 已推送并发布新加坡测试环境。完整备份及恢复演练、双管理员登录与权限隔离、三队列 ping、五服务同版本均通过；详见[修正记录](validation/2026-09-15-task-detail-refinement.md)。

## 2026-09-15：搭建链路封面复用、调度去重与安全纠正

- 已完整备份/恢复验证后发布 `0cd835525345bd799737f31e6478a2570425e9c9`，7进程同版本同配置，开关/配额不变，备份批次 `20260914T165752Z`。真实平台旧组停用回读后补建新组 `1876327457420498` 及广告 `1876327473587282`、`1876327490290866`，均MATCH/ENABLE；原6条ID/摘要/远端状态不变。新组到两广告完整回读40.062秒，单次写入回执2.6–3.7秒。原任务历史仍2条UNKNOWN/NEEDS_REVIEW，未篡改为8/8；平台实际对象已补齐。详见[最终验收](validation/2026-09-15-build-throughput.md)。未推送，日万级容量未认证。
- 用户授权处理当前两剧两账户及后续多租户批量效率。真实目标账户读取另一账户封面，并创建一条 DISABLE 诊断广告后按 ID 回读，证明本次同 BC 封面可直接复用；不再省略平台必需的 image_info。原六条成功广告保持原 ID/正文，原两条 UNKNOWN 不盲重发。
- 封面任务单次 gateway 会话、完整回执直接 READY、同租户/BC/素材目标图片正证据复用；缺项/歧义继续只读，candidate 不伪造上传记录。网络未发送与只读瞬断按连续故障有界恢复，上传 UNKNOWN 保留不重传。最后发送检查接纳已验证复用 ID。
- 重复 UNIT 唤醒合并：真实 PG/Redis 合成 1000 次前置完成从 1001 条消息降至 1 条。多发布器改为按需锁租户、有界补页；两个发布器不再预占未服务租户。单发布器本地 SQL 成本增加约 9–23%，不夸大为端到端速度提升。详见[调度验证](validation/2026-09-15-dispatch-capacity.md)。
- 补充隔离合同只允许固定账本的原组 DISABLE，主 MCP manifest/冻结路由不变；当前批次拟先核实规则和原组范围，停用并回读后独立补建两条，保留历史 UNKNOWN。完整备份、部署及真实补建结果尚待执行，不能把方案记为完成，详见[实测记录](validation/2026-09-15-build-throughput.md)。
- 本地分项：封面完整组合 146 项通过，回执防撤 arm 附加增量 13 项通过；调度组合 55 项通过；隔离/MCP/双通道适配组合 158 项通过。根整合首轮 178 通过、1 个本地 HTTP fixture idle 连接竞争失败；已在真实协议边界受控复现，修正仅测试 fixture 的 Connection:close，保留专门 transport keepalive 测试，不扩大生产超时。最终整合及最后只读恢复增量将在提交前补记。
- 最终冻结代码根整合 133 passed /208.86 秒；封面快路径与标准补充观察组合 61 passed /193.84 秒。最后恢复增量 RED 5 failed/3 passed → GREEN 8 passed，安全负向组合 25 passed；标准刷新/管理 31 passed。Ruff/格式 21 文件、mypy 13 个生产文件、diff-check 全部通过。聚焦提交主题 `builds: reuse verified covers and coalesce tenant dispatch`，不推送。

## 2026-09-14：原任务安全恢复与无封面实测准备

- 最终已发布 `68e18c6`，第二轮完整备份/隔离恢复为 `20260914T114539Z`；7进程同版本、同私有配置，开关true/true与额度、必需封面代码检查通过。认证入口验收通过，原6条广告保持不变；2条未知尝试仍待后续明确的新补建意图或原平台裁定。最终结果以[实测与发布记录](validation/2026-09-14-build-chain-latency.md)为准，未推送。

- 实测已推翻省略封面的假设：独立 DISABLE 诊断请求返回 40002，明确要求视频提供 1 个 image_info。正在恢复当前自动搭建必需封面路径，通用 optional 合同不变；完整证据与原 2 条 UNKNOWN 的不可盲重试边界见[实测纠正](validation/2026-09-14-build-chain-latency.md)。
- 首轮 `ebd7e90` 已完整备份并发布，原 CTA/系列/组恢复至 4/4；原 6 条广告不变。剩余 2 条无封面尝试返回业务错误，正常只读核查未找到，仍 NEEDS_REVIEW，不能宣称 8/8 完成。
- 最终纠正恢复当前单视频构建的封面准备与恰好一张图片的最后发送检查；既有合格图片复用，原已完成视频不重传。MCP 非零业务码作为安全整数进入 CallEvidence 与 RESULT_UNKNOWN，保留 UNKNOWN/禁止盲重发语义，不记录原错误文案。
- 最终根集成 181 passed / 2 failed：仅双通道旧无封面夹具与真实平台必需封面相矛盾；恢复视频读取→图片上传→图片回查完整流程后，双通道/编译器/SDK 合同组合 64 passed。状态15项、安全恢复19项、MCP解析/传输162项通过；5个实现文件mypy、Ruff通过。扩展封面组合中途113 passed/2 failed，不计全量完成；2项displayable=false旧断言与已有封面规格相矛盾，单独纠正重测。
- 图片非法回读夹具恢复正确规则：displayable=false 已由既有实现支持，未知布尔 None 才拒绝；ID/摘要/尺寸核验不变，8项非法与1项合法false回归通过。额外通道执行组合出现1次本地协议 initialized 阶段 network，未进入目标 tools/list 变更 claim 的边界，记录为待复测现象，不能由此放宽安全断言。
- 根代理最终复测：上述 claim 发送前变更、API/MCP 带封面完整创建回读及8种图片非法回读共11项通过（116.65秒）；未增加超时或放宽权限/claim检查。先前本地协议瞬时失败未查明根因，不声称全部后端大矩阵全绿。

- 用户明确授权实测不传封面并处理整条链路。范围固定原提交、原 BC、原两个账户；只恢复失败 CTA 和从未发送的依赖，保留既有 6 条广告。真实平台结果待发布后记录，不以 optional 文档和替身测试代替。
- 原恢复入口接纳有完整逐 nonce NOT_SENT 证明的冻结失败请求；保留正文、摘要、attempt、原路由，显式恢复才重置三次瞬时故障上限。未知/迟到创建证据阻断重发；依赖恢复后自动续建原分支。
- 原生共享源读取与共享复用一次短期限会话，每操作独立准入、权限与 claim 复核；新增独立 control Worker 模板，业务并发仍为 2。未实施跨素材批量发送，避免缺少批次持久证据的重复共享。
- Alembic 保留脱敏 transport logger 原启用状态，其余 SDK 原始日志继续禁用。两个不同顺序组合 126 项、89 项通过；素材专项 100 项通过；根代理最终集成 164 项通过，4 个实现文件 mypy 及业务/测试 Ruff 通过。末次冻结广告恢复补充及四种创建步骤耗尽恢复 23 项通过。完整备份、发布及真实验收另记。

## 2026-09-14：搭建链路耗时诊断与本地修复

- 新加坡只读证据：45 个视频在两账户形成 90 个素材步骤；提交至首次创建约 22 分钟，后续三组合实际创建约 80 秒。最终 6/8 条广告创建并回读，其余被一个 NOT_SENT/mcp_call_failed 的 CTA 连带阻断；队列已清空，无持续运行的死锁证据。
- 本地增加 MCP 明确未发送传输故障最多 3 次重试，保留冻结正文及 nonce 证据；增加不含异常正文的阶段/类型/耗时日志。完整唯一的共享目标搜索证据直接发布，缺项或歧义仍核验。
- SINGLE_VIDEO 请求可省略自定义图片，移除新搭建的未指定封面依赖；旧冻结图片正文保留最后检查、未知封面历史保留核查。官方 SDK 标注 optional，但当前 Minis 组合的真实省略条件尚待确认，未以离线回执代替平台验收。
- 周期 control 消息按周期过期，业务 outbox 保留。独立 control 消费资源、源读取批量/会话复用、原终止 CTA 的安全人工恢复与测试服发布尚未实施。
- 验证：专项首次 122 通过/3 失败；补齐真实链接 Mini 选择夹具后 API/MCP 链路 2 通过；日志诊断独立 1 通过。安全组合 87 通过，mypy 10 文件、Ruff/格式通过。大矩阵未完成及间歇现象如实记录，不宣称全部回归通过。详细边界见[链路诊断记录](validation/2026-09-14-build-chain-latency.md)。
- 聚焦提交：`builds: reduce redundant asset work and bound unsent failures`；仅本地，未推送、未发布、未重试或重新创建真实广告。下一轮先确认封面场景、补稳定组合验收，再按完整备份流程部署与恢复原失败分支。

## 2026-09-14：素材批量选择发布新加坡测试环境

- 用户明确要求部署后，将固定提交 `e45decdc632b82f6df00caaf1c701fb15b0d772e` 从本地归档部署到新加坡测试服务器，替换 `b618aa2`；未推送、未变更生产。
- 发布前正常排空 API/Worker/Beat；完整备份 `/var/backups/tt-ada-staging/20260914T093726Z/` 包含数据库、Redis、独立项目/前端构建产物及完整私有配置/证书，均通过摘要及隔离恢复验证，留档正文解密与摘要核验通过。数据库 head 保持 `provider_display_drama_id`。
- API、3 个 Worker 进程、唯一 Beat 同版本同配置；导入与自动清理保持 true，调用策略、密钥、媒体白名单一致，环境文件摘要不变，备份和证书 timer 已恢复。HTTPS、页面/JS 产物精确匹配、管理员及租户管理员登录/profile、Worker ping 和错误日志检查通过。
- 使用线上实际前端及模拟 API 数据复验 3 项批量选择交互（1440/390px、跨页搜索、去重、取消/清空、统一保存），全部通过；未修改业务草稿或创建广告。详见[发布记录](validation/2026-09-14-material-batch-release.md)。

## 2026-09-14：调整素材支持批量添加

- “添加素材”改为多选弹窗，支持逐条勾选、全选/取消本页、跨页和搜索保留选择、清空已选及统一确认；已有素材标记“已添加”并禁选，取消或关闭不修改外层分组。
- 素材列表独立滚动，搜索与底部已选数量、分页、确认按钮保持可见；长文件名换行，入口使用明确的“添加素材”按钮。确认后去重追加至最后一组，保留原有顺序和分组，仍由“保存素材分组”统一持久化。
- 租户/BC 目录接口与现有分组保存、权限及未知响应恢复机制沿用；只改前端，无迁移、外部服务或广告写入。
- 验证：准备页 58 项 Playwright 回归通过（含新增 1440/390px 跨页搜索、去重、一次保存，以及取消/清空和长列表固定底部），390px 长文件名截图人工检查通过；TypeScript/Vite 构建、3 文件 Biome 和 git diff --check 通过。均为本地合成数据验证。
- 聚焦提交主题：`builds: support batch material selection`；未推送、未部署。

## 2026-09-14：数字剧目 ID 与链接编辑发布测试环境

- `f17cbe7` 已推送现有 `feat/platform-implementation` 并部署新加坡测试服务器，替换 `9381270`；生产环境未变更。
- 后端专项 64 项、准备页 54 项、生产构建、Biome、Ruff/格式和 mypy 重新通过。完整备份 `/var/backups/tt-ada-staging/20260914T080717Z/` 完成 PostgreSQL、Redis、项目/前端产物和完整私有配置的摘要校验与隔离恢复。
- 新迁移先在恢复库演练，再将业务库从 `manual_promotion_links` 升级至 `provider_display_drama_id`；4 条既有剧目中 3 条按唯一可靠归因补齐展示编号。API、3 个 Worker 进程、Beat 同版本同配置，HTTPS、管理员登录、受保护 profile、Worker ping、定时器与错误日志验收通过。未调用外部集成或创建广告；详见[发布记录](validation/2026-09-14-drama-id-and-link-edit.md)。

## 2026-09-14：统一版权方选择弹窗发布测试环境

- `8c1588f` 与最终版本 `9381270` 已推送现有 `feat/platform-implementation` 并部署新加坡测试服务器，替换 `347ea52`；生产环境未变更。
- TypeScript/Vite 构建、3 文件 Biome 和准备页/版权方页 75 项浏览器回归重新通过。完整备份 `/var/backups/tt-ada-staging/20260914T040552Z/` 的 PostgreSQL、Redis、项目/前端产物和完整私有配置均完成摘要校验与隔离恢复；数据库 head、功能开关及调用策略未变。
- API、3 个 Worker 进程、Beat 同版本同配置；HTTPS、管理员登录、受保护 profile、Worker ping、备份/证书 timer 与错误日志验收通过。未调用外部集成或创建广告；详见[发布记录](validation/2026-09-14-build-interaction-refinement.md)。

## 2026-09-14：广告搭建交互修正发布测试环境

- `347ea52` 已推送现有 `feat/platform-implementation` 并部署新加坡测试服务器，原版本 `7b2ee95`。没有 `main` 分支，未新建分支或强推；生产环境未变更。
- 完整备份 `/var/backups/tt-ada-staging/20260914T021951Z/` 的 PostgreSQL、Redis、项目/前端产物和完整私有配置归档均完成摘要校验与隔离恢复；数据库 head 保持 `manual_promotion_links`，导入、自动清理及调用策略保持原值。
- API、3 个 Worker 进程、Beat 同版本同配置；HTTPS、管理员登录、受保护 profile、Worker ping、备份/证书 timer 与错误日志验收通过。未调用外部集成或创建广告；详见[发布记录](validation/2026-09-14-build-interaction-refinement.md)。

## 2026-09-13：合并剧目输入与素材列表

- 取消独立“剧目输入”页签，在“剧目与素材”同一列表保留剧名、版权方剧目 ID、取链及素材状态；原始输入在侧栏详情查看，内部 UUID 不作为业务剧目 ID 展示。
- 保留用户指定的“选择剧目”名称，仅当前版权方任务处于 needs_resolution 且有候选时提供选择；成功后收起入口，历史候选不再导致误显示。未知响应沿用防重记录，留在当前页刷新结果；只读角色只能查看详情。
- 后台按当前任务返回候选和版权方 ID，已完成且无当前准备任务时从已绑定版权方剧目读取 ID；租户、版权方连接与应用范围保持一致。未增加迁移或真实版权方/广告操作。
- 验证：当前工作区准备页与预览页 67 项浏览器测试通过；1440/900/390px 三步截图及只读角色复验通过；3 项 PostgreSQL 进度与只读目录测试、前端构建、Ruff、Biome、后端类型检查通过。
- 本轮聚焦提交：`builds: unify drama review and show provider drama IDs`；未推送或发布。

## 2026-09-13：广告搭建准备进度与列表稳定性

- 搭建页按原输入顺序展示剧目，取链前展示原文或 ID，解析后原位更新剧名、链接阶段及素材结果；重复、空行、失败和结果待核实均保留说明。
- 移除页面顶部的取链跳转入口，链接详情与素材调整复用既有侧栏；准备期间按钮显示“准备中…”，正常请求不闪现“结果待核实”警告。
- 仅关闭搭建页列表刷新提示；同一草稿版本的状态切换保留行和分页，输入版本变化重置分页，避免旧游标和旧结果残留。
- 输入读取接口按租户、草稿当前版本及输入行批量汇总版权方阶段和已有素材结果；只读，无迁移、无外部调用或任务调度。
- 验证：准备页及预览页 56 项浏览器回归通过；新增进度及现有只读目录 3 项 PostgreSQL 测试通过；前端构建、后端 Ruff 与类型检查通过。准备中的截图已检查（合成数据），未调用真实广告服务，未发布。
- 聚焦提交主题：`builds: show preparation progress without empty or jumping lists`；保留其他任务的未提交修改。

## 2026-09-13：统一广告名称格式

- 按用户确认方案，删除独立 Campaign 后缀配置；用一套中文格式编辑版权方＋剧名、剧目 ID 和可选日期。批次编号由系统统一追加，网眼仅替换开头的归因基础名，剧目 ID 独立保留，两版权方示例同步变化。
- 历史策略在存储读取边界转换到统一格式，原 JSON、请求摘要、冻结预览及已提交名称保持不变；旧 BUILDING 预览明确提示重建。无数据库迁移或真实广告操作，未推送/发布。详细边界见[验证记录](validation/2026-09-13-unified-ad-naming.md)。
- 验证：102 项后端测试通过；策略与搭建预览浏览器回归共 64 项通过，最终策略全量 38 项及最后格式解析调整的 2 项复验通过；1440/390px 截图检查、TypeScript/Vite 构建、Ruff、Biome、Python 语法及差异检查通过。
- 本轮聚焦提交主题：`strategies: unify ad naming across providers`。保留工作区其他任务的未提交修改。

## 2026-09-12：MCP 共享授权与多个 BC 独立接入

- 按用户批准的[多 BC 设计](superpowers/specs/2026-09-12-mcp-multiple-bcs-design.md)完成共享授权、完整目录多选、已有授权追加、每 BC 同步/重试/解绑及独立默认通道。后端由代码连接官方 MCP，未引入运行时 Agent。
- 新增绑定代数，解绑重接不会复活旧冻结任务；已有 API/MCP 连接和六字段历史路由保留。修复共享目录时间、旧 ACTIVE 工具观察、旧场景哈希和管理员撤权恢复的升级边界。
- 功能已提交并推送 `f9d74257d7e74d91dfcf2f55f2006fba3b6af5b0`，按手册完整备份和恢复验证后部署测试环境，迁移 head 为 `mcp_multibc_runtime`；983 个源码文件与固定提交一致，API/Worker/Beat 版本和调用策略一致。
- Sun Browser 已验证现有授权读取及刷新 BC 目录；正式同步接口验收为 COMPLETE，42 个账户全部有效，原授权、绑定代数和默认通道保留。窗口后续不可读取，因此最后同步采用 HTTPS 与数据库回执核验。现有真实授权仅一个 BC，多 BC 并发由隔离测试覆盖；素材/广告写入未实测。详见[验证记录](validation/2026-09-12-mcp-multiple-bcs.md)。

## 2026-09-12：调用额度纳入部署强制检查

- AGENTS.md 及通用、首次部署、测试、生产手册统一要求：启用 API/MCP 前配置有效 TIKTOK_CALL_POLICIES，校验租约与配额域、实际服务加载，并执行授权后的 BC 读取验收。不能仅凭健康、READY 或授权页面成功交付。
- 补齐完整单行环境配置、8 个 base 字段解释、MCP 未知主体共享域规则、systemd 实际环境检查命令及 Compose 环境更新方式。区分当前测试策略与官方/生产额度；现为强制人工步骤，未宣称启动脚本已经自动执行。
- 本轮只修订文档，保留真实 BC 读取待用户授权复验的状态；配置样例和两段实际检查代码通过，Shell 语法、67 个 Markdown 本地链接及 git diff --check 通过。

## 2026-09-12：MCP 账户文本回执与候选版本修复

- 修复上游错误映射缺少 502 导致的二次异常；依据官方自定义客户端指南，为 7 个账户 READ 合同接受单条 JSON 文本回执，保持 code/形状/歧义检查，WRITE 门禁未放开。
- 更新合同 SHA，并按当前 SHA 查询候选工具观测；旧观测不能被绑定复用，读取可重新核实工具后继续。新增真实 PostgreSQL/Redis 回归先红后绿，gpt-6-astra high 独立复核 PASS。
- 完整相关七文件 **253 passed / 0 failed（89.35 秒）**，Ruff、ty 和 diff 检查通过。独立本地测试库，不使用业务数据库；外部 HTTP 使用传输层替身。
- 修复已以 `87a3b33e822728461dd773ea0b5b841043d5c312` 发布到测试服务器，970 个跟踪文件核对一致，服务/权限/回调验收通过；[排查、备份及发布记录](validation/2026-09-12-staging-mcp-auth-fix.md) 保留每一步实际结果。上一候选选择窗口已过期，最终线上 BC 读取需用户重新授权，未伪造授权/延长期限。

## 2026-09-12：MCP 授权后额度与工具定义排查

- 第二次租户授权已兑换并加密保存候选令牌；候选 BC 读取被空 TIKTOK_CALL_POLICIES 拦截。测试环境补齐本地限流：每逻辑操作 3 次/秒、全局 10 次/秒，并发分别 app 4 / endpoint 2 / tenant 4 / advertiser 4，lease 960000ms；全局数字为本地调度策略，不是官方 App 额度。未核实主体时保持所有 MCP 连接共用 shared-unverified 配额域。
- 配置变更前完整备份 `/var/backups/tt-ada-staging/20260912T070256Z/`，含 PostgreSQL、Redis、项目源码/已构建前端及私有配置。PG 新库恢复、Redis 独立实例恢复、项目隔离解压和 SHA-256 均通过，RELEASE_COMPLETE 已写入；API/Worker/Beat 恢复。
- 实际 tools/list 已可请求；7 个账户工具中发现空 properties 省略及 number 的 double 注解差异。仅规范化这两类表示，保留字段、类型、边界、字符串/未知格式及业务字面量校验。新增 6 个回归先失败，再修复后完整协议测试 50 passed；Ruff、ty、diff 检查通过，真实 7 个脱敏工具定义离线比较通过。
- 当前修复待固定版本发布及候选 BC 读取复验；未替用户绑定 BC、设默认连接、上传或创建广告。

## 2026-09-12：发布规范与双通道状态核对

- 补充 AGENTS.md、通用及测试环境部署手册：每次实际发版均备份数据库、Redis、独立项目源码/构建产物归档和私有配置，核对配置并验证恢复；无迁移也适用。明确现有测试服务器备份脚本尚无项目归档，后续发版需人工补齐，未改写历史备份事实。
- 核对 accounts/routing.py：明确连接优先，否则 BC 唯一默认连接；未设置则阻断。授权不自动改默认，冻结任务不随默认切换或跨通道重试。
- MCP 实际证据仍为官方登记 HTTP 201 与本地 configuration READY；尚未完成租户授权和真实账户/上传/广告请求。文档修订不改变运行代码或服务器配置。
- 验证：git diff --check、变更 Markdown 本地链接检查；未重复运行业务回归。本次提交主题为 docs: require complete backups for every deployment。

## 2026-09-12 测试服务器发布准备与 Linux 验证

用户明确授权提交、推送全部本地代码并部署新加坡测试服务器。历史建设方案已纳入 `46db70e`，此前 33 个本地提交已推送到现有 `feat/platform-implementation`；本地及远端均无 `main`，未创建分支或强推。

- 767 文件与此前全量矩阵快照核对一致后，只修改三个测试文件；业务源码未变。服务器冻结安装成功，使用 Bun 执行 `tsc -p tsconfig.build.json` 和 Vite 构建通过，Ruff 和显式指定虚拟环境的 ty 检查通过。
- Linux 专用验收共 9 个不同用例通过：源素材双通道/迟到及硬终止 4 passed（83.22 秒）；广告原连接硬终止恢复 2 passed（81.06 秒）；同轮已通过广告硬期限及原件校验 3 项。真实 PostgreSQL 18/Redis 8、隔离测试 DB14/15；不调用真实 TikTok/R2。
- 修复测试 Redis 库错配、两个嵌入 worker 共用 Kombu Hub/停止标志、假 HTTP 服务一秒空闲连接复用竞争、模拟回读金额表示，以及恢复测试过早软中断/硬终止。源素材真实硬限仍为 3 秒；广告恢复为 10 秒，仍断言实际到达后进程硬终止、只创建一次、原连接读取和撤权阻断。生产任务期限不变。
- TikTok 官方动态客户端登记实际返回 HTTP 201；测试站点独立注册材料已保存于服务器受控文件并通过应用用户读取校验，回调为 `https://tk-ada.137-220-150-31.sslip.io/api/integrations/tiktok/mcp/callback`。注册与租户登录授权分开；未使用 Codex token，未完成租户 TikTok 授权或广告/素材真实联调。
- 既有数据的独立恢复副本已成功演练迁移至 `mcp_cover_evidence`。服务器增加 2 GiB swap；备份脚本覆盖 MCP 注册配置。实际库迁移、服务切换、备份恢复、双管理员登录与 MCP READY 均已通过，运行提交 `0535f050dab73632a2ea4f4bb801dca0871de238`；见 [发布验收](validation/2026-09-12-staging-mcp-release.md)。

# 实施进度

## 2026-09-17 本地素材匹配批处理

- 草稿本地选材每轮最多五页，累计一秒后不再开始下一页；有剩余素材及刚完成素材匹配时立即续调度，移除逐剧五秒等待。账户、取链和场景依赖等待保留退避，父/子锁顺序、代数去重、事务回滚及恢复身份保持不变。
- 真实新加坡隔离 PostgreSQL 回归 30 项通过（29.36 秒），涵盖批次上限、时间预算、重复投递、205 条分页、错误回滚、外部等待与草稿范围。22 部剧/550 条合成素材的实际匹配五轮耗时 5.255 秒，不含 Broker/Beat 调度等待，未执行平台广告或素材写入。
- Ruff、语法与差异检查通过；无迁移、依赖、前端及配置变更。功能提交 `1d7ab27` 已推送并部署新加坡，完整备份恢复及五服务九进程运行验收通过，详见[发布记录](validation/2026-09-17-material-matching-batch.md)。

## 2026-09-17 网眼剧名首尾空白修复

- 网眼返回的部分标题末尾带制表符，导致完整剧名精确比较失败。在 WangyanClient 搜索响应边界清理首尾空白，保留内部空白、大小写、标点以及版权方 ID。
- 新增制表符、换行、Unicode 首尾空白回归，并在真实 PostgreSQL 的网眼工作流验证匹配后复用历史链接及稳定命名。
- Linux 隔离库协议及相关工作流 56 项通过（5.11 秒）；Ruff、语法及差异检查通过。本地测试数据库端口未启动，改用服务器独立测试库。扩大工作流运行时 62 项通过，但一项清理临时数据库时因终止连接权限不足报错；本轮相关用例单独重跑通过，未将其计为全套通过。
- 用户授权推送 main 并部署新加坡测试环境，功能提交 `9a9fea4` 已上线；无迁移、无配置或开关变更。完整备份及隔离恢复通过，五服务同版本同配置，三部剧真实只读分页搜索均唯一命中。旧草稿历史失败结果未改写，详见[发布验收](validation/2026-09-17-wangyan-title-whitespace.md)。

## 2026-09-12：MCP P3.6 双通道集成与离线验收

- 官方 API 与无 API App 配置的 MCP 两条合成链路，经过实际场景任务、原件验证与入库、来源上传、跨账户目标分发/封面、具体预览提交、独立 CTA、三级直接 ENABLE 创建及实际父子/金额/素材回读。业务使用后端代码中的官方客户端；租户授权、BC 绑定和冻结连接规则贯穿各步骤。
- 完整首跑为 2149 项通过、36 项失败、8 项初始化错误、9 项跳过，保留原日志。修复发送前期限错误丢失 NOT_SENT 类型、既有已验证封面的复用顺序，并同步旧测试的授权事实、历史列及逻辑操作限额配置；各修复经独立审查与定向验证，没有放宽业务门禁。
- 最终完整六目录后端矩阵 **2203 项通过、0 失败、9 项跳过，1304.32 秒（21 分 44 秒）**。其中 8 项真实 Linux prefork 验收在 macOS 未运行；另 1 项为 API 分支不适用的 MCP 文本 envelope 用例。前端 workspace **348 项通过（3.9 分钟）**，TypeScript/Vite 构建通过；完整 app ty、Ruff app/tests 通过。阶段测试不与完整矩阵累加计数。
- P2.4 已提交 `2429a22`，单一迁移 head 为 `mcp_cover_evidence`。P3.6 已提交 `10da9fd`，覆盖整合类型检查、回归、历史迁移与发布/恢复记录；完整代码提交和后置核对登记于 [离线验收](validation/2026-09-11-tiktok-dual-channel-offline.md)。代码与测试完成本地整合，未将 23 项计划等同于全部环境验收。
- 正式 MCP 客户端注册、真实租户授权及主体/权限/工具响应、视频服务策略仍待 [真实联调](acceptance/live-mcp.md) 核实；MCP 视频门禁保持关闭。Linux 恢复验收、真实 TikTok/R2/版权方写入及部署未执行；没有复制 Codex 凭据、推送或发布。子 Agent 与完整测试均自然结束，未因等待超时中断。

## 2026-09-12：MCP P2.4 目标封面与持久回执

- 图片上传、详情和建议封面接入冻结目标连接的 API/MCP 网关，保留官方 API 的封面权限叶规则。每次实际请求检查当前读取/上传权限、目标 VID、原视频 MD5 和领取状态；仅采用本目标视频详情或同比例建议，不自动裁图或绑定其他素材。
- 新封面任务保存不可变原视频 MD5；实际图片 ID/签名在客户端清理前持久化，主事务失败时由独立回执保存。迟到回执与已核实图片冲突时撤下该目标的冲突图片映射、保留 ID 并持续阻断；普通核查不能清除已证实的冲突。
- 状态查询使用原连接当前只读权限，查看者不需要搭建权限；默认切换不代替原授权。历史原摘要或回执观察缺失时保持阻断，不补写今日事实。新增迁移 `mcp_cover_evidence` 经独立审查，专属本地测试库备份、升级和 Alembic check 通过；已有新证据时拒绝降级丢失。
- 完整矩阵发现新摘要校验意外拦截无历史封面任务的既有已验证图片；已恢复其当前连接读取权限与有效期校验后复用。已有任务仍核对原摘要，新封面仍须保存摘要；新增双通道正反例没有给旧图片补造原件证据。相关旧迁移测试按真实旧列播种，避免当前模型字段提前进入历史库。
- 规格/质量最终复审通过；原 11 文件聚合 357 项通过、1 项既有 SDK 不适用例跳过，最后封面增量与广告执行九文件 187 项通过（209.91 秒）。整合后的全 app ty、Ruff app/tests 与相关 mypy 通过；完整后端首跑失败及各组修复保留记录，最终完整矩阵待复跑，不将阶段数量相加当作全量结果。
- P2.5 已提交 `b3f9ada`。本轮以 `materials: verify channel covers and preserve receipt evidence` 本地提交；未访问真实 TikTok 或生产环境，MCP 视频入库真实服务策略仍未核实。

## 2026-09-12：MCP P2.5 未知素材恢复与原件保护

- 修复分多次任务搜索素材时只检查总页数的缺口：现在持久核对总行数、连续页、已见视频 ID 摘要及完整计数。目录变化、重复、历史游标缺证据或超限均保持未知，只继续核查；完整唯一候选仍须按实际 VID 核实摘要和大小。
- 接受上传后失联、签名 URL/本地租约到期、迟到回执、其他操作使用同一原件及完成事务回滚时，原件用途和容量预留均保留。现有清理保护已通过回归，无需额外改写清理逻辑；每轮最多 100 页/10,000 条，持续变化按既有 60 秒间隔只读核查，不自动重传或超时释放。
- 独立规格/质量审查通过；作者八文件 103 项通过，根级两个文件 67 项通过（48.82 秒），相关 mypy、ty、Ruff 通过。新增四项 Linux 双 worker/prefork 故障用例在本机未执行，不能将跳过算作通过。
- 本轮以 `materials: preserve originals across unknown channel uploads` 本地提交；无新增迁移、真实上传或生产变更。封面整合与全矩阵继续执行；真实 MCP 视频上传策略仍待独立服务证据。

## 2026-09-12：MCP P3.3–5 广告执行、只读恢复与连接页面

- 广告创建迁至官方 API/MCP 两个适配器，按具体预览直接 ENABLE，CTA 独立执行。原请求和尝试编号在发送前保存，实际 ID 先提交再关闭客户端；明确未发送可保持原正文和编号重新排队，结果未知不重建或换通道。每次实际请求前检查原路由、当前权限、任务领取和场景/素材证据。
- 普通回读核对实际账户、父级、完整字段和全部分页；新授权只读核查必须管理员显式发起并证明同连接、同主体和同资源，结果独立保存，找到对象也不会继续原广告创建。新增不可变历史核查迁移和后台修复调度，支持按原 request_id 找回丢失回执。
- 草稿可明确选择当前 BC 的 API/MCP 连接，或清除选择使用 BC 默认；只有新准备解析偏好。预览和执行详情展示实际冻结连接，权限不足、历史证据缺失和 UNKNOWN 显示相应可操作状态。独立核查明确结束后可再次显式只读核查；受理未知或运行中仍只查询原请求。
- 独立审查发现并修复回读伪 ID 和页面终态无法再核查的问题，中文广告名称不受影响。公开响应仅给业务字段，不暴露 token、授权回调或原始 MCP 响应。草稿连接迁移与历史核查迁移均经独立审查、本地升级与历史数据保护验证；单一 head 为 `mcp_draft_connection`。
- 验证：执行作者 76 项、适配器 69 项，根级执行/状态/公平 35 项通过；恢复作者 185 项通过、3 项 Linux 专用测试未运行，ID 修复后 67 项通过；根级恢复/路由联合 30 项通过，唯一测试分页假设修正后相关 5 项通过。页面原整组 184 项通过，终态补丁后相关 16 项通过；TypeScript/Vite、相关 mypy、Ruff、Biome 通过。暂存版本独立导出后，另有 15 项跨执行/历史 HTTP/迁移/草稿/展示用例通过（16.78 秒），同版前端类型检查和 Vite 构建通过（438 毫秒），确认不依赖后续未提交改动。测试组有重合，不累加成总数。
- 三项分别通过规格/质量审查，因共享类型、执行恢复接口和连续迁移依赖合并为一次本地提交 `builds: execute and reconcile frozen API and MCP requests`。没有推送、部署、真实 OAuth/TikTok/R2 或广告操作；完整素材封面/清理和跨阶段验收继续执行。
- 已知环境限制：macOS 无 Linux prefork 实测，三个 hard-kill 用例不能以跳过视为通过。先前一次 MCP `mcp_call_failed/NOT_SENT` 未复现且尚无已核实根因，后续相关完整回归通过；未新增重试或调整期限掩盖异常。

## 2026-09-12：MCP P2.3 素材上传与实际回读

- 来源 URL/FILE 入库及跨账户目标分发改用统一 API/MCP 网关，按原任务连接逐次检查权限、账户和领取状态。实际 VID/MID 先保存，再关闭客户端；结果未知只核查原操作，默认切换不补传。
- MCP URL 上传采用按契约版本登记的容量、内容身份及自动行为门禁；真实服务证据仍未核实，生产入口保持关闭。原有 API URL/FILE 路径保留应用容量与明确参数，由实际摘要、大小和 VID 回读决定可用。合成测试能力不能当作真实上传能力。
- 原件 UNKNOWN 用途与字节预留持续保留；当前领取者获得明确未发送结果时，只结束这次临时签名用途。批量同目标权限核查已消除重复查询，单条和 30 条素材均为 34 条只读查询，原规模不缩减。
- 独立规格/质量审查通过；作者最终 477 项通过、1 项仅适用 MCP 的参数例跳过，加强实际凭据轮换后 11 项通过；根级实际 PostgreSQL/Redis 与传输边界 57 项通过（21.92 秒）。暂存版本另在新建独立 PostgreSQL 测试库验证同组 57 项通过（22.10 秒），确认不依赖后续未提交代码。相关 Ruff、mypy、差异检查通过。无新增迁移、真实 TikTok/R2 调用或生产操作。
- P2.2 已提交 `e478511`；本轮以 `materials: upload through frozen API and MCP gateways` 本地提交。封面与完整原件清理验收继续执行。

## 2026-09-12：MCP P2.2 素材父任务与实际来源路由

- 新上传批次、R2 素材接收和各次素材操作在排队前保存连接；目标分发和封面继承搭建父路由，来源读取保存实际上传账户与连接。切换默认连接不会改变已接受的任务，幂等重入读取原记录；普通凭据轮换仍可用，授权或契约变化明确阻断。
- 同一素材 VID 的历史封面身份不会因换连接重复创建，实际上传连接不被目标执行连接覆盖。旧记录缺少原授权证据时保持未知，数据库禁止补写或改写其路由；新增迁移保留原请求、摘要和远端 ID，含路由证据时拒绝降级。
- 独立规格/质量审查通过。作者素材组合 100 项、来源/并发/封面等组合 99 项、搭建调用方 39 项通过，最终封面修复 44 项通过；根级最终 20 项实际 PostgreSQL/Redis 回归通过（4.15 秒），迁移独立审查、升级及 Alembic check 通过。相关 mypy、Ruff 通过。各组覆盖有重合，不累加为总数。
- P1.8 已提交 `9df5b2b`；本轮以 `materials: freeze upload and distribution routes` 本地提交。P2.3–4 继续接入素材写入网关，当前 MCP 素材写入口仍明确关闭；没有真实 TikTok/R2 上传或生产操作。

## 2026-09-12：MCP P1.8 租户连接页面与 HTTP

- 管理员可选择官方 API 或 MCP 授权；MCP 独立于开发者 App 配置，授权后明确选择一个 BC 绑定。账户列表按租户、BC、连接查询，无默认时等待显式选择；管理员可为指定 BC 切换默认。连接详情区分读取、上传、搭建权限的已知与未知状态。
- 授权取消或失败保留已有连接。页面持续更新正在刷新的凭据状态，到终态停止；绑定成功清除回调参数；分页权限被撤销时隐藏已缓存的受保护连接名。重新授权后的当前状态不会被旧刷新异常覆盖。
- 后端与页面分别通过独立规格/质量复审。最终真实本地 PostgreSQL/FastAPI 23 项通过（包括原 10,001 条目录分页），workspace 浏览器 84 项通过；TypeScript/Vite 构建、相关 mypy 与 Ruff 通过。浏览器使用 HTTP 边界合成数据，没有真实授权或广告操作。
- P1.6/P1.7/P3.2 已提交 `3bfce19`；本轮以 `accounts-ui: manage tenant MCP authorization and BC connections` 本地提交，后续素材与广告执行继续实施。

## 2026-09-12：MCP 账户目录、场景与搭建父路由

- P1.6：API/MCP 使用完整分阶段目录后原子发布授权与账户，旧记录在失败时保留。过期事实在原连接上完整重新观察，普通令牌轮换不使任务失效；完整授权集合可收紧同连接其他 BC 的失效授权，缺失详情不会清空另一连接已核实的账户元数据。MCP 写权限没有独立证据时仍保持未知。
- P1.7：草稿准备排队前固定连接，场景分页、账户能力与后续预览都明确校验原路由。MCP 握手和每次业务 HTTP 前重新核实当前权限及任务领取，失效后不再发送；刷新结果未知或需重新授权保留明确阻断原因。旧 SDK 场景第二调用路径已移除。
- P3.2：预览、执行步骤与每次尝试保存原连接、授权及契约版本，默认切换只影响新的准备。迟到回执关联原尝试；历史无法证明原授权的任务保留请求、UUID 和远端 ID，阻止按今日默认续建。历史伴随迁移、场景路由和目录观察语义共用连续迁移链，因此三项已分别审查的依赖一次提交，避免中间版本缺少必填父路由。
- 验证：P1.6 最终 139 项联合回归及两项各 5,000 条容量测试通过；P1.7 作者完整 117 项、共享网关 64 项通过，根级追加四项真实 HTTP 边界测试和修复夹具后的 49 项场景任务、五项草稿回归通过；P3.2 作者 175 项与根级 19 项历史/路由验证通过。相关 mypy、Ruff 通过，规格与质量审查均批准。根级将本次暂存版本独立导出并使用全新 PostgreSQL 测试库，再验 48 项跨模块边界通过（51.34 秒），确认没有依赖后续未提交代码。各组覆盖有重合，不相加为总数。
- 并行验证暴露的旧测试全表读取和重复插入绑定已按合成租户范围修正；没有降低测试数据量，也未因等待时间中断子代理。全部验证使用本地 PostgreSQL/Redis 与传输层替身，尚无真实 TikTok、Linux worker或生产验收。本地提交以 `tiktok: preserve authorization routes across directory and build work` 标识；素材写入和广告 worker迁移继续独立实施。

## 2026-09-12：MCP P3.1 广告类型与双通道回读

- 新增完整冻结创建意图及 API/MCP 只读适配器，覆盖 Campaign、Ad Group、Ad、CTA 和广告组状态。原有字段可完整编解码，保留精确 ID、预算、ROAS、素材、链接、文案及直接 ENABLE 意图；新适配器创建入口仍明确关闭。
- 回读只采用远端实际字段，不从请求补齐。独立审查复现 SDK 将原始小数舍入后错误判定匹配；修复后远端浮点缺乏精确依据时返回证据不足，历史本地输入兼容与远端解析分开，精确整数和字符串仍可比较。
- 整体独立审查及精度修复复审通过。初始合同、HTTP、网关及旧 SDK 组合 96 项通过；最终精度相关 45 项真实客户端 HTTP 与 25 项合同测试通过，mypy、Ruff 和格式通过。网关根级组合 37 项通过；全部为本地替身，没有创建真实广告。
- 已知限制：CTA 回读缺少实际账户字段、Smart+ 广告组需另读状态、普通 JSON 小数金额的精确性，以及未知 CTA ID 检索仍需后续能力/恢复阶段处理。此提交不代表完整写入闭环或线上验收。本地提交以 `builds: add typed API and MCP readback contracts` 标识。

## 2026-09-12：MCP P2.1 素材双通道读取

- 统一视频、图片、封面建议和临时预览的类型与 API/MCP 适配器，通过同一任务网关按冻结来源连接读取。返回前再次核对原授权、绑定及素材的持久标识；正常凭据轮换仍可继续，授权收缩或素材变化立即阻止返回预览。
- 初始来源查询、网络读取及最终校验共享绝对期限。预览读取不投递素材准备或写入任务；凭据不足时沿用统一凭据刷新调度。缺少真实权限或工具能力的上传仍关闭。
- 独立规格/质量审查两项问题已修复并复审通过。材料相关组合 220 项通过、1 项不适用跳过；最终修复相关 29 项及既有预览 2 项通过，另复核 10 项精确错误码与正常轮换。真实 PostgreSQL 锁等待、本地 SDK/MCP HTTP 替身及静态检查通过；未访问真实 TikTok 或上传素材。
- 旧素材 SDK 原子操作暂由新适配器共用，后续 P2.3–4 迁移全部写调用后删除旧入口；未宣称素材 worker 已全部迁移。本地提交以 `materials: read assets through frozen API and MCP routes` 标识。

## 2026-09-12：MCP P1.5 固定 BC 路由与统一网关

- 新任务从 BC 的明确默认连接或显式连接冻结路由；默认改变不会改变已有路由，缺默认时要求管理员选择。管理员设置默认的 HTTP 入口校验租户、BC 与绑定，记录审计。
- 统一工厂创建官方 API/MCP 的账户与场景适配器，逐次实际请求检查当前操作者、连接、账户、授权和契约修订，并使用独立短事务及共享准入。仅 API 分支要求开发者 App；业务任务和冻结路由不携带 token。
- 普通 token 轮换可继续使用原路由；已打开会话期间凭据变化会在下一次发送前停止，调用方可用同一路由重新打开。目录/角色刷新可在账户证据过期时进行，普通账户操作仍要求当前完整证据，401 不自动刷新重放。
- 完整规格和质量独立审查通过。网关、路由与权限 72 项通过；根级默认设置 HTTP、路由与既有列表 32 项通过，相关 mypy、Ruff、格式和语法检查通过。全部为独立 PostgreSQL/Redis 与合成 HTTP，本任务尚未迁移后续素材及广告 worker。本地提交以 `accounts: pin BC routes across background work` 标识。

## 2026-09-12：MCP P1.4 自动刷新与回执恢复

- 网关只按任务期限检查凭据，需要刷新时持久排队；刷新 worker 通过连接锁、一次发送标记和加密回执恢复，正常轮换只增加凭据修订，不改变冻结任务的授权版本。完整响应在连接清理前保存，响应未知不重放旧 refresh token。
- 每个发送和发布边界重查排队操作者的当前租户权限；权限变化保留已收到回执，不越权发布。恢复消息与 publisher 的发布事务通过行锁同步，避免 worker 把自己的原始消息误当成后续恢复消息。
- 独立规格/质量审查及两轮定向复审通过。最终 31 项真实 PostgreSQL/Redis 与合成 HTTP 测试通过，Ruff、格式、mypy 和语法检查通过；没有真实 OAuth、TikTok 或生产调用。
- 同一授权的标准刷新沿用已验证的客户端、服务端与 scope 链路；TikTok 丢响应后的轮换/重放保证仍未验证。范围或身份变化的候选保留加密材料并要求管理员重新授权，未宣称已有直接接受刷新候选的界面。本任务以 `accounts: refresh MCP credentials without replaying unknown requests` 本地提交。

## 2026-09-11：MCP P1.3 租户授权与 BC 选择

- 接入不依赖 Marketing API App 的独立 MCP 授权：固定官方端点、PKCE、持久防重放 claim、加密候选和完整 BC 目录。租户管理员选定 BC 后只排队验证，完整目录发布由 P1.6 处理；未提前激活连接。
- MCP 授权及候选全过程持续检查有效租户成员关系和当前管理权限。完整工具目录中，可选上传/广告工具契约漂移只限制对应能力；账户必需契约仍严格校验。旧页面和新入口共用本地停用、授权版本与候选任务清理。
- OAuth 增加清理前回执钩子供刷新使用，原始客户端标识与令牌一同加密保存；数据库及 Redis 检查使用独立短连接与期限。驱动超时不替代 worker 硬期限，也不声称能绝对取消同步 DNS/网络分区等待。
- 独立规格/质量审查发现两项权限及能力隔离问题，修复复审通过。授权/绑定最终 46 项通过；此前含旧回调回归 42 项、根级路由/资源期限 21 项、客户端绑定定向 1 项通过，相关 mypy、Ruff、语法检查通过。均为本地合成验证。
- 运行时需独立部署注册材料，未借用 Codex token，未执行真实注册/授权或发布。远端撤销因 grant 隔离语义未核实而明确返回不支持；本地停用可用。本任务以 `accounts: authorize tenant MCP connections and BC candidates` 提交。


## 2026-09-11：MCP P1.2 账户与场景读取契约

- 统一 API/MCP 的账户目录、BC 角色和场景事实，保留实际广告账户 ID、明确分页与未知权限；运行连接限定绑定 BC，候选读取使用独立上下文。两个适配器复用同一规范化逻辑，API 继续使用固定官方 SDK。
- 独立审查发现官方 SDK 会把 false、浮点数和字符串业务码转换为整数；在 SDK 原始反序列化入口补上严格校验，避免错误结果被当作成功。每次实际请求仍独立检查准入与期限。
- 初始读取及相关回归 105 项通过；审查修复先复现 6 项失败，修复后双通道 32 项通过，规格与质量复审均通过。相关 ty、Ruff 和语法检查通过；未进行真实 TikTok 调用。
- 本地提交以 `accounts: unify API and MCP read contracts` 标识。后续继续授权、刷新与运行路由接入。

## 2026-09-11：MCP P1.1 租户连接与 BC 模型

- 新增通道类型、凭据/授权/契约独立修订、授权事实、工具观察、MCP 授权候选/刷新 attempt、BC 绑定和默认路由。数据库约束保证租户与连接对应、每条 MCP 连接只绑定一个 BC、默认只指向该 BC 的有效绑定；缺失上游主体/grant 保持空。
- 新迁移 `mcp01` 从实际 `0017_preview_naming` 生成，5 个 TikTok 列原位改名并同步全部调用方、脚本和测试；版权方版本和历史 JSON/远端 ID 保留。既有 API 多 BC 关系全部回填；只有唯一有效连接的 BC 自动设默认，多条有效连接留空。存在 MCP 连接时拒绝降级，防止旧代码把 MCP 凭据当作 API 凭据。
- 规格和质量独立审查通过；相关账户、场景、草稿、来源上传 385 项回归通过，其中含 10 项模型测试和历史升级/降级保护。根代理另验证既有历史迁移/目录 23 项通过；全新专属测试库升级及 Alembic check 通过，模型无未生成差异。改动脚本语法与 Ruff 通过，Alembic env.py 仅保留既有导入顺序例外。
- 测试并行时曾提前执行空迁移骨架，仅影响专属测试库；已废弃该库并建立新测试库完成上述升级与回归，未修改应用或生产数据库。后续迁移在审查和根代理串行升级完成后才恢复并行测试。
- P0.3–4 已提交 `131b5f7`；本任务以 `accounts: model TikTok channels and BC bindings` 本地提交，不推送。继续 P1.2 读取与 P1.3 授权；真实授权和写能力仍未验收。

## 2026-09-11：MCP P0.3–4 会话与共享准入

- 后端直接使用官方 MCP Client，任务各有独立会话和绝对期限；实际 HTTP 请求逐次鉴权/准入，阻止 SDK 隐式重发、重定向和发送后补查目录。收到回执后关闭失败不覆盖结果；发送后无法确认则保持 UNKNOWN 并停用该会话。
- API 仍使用真实 App 配额域；MCP 在未核实独立上游额度时共用保守配额域，连接与候选 ID 不能拆分额度。候选仅能进行协议和账户目录读取，不伪造 BC；正常清理释放并发占用，中断保留到期。
- 独立 HTTP 审查定位并修复 SDK 独立日志、UNKNOWN 并发竞态和主线程中断丢失。51 项本地 HTTP 测试覆盖真实官方客户端、两个确定性竞态和三类中断；协议/解析/传输/通道准入组合 174 项通过，既有及新增准入组合 54 项通过。显式清空三项 API 配置后 MCP 本地调用成功，API 仍拒绝。
- 两项规格与质量复审通过；P0 八份源文件 mypy、传输 Ruff/ty/语法及差异检查通过。复审后仅补类型标注和精确测试键清理，后者定向复测通过。测试使用独立 PostgreSQL/Redis 与本地 HTTP，没有真实 OAuth、TikTok 或广告写入；这不代表真实授权或生产联调通过。
- P0.2 已提交 `8ed44cb`；P0.3–4 因实际 HTTP/Redis 回归相互依赖，以 `mcp: add bounded sessions and shared admission` 一次聚焦提交，避免提交间测试缺依赖。不推送，继续 P1 连接授权。

## 2026-09-11：MCP P0.2 冻结上下文与结果解析

- 新增不含凭据的冻结通道路由、调用证据和副作用错误类型；仅接受契约允许的完整业务回执。自然语言成功、冲突响应、非零业务码、重复 JSON 键和无法确认的回执保持 UNKNOWN，不自动重发。
- 独立规格和质量审查通过；结果解析 68 项，协议/结果组合 107 项通过；相关 Ruff、mypy、语法检查通过。合成结果夹具没有被记作真实 TikTok 返回证据。
- P0.1 已提交 `f88884f`；本任务提交以 `mcp: add frozen routes and strict result decoding` 标识，不推送。传输和配额仍在独立审查。

## 2026-09-11：MCP P0.1 协议与工具契约

- 按用户要求开始分派实施，所有子代理使用 gpt-6-astra/high。固定官方 MCP Python SDK 2.2.0 和 httpx2 2.12.0；公开只读 metadata 核实官方 endpoint、issuer/resource、授权端点与 PKCE，保存公开来源及未核实项。
- 建立账户、场景、素材、广告操作白名单与 schema 比较。工具声明属于文档证据，尚未使用本产品真实授权观察；正常 token 轮换保证、账户写权限和远端大小上限仍未核实。
- 独立审查发现并修复 boolean 枚举、JSON boolean/number 比较及依赖字段语义问题；最终协议 39 项、与结果解析组合 107 项通过，相关 mypy/Ruff/语法及摘要检查通过。没有注册、授权或业务写入。
- 本地提交以 `mcp: define verified protocol and tool contracts` 标识，不推送；后续继续传输、配额与 P1 连接实现。

## 2026-09-11：TikTok MCP 双通道实施计划

- 用户以“没问题”确认书面设计，继续编写实施计划。[总览与执行顺序](superpowers/plans/2026-09-11-tiktok-mcp-implementation.md) 关联 P0 协议、P1 授权连接、P2 素材及 P3 广告四份计划，共 23 个任务；每份列明文件、公共接口、失败用例、实现要点及验收命令。
- 根代理完成设计覆盖和跨阶段接口自查：明确凭据修订与授权语义分离，任务绝对期限沿调用链传递，候选 tools/list 先于业务读取；父预览路由先落库，再一次性更新场景/素材准备函数全部调用方。迁移按实际 head 串行生成，先完整只读，再开放上传与创建。
- 同步设计确认状态和 AGENTS.md 入口。保留现有直接 ENABLE、全部剧目投向全部账户、实际素材来源及 R2 未知用途保护；不引入另一套激活流程，也不复制 Codex 登录凭据。
- 文档验证：5 份计划必需结构与占位符检查、13 个本地链接检查、44 段 Python 示例语法检查通过；语法检查不等于示例执行或业务测试。提交前检查 Git 差异及空白。未安装依赖、修改业务代码、运行应用测试、迁移数据库、注册客户端、真实授权、上传素材、创建广告或部署。
- 本地文档提交以 `docs: plan official TikTok MCP dual-channel implementation` 标识，不推送。下一步从 P0 执行，真实协议、离线测试及目标环境联调分别登记，不能把计划中的预期结果记为已通过。

## 2026-09-11：TikTok 官方 API 与 MCP 双通道书面设计

- 用户确认复用目前骏伯、星屿使用的 TikTok 官方 MCP，后端代码直接调用；连接由各租户管理员自行授权并明确绑定 BC。采用同一业务接口下的 API/MCP 两个适配器，官方 API 保留固定版本 SDK。
- [书面设计](superpowers/specs/2026-09-11-tiktok-dual-channel-mcp-design.md) 已形成，用户随后确认并完成上述阶段计划；未实施代码、数据库迁移或部署。AGENTS.md 同步登记用户变更后的通信约束，旧“仅 SDK”设计作为历史保留。
- 设计包含独立授权/刷新、租户与 BC 隔离、固定父子任务连接、凭据材料与授权语义版本分离、共享限流、R2 用途保护、三级广告与结果未知回读。TK-ADA 继续按具体预览提交后直接 ENABLE；不继承运营 skill 的轮转、固定预算和停用后启用流程。
- 只读代码与脱敏连接配置核对确认两条 MCP 使用同一公开官方 flat 端点；官方介绍和接入目录支持自建客户端及独立 MCP 授权。具体客户端注册、scope 证据、令牌轮换、工具字段和远端重试保证列入 P0 协议核实，不能将已在 Codex 使用视为 TK-ADA 后端联调通过。
- 验证范围：书面设计自查、独立架构审阅、占位符/本地链接检查及 Git 差异空白检查；未运行应用测试，未读取缓存 token、未调用已登录 TikTok MCP、未执行真实授权或广告写入。本地文档提交以 `docs: design official TikTok MCP dual-channel integration` 标识，不推送。

## 2026-09-11：新加坡版权方连接真实读取验收

- 按用户要求检查其新增的两家版权方连接，目标仍为新加坡测试环境，运行版本 `8b59aed`。经应用租户权限校验后分别调用连接 verify，真实登录、应用发现均返回 200/active，无错误。
- 经现有 open_provider_session 的租户隔离路径，对两家各读取剧目第一页，均返回 20 条且还有下一页。仅验证登录/应用发现/剧目读取，未创建推广链接、修改远端业务数据或调用广告写接口。真实应用及连接 ID 不记录于仓库。
- 已比对服务器与本地 connections/session_refresh 文件 SHA-256 一致，Worker/Beat active。自动重登录实现为发现过期后由业务任务驱动登录及重新核对应用，有限重试/冷却；本轮未人为失效真实会话，不能视为真实过期恢复验收。TK-ADA 自身登录有效期运行配置为 8 天，401 后需要用户重新登录。
- 本轮只新增验收记录，无代码改动或部署；文档提交以 `docs: record staging provider connectivity verification` 标识，不推送。

## 2026-09-11：批量视频单文件上限改为 1 GiB

- 用户要求单文件最多 1G，统一按 1 GiB（1024 MiB / 1073741824 字节）实现，包含上限本身。更新浏览器选择/续传校验与提示、后端 R2/URL 默认容量、Compose、环境变量示例和 R2 配置检查脚本；导入与来源 URL 校验直接读取统一设置，删除旧 256 MiB 兜底值。
- R2 分片、原件流式校验、来源 URL 入库及目标 URL 转存仍共用此容量设置；分片大小、并发、暂存空间与旧 FILE SDK 内存保护保持原值。顺手将同文件中 R2 主机名校验的空值判断改为显式 `is not None`，消除 mypy 类型收窄错误，校验语义不变。
- 验证：素材导入、URL 入库、原件校验、SDK 契约共 173 项回归通过（新增越界用例初次误读错误响应层级，修正断言后 3 项边界复测通过）；另有 R2 配置探测、存储及运行配置 32 项通过。测试使用独立 PostgreSQL、Redis 和外部传输替身。前端上传基础与 R2 队列 65 项通过；TypeScript/Vite 构建、Ruff、Biome、mypy 与差异空白检查通过。新增边界测试只验证元数据与选择行为，不声称已完成真实 1 GiB 网络传输。
- 无数据库迁移，未推送或部署。下次发布须将已有环境的 `MATERIAL_URL_MAX_UPLOAD_BYTES` 更新为 `1073741824`，核对每个并发校验任务的临时磁盘余量，并单独记录真实 R2/TikTok 1 GiB 联调。详见 [R2 运行手册](runbooks/r2-video-upload.md) 与 [功能设计](superpowers/specs/2026-09-10-r2-transient-video-upload-design.md)。本次提交以 `fix: raise batch video upload limit to one GiB` 标识。

## 2026-09-11：命名随机号缩短为 4 位

- 用户要求不超过 4 位，并确认纯数字或字母数字混合均可。新批次号改为 4 位 `0-9A-Z` 短码，例如 `A7K2`，共 1,679,616 种组合；沿用数据库全库唯一约束、最多 10 次碰撞重试和耗尽后明确失败，不接受重复号码。
- 编号生成与当前格式校验共享长度和字符集；旧 12 位数字/32 位 UUID 的未完成预览提示修改草稿重建，已冻结和已提交记录不改名。同一预览及其重试仍复用原短码。
- 策略说明及嘉书/网眼三级命名示例同步缩短；金额精度设置与测试保持原样。功能设计已更新，原 12 位实施计划保留为历史记录并注明新规则优先。
- 验证：独立 UTF-8 PostgreSQL 测试实例中，策略、预览、随机号碰撞/并发和历史迁移 88 项通过（6.40 秒）；策略页面 Playwright 36 项通过（14.9 秒）；TypeScript/Vite、Ruff/Biome、mypy、ty、`git diff --check` 通过。没有真实广告或版权方调用。
- 本轮不新增数据库迁移；前次命名功能的 `0017_preview_naming` 仍须随未发布版本执行。仅本地提交，未推送、未部署；提交以 `fix: shorten ad batch codes to four characters` 标识。

## 2026-09-11：版权方默认命名模板与短随机批次号

- 策略新增默认模板 `{provider_pinyin}-{drama_name}-{drama_id}-{random}`，嘉书及无专用规则的版权方使用该模板；支持点击插入版权方拼音、剧名、版权方剧目 ID、随机号和日期。要求保留剧目 ID 与随机号，拒绝非法变量、属性/下标、转换、控制字符与超长名称，不截断归因名。
- 网眼等已有已核验归因基础名的版权方继续优先使用原基础名及特殊后缀。广告组继承 `-g01`、广告继承 `-sp1` 等编号；模板值中的花括号不会再次解析。
- 新预览使用 12 位数字批次号，沿用全库唯一约束 `uq_preview_batch_short`；冲突仅回滚保存点，最多重试 10 次，耗尽明确失败。相同草稿版本、预览恢复与同一批次不同账户复用编号；保留已冻结和已提交广告原名。
- 新增迁移 `0017_preview_naming`（上游 `r2_part_receipts`），仅为 `preview_drama` 增加版权方规范标识和外部剧目 ID 快照列，不改写旧策略 JSON、冻结名称和摘要。旧版仍在 BUILDING 的预览明确失效，页面提示修改草稿重建；历史冻结预览继续读取原记录。
- 前端分别提供默认模板与专用后缀，展示嘉书和网眼三级命名示例；保留 USD 禁用选择与中性黑色主题。同步生成 API 客户端，原总体设计和策略设计已注明新命名设计的优先级。
- 回归发现原批量验收夹具未固定对象存储类型，继承本地 R2 设置后与其模拟的 S3 endpoint 不符，导致 `object_storage_unconfigured`。仅在测试夹具显式设置 S3 类型与区域，未修改业务存储实现或真实配置。
- 验证：策略与搭建模块分组回归 469 项通过（主组 461 项 / 164.09 秒，批量展开 8 项 / 294.54 秒），1 项 Linux 专用 prefork 用例在 macOS 跳过；真实 PostgreSQL 强制撞号、并发碰撞、耗尽后事务继续写入、幂等复用、快照保护及历史迁移均已通过。策略页 36 项、预览页 26 项 Playwright 回归通过；TypeScript/Vite、改动文件 Ruff/Biome、mypy、ty、Alembic check 和差异空白检查通过。外部调用均在传输边界模拟，没有真实投放或版权方写入。
- [功能设计](superpowers/specs/2026-09-11-provider-ad-naming.md) · [实施计划](superpowers/plans/2026-09-11-provider-ad-naming.md)。本轮仅本地实现与提交，未推送、未部署。发布时须先按目标环境手册排空、备份并执行新迁移，不能只更新前端或代码。提交以 `feat: configure provider ad names with unique short batch numbers` 标识。

## 2026-09-11：策略预算币种默认 USD 并禁用选择

- 按用户要求保留预算币种下拉组件及完整选项，新建空白策略默认 USD，禁用鼠标展开与键盘切换；已有策略和历史版本保留原币种，不自动换汇或改写历史数据。
- 默认 USD 不计入用户修改，避免空白表单离开时被误判为未保存。同步原币种选择回归，验证禁用状态、键盘跳过和实际保存请求中的 USD。
- 验证：策略页面 35 项 Playwright 回归通过（18.5 秒）；TypeScript/Vite 构建、2 个改动文件的 Biome 检查与 `git diff --check` 通过。浏览器测试使用 API 边界替身，未调用外部广告接口。
- 无数据库迁移；本轮仅本地修改与提交，未推送、未发布测试或生产环境。提交以 `fix: default strategy currency to USD and disable selection` 标识。

## 2026-09-10：统一 TK-ADA 应用图标

- 使用用户确认的黑白 A/播放符号图标，统一登录页、桌面侧栏和移动导航；保留 18px 品牌名及 11px 副标题。补齐 32px favicon、180px Apple 主屏幕图标、192/512px manifest 图标和开发者平台上传原图。
- 删除未使用的 FastAPI Logo 组件、模板图标和旧品牌样式；不新增离线缓存或服务工作线程。
- TypeScript/Vite 构建、22 项 workspace-shell 浏览器回归、改动组件与 manifest 的 Biome 检查、git diff --check 通过。代码提交 `8b59aed`；已发布新加坡测试环境，线上图标字节/尺寸、manifest、入口边界、管理员登录与 Worker ping 通过，备份 timer 已恢复。服务器编译因内存限制退出 137，改用相同提交的本地已验证产物；未修改生产环境。[发布记录](validation/2026-09-10-staging-singapore-icon-release.md)。

## 2026-09-10：新加坡测试账号与租户初始化

- 按用户授权，经 sslip.io HTTPS 应用 API 修改平台管理员 `admin` 密码，创建 `junbo` 租户及同名租户管理员；密码仅同步服务器与本地私有配置，不写入 Git。
- 实际重新登录验证 `admin` 为平台管理员、`junbo` 为非超级用户；junbo 仅能看到所属租户，成员角色为 tenant_admin，访问平台租户管理返回 403。未配置外部连接或启用素材自动化。
- 私有初始管理员配置和登录凭据已同步，操作后备份成功。记录提交以 `docs: record Singapore tenant administrator setup` 标识，不推送。

## 2026-09-10：按用户要求停用 Sites 转发

- 删除独立 Sites 项目的转发实现和回源环境变量，发布版本 2，源码提交 `5a1e34fccfeb73a4ebf081c151301394938ddd4f`。旧入口所有请求直接返回 410，不再回源或重定向；未删除 sslip.io 域名或修改 TK-ADA 服务器。
- 构建、改动文件 lint、线上 GET 首页/健康路径和 POST 登录返回 410、sslip.io 健康返回 true 均验证通过。Sites 平台项目记录保留，当前工具没有整站删除接口。
- 本仓库文档以 `docs: record retirement of Sites forwarding` 聚焦提交，不推送。此前 Sites 接入条目为历史记录，以本条停用状态为准。

## 2026-09-10：新加坡测试环境 Sites 域名接入

- 按用户要求发布现有 Sites 域名 `https://ytf-server-gateway.defuelscoulter38963.chatgpt.site`，保留其仅所有者访问设置，转发至新加坡 TK-ADA。Sites 独立源码 `148a0a7fce5fd9487366f7006852b5ff5458e29c` 已上传并发布版本 1，原 IP 入口继续有效。
- 服务器新增 sslip.io 主机名的 HTTPS 回源站点及受信任证书，以满足 Workers 不支持裸 IP fetch 的限制；既有每小时证书续期 timer 接管新证书，dry-run 与更新后备份通过。
- 使用自定义 Worker 在框架 URL 规范化之前转发，保留 FastAPI 尾斜杠、状态码、字节流、业务 JWT，剥离 Sites Cookie 和身份头。5 项边界测试、改动文件 lint、构建与实际线上登录/受保护 profile/静态 JS/API 边界验收通过，未开放 Sites 访问权限或执行外部广告操作。
- 详见 [测试环境手册](runbooks/staging-singapore.md)。本仓库文档提交以 `docs: record Sites gateway for Singapore staging` 标识；应用仓库未推送，运行 SHA 未变化。

## 2026-09-10：新加坡测试服务器无 Docker 部署

- 按用户授权在 `137.220.150.31:22211` 安装原生依赖，以固定提交 `995f89569880df330b02314bfff5e1df33256b1a` 部署至 `/opt/tt-ada-staging/releases/`，未安装 Docker、未复制本地或生产业务数据和集成凭据。
- 公网入口 `https://137.220.150.31`，受信任 IP 证书与每小时自动续期已配置；API、2 进程 Linux prefork Worker、唯一 Beat 由独立 systemd 服务管理并开机自启。PostgreSQL、Redis、API 仅本机监听。
- 服务器冻结依赖安装、前端构建、迁移 head、管理员 HTTPS 登录/受保护接口、72 项真实 PostgreSQL/Redis 登录与调度回归、持久化 outbox→Beat→Worker no-op、浏览器登录页与无 console error 均通过。首次回归因应用角色没有 CREATEDB 导致 8 项测试未通过，改用独立测试角色后全部通过，未扩大应用数据库角色权限。
- 每日私有备份已配置且首次执行成功，PostgreSQL 临时库恢复与 Redis RDB 完整性检查通过；IP 证书续期 dry-run 通过。未执行真实 TikTok/R2/版权方调用，素材导入与清理保持关闭。
- [环境手册](runbooks/staging-singapore.md) 和 [验收记录](validation/2026-09-10-staging-singapore-release.md) 记录服务、版本、备份和边界。本轮文档提交以 `docs: record native Singapore staging deployment` 标识；不推送。当前仓库没有本地 `main` 分支，保留既有 `feat/platform-implementation`，不自动创建分支。

## 2026-09-10：品牌更名 TK-ADA 与标题微调

- 按用户要求统一实际页面、浏览器标签、默认配置与 README 的产品名为 `TK-ADA`。左上角共享品牌主标题从14px增至18px，副标题为11px的“广告投放工具”；登录按钮只显示“登录”，移除箭头，保留加载状态。其余页面字号、颜色、卡片和间距不调整。
- 同步既有登录及移动导航测试定位器。TypeScript/Vite生产构建、33个改动TS/TSX文件的Biome检查、22项workspace-shell浏览器回归和本地实际登录页尺寸核对通过。
- 无数据库迁移或业务逻辑变化。生产仍使用原目录、Compose项目、持久卷及备份服务标识，避免名称变更创建新数据环境；发布时只同步私有 PROJECT_NAME，按生产规则备份、换版并只读验收。
- 代码 `d09c070` 已提交、推送并发布；生产只读浏览器18项、两环境bootstrap、实际18px/11px品牌及登录按钮检查通过。发布前备份完成，数据库head和用户/租户/策略数量保持一致，定时备份已恢复。[生产发布记录](validation/2026-09-10-tk-ada-brand-release.md)包含版本、回退入口与验证边界。

## 2026-09-10：骏伯生产发布与基础验收

- 已按用户授权提交并推送现有代码，生产运行 SHA `016217f65a39330b4b715ab043fe866eea87810c`。入口 `https://manjuad.gzjunbo.net:8000/`，HTTP 自动跳转；原站 80/443 和 8 个原有容器保持正常。
- 生产独立 PostgreSQL/Redis，Alembic head 为 `r2_part_receipts`，API、3 个 Linux prefork Worker 与唯一 Beat 同镜像。平台管理员 admin、租户 junbo、租户管理员 junbo 已建立，密码未写入仓库。
- 真实 API 登录/隔离/策略版本测试、19 项完整生产浏览器检查、18 项重启后只读复验、outbox→Beat→Worker no-op、备份及临时库恢复通过。每日备份定时器已启用并实际试跑成功。
- 专用生产 Compose、Nginx、版本入口脚本、备份脚本与 systemd 单元已提交。构建镜像源调整保留锁定版本/hash，受限 GitHub 下载使用同一官方 SDK commit 的已验证 Git 缓存。后续文档提交记录首发事实，不表示运行镜像自动更新。
- [生产部署/更新/数据库规则](runbooks/production-junbo.md) 已登记在 AGENTS.md，[本次发布验收](validation/2026-09-10-production-release.md) 记录具体版本、证据与限制。相关实现提交：`7c11176`、`f39292d`、`5e1ecda`、`016217f`。
- TikTok API 出站探测两次连接超时，需在真实授权前处理；App、BC、R2 和版权方仍未配置，未执行真实上传或广告操作。该限制与基础上线通过分别记录。以下原有“未推送/未部署”描述是当时阶段记录，不代表当前状态。

## 2026-09-10：产品统一命名为 TT ADA

- 用户要求产品改名为 `TT ADA`，所有实际页面不再使用“短剧投放”“TikTok 工作台”或“工作台”作为名称。已覆盖登录页及页脚、侧栏品牌及移动导航、所有已有路由标题、设置页、根路由兜底和返回首页提示；功能页继续使用素材库、广告搭建等业务名称。
- 同步 README、`.env.example` 与 Compose 默认产品名，并更新既有浏览器测试的文案定位器。技术包名、仓库路径及历史设计记录保留。
- 验证：`bun run build`、改动 TS/TSX 的 Biome 检查及 `git diff --check` 通过；`bunx playwright test --project workspace --workers 4 --reporter line` 全部 **318 项通过（2.1 分钟）**。独立静态复核未发现业务改动或实际页面旧名称遗漏。
- 本地 `http://127.0.0.1:8011` 已更新，私有配置仅同步 `PROJECT_NAME` 并重启 API；OpenAPI 标题为 `TT ADA`，bootstrap 检查通过。使用实际本地登录只读检查登录、平台管理、账号设置、租户功能及 404 共 12 个页面，标题与页面文案通过；截图和检查结果保存在忽略的 `.runtime/tt-ada-*`，不提交凭据或本地数据。
- 本轮提交以 `ui: rename product to TT ADA across all pages` 为标识；未推送。

## 2026-09-10：仓库协作约束更新

- 按用户要求更新 `AGENTS.md`：明确独立仓库、聚焦提交、已有分支工作树、合并与显式推送规则，替换旧的功能分支开发及推送约束。
- 补充中文业务注释、复用封装、旧代码清理、禁止旧业务逻辑兼容/回退、影响评估、统一 shadcn/ui 设计、设计与计划拆分及生产发布要求。
- 验证：检查文档差异、发布手册路径和 `git diff --check`；仅文档修改，无需运行应用测试。提交以 `docs: update repository contribution constraints` 为标识，可通过 Git 历史定位；未推送，未改动已有未跟踪目录。

## 当前状态

2026-09-10 全工作区视觉统一：用户认可策略试版并要求主按钮恢复原黑色、推广全部页面。共享正文22px标题/灰底白卡/16与24px内外距、管理页、素材上传、策略编辑、广告搭建/任务与设置辅助页均已统一，整合源码`27ea0aa`，回归提交`47b074a`。完整workspace **318项通过（2.0分钟）**，TypeScript/生产构建及独立审查通过；实际8011应用已更新并核对。已推送回退标签`ui-before-workspace-rollout-20260910`（`601c181`），主目录与推广分支同步。[实施计划](superpowers/plans/2026-09-10-workspace-visual-rollout.md)及[验收记录](validation/2026-09-10-workspace-visual-rollout.md)记录覆盖范围与联调边界。以下视觉记录保留为历史。

2026-09-10 策略列表视觉试版：用户要求先保留代码再试改，已推送回退标签 `ui-before-refinement-20260910`（`4c732a2`）。本轮仅策略列表恢复22px正文标题、灰底白卡分组、16/24px内外距、固定短列与蓝色主按钮；其他页保留上一版。工作区/租户账户93项、策略36项及生产构建通过，独立评审发现的列宽和长数覆盖问题已修复。实际8011页面截图与跨页恢复已核对；[变更与验收记录](validation/2026-09-10-strategy-ui-refinement.md)保留细节及回退方式，等待用户评价试版阅读体验。

2026-09-10 视觉调整：用户确认后已按官方 dashboard-01 统一中性色主题、288px inset侧栏、16px顶栏标题、自适应内容区及单边框列表，代码 `eb613a4`。原有310项workspace回归、新增5项几何回归及生产构建通过；实际8011应用已更新并核对标题、24px边距与菜单跳转。[实施计划](superpowers/plans/2026-09-10-shadcn-official-alignment.md)与[验收记录](validation/2026-09-10-official-ui.md)覆盖本轮变更，以下保留业务功能交付历史。

2026-09-10：普通账号密码登录、邮箱移除和租户版权方自动续登已交付；运行时使用独立Python协议适配器，不调用网眼/嘉书CLI或共享其账号文件。账号身份保留迁移、自动续登和本地浏览器证据见[账号与续登验收](validation/2026-09-10-account-session-integration.md)，该基线 `a176fd8` 的 [CI 34385153117](https://github.com/yaotingfeng/tiktok-ad-automation/actions/runs/34385153117) 七组通过。

本轮继续实现[独立R2上传计划](superpowers/plans/2026-09-10-r2-batch-video-upload-plan.md)：Task1～7代码已形成，Task8已具备离线浏览器、完整业务链、10k/20k元数据和1,000文件故障验收；最终集成回归与本地更新记录在下节。真实R2/TikTok、CORS、当前媒体域名、生产prefork和日吞吐仍按[运行手册](runbooks/r2-video-upload.md)验收，新导入、自动清理默认均关闭。

| 范围 | 当前行为与证据 | 主要提交 |
| --- | --- | --- |
| 临时对象和预算 | 对象代次不可变；tenant/BC隔离；全局8GiB、租户2GiB为应用暂存窗口，预留和已存不重复相加；清理证据释放一次 | 83adf1e, 3beba10, 0b2cdc1 |
| 导入API与恢复 | 最大20k文件；≤200一批，≤100明细分页；稳定请求回执、发送前持久意图、响应丢失只读回查 | d521929, 7bc1912, eacc3a0, 8642e78 |
| 浏览器批量上传 | 4文件×2分片有界传输；IndexedDB保存恢复元数据；暂停、重选核对、跨范围停止；上传不绑定剧目 | 7582056, 9696fb5, 5dc4135 |
| 分片权限及取消 | 每次权限独立身份；completed实际回查、unused受控浏览器声明、unknown保留；实际签名期限不越过台账；取消竞态不能继续Complete | 301a46f, 2d199cb |
| 校验与来源入库 | 流式MD5/SHA256与ffprobe；事务outbox交接；合法来源公平分流；官方SDK URL上传和强回读，记录实际广告账户 | 0b2cdc1, b745e2e, 2df5e85, 2a440ba |
| 删除后素材使用 | 即时授权TikTok URL接力、新目标实际VID/封面、只读预览；新代次不退回文件上传 | e005bd1, 02c0ee8 |
| 自动清理与回收 | 源强回读成功后即安排删除；exact代次Delete/HEAD、Abort/ListParts/HEAD；消费者或结果未知保留预算；定时100项扫描与审计去重 | 8186adc, bc1c9ba, 76cd455, 856b83f, ce0e12f |
| 部署与验收 | 默认零网络检查；显式probe仅自建key；真实API浏览器3文件/4PUT、6条完整业务链、1,000文件400次清理；独立审查通过 | 20d8fed, dc3f5c7, 304a231, 865158c |

本轮证据按边界区分：[10k/20k元数据](acceptance/r2-ingest.md)、[浏览器→真实API](acceptance/r2-browser.md)、[API→校验→源→清理→目标](acceptance/r2-pipeline.md)。浏览器完整上传3个文件、16,782,295字节，终点是stored及3个待发校验任务；后端6条完整链使用真实数据库/校验/业务逻辑和外部传输替身，不冒称Cloudflare或TikTok实际成功。1,000文件混合故障中400个对象实际经清理worker回收，预算归零；它不是1,000次视频平台入库。

20,000文件选择测量：准备好FileList后16.4ms出现可用界面，DOM100行；合成FileList构造另用1,568.1ms，不能称整个选择过程小于1秒。JS堆为离散样本，未测真实峰值、Worker RSS、慢机P95或生产日量。

### 本轮最终集成验证

以下为合并工作树的实跑证据，外部服务均为传输替身；不沿用账号基线的通过数字。

- 完整后端模块（不含独立acceptance目录）：**1,687 passed、1 skipped，730.06秒**。唯一skip为已有Linux prefork测试；该次启动于 `92cf783`，随后SQLModel类型修复另有34项回归；没有把中途改动冒称已被同一次旧进程载入。
- 最终 `742f94a` 的源选择/源上传、分片权限回归、新validator进程测试、R2 probe/capacity以及6条全链/1,000故障批次联合运行：**60 passed、1 skipped，41.10秒**。skip为新增Linux实际硬终止测试，本机只运行其PG用途归属companion；实际Linux结果由CI提供。
- 前端TypeScript和生产构建通过；完整workspace **310 passed，1.8分钟**。首轮309通过/1失败为退出登录导航时读取旧JS上下文；`d29c699` 等待实际登录导航，保留全部停止传输/不再Complete/清除登录断言，连续5次通过后重跑上述全套。
- 合并分支真实浏览器→FastAPI/JWT→loopback HTTPS R2边界：**1 passed，38.8秒**（场景10.1秒），3文件/4PUT/16,782,295字节，3个stored和3个待发校验outbox，刷新零重传，哈希一致。该测试没有启动素材worker。
- 全量Ruff、Ty无诊断通过，Alembic check无新差异。Compose缺失的11个新R2环境字段已在 `153e704` 补齐，独立CLI实际渲染默认/覆盖×本地/预发布四种配置，API/Worker/Beat/prestart全部一致；未启动容器。
- 代码提交 `153e7045eb1e25fd28a9f9bf0bc75b53aee76118` 的 [CI 34429426415](https://github.com/yaotingfeng/tiktok-ad-automation/actions/runs/34429426415) 包含7个独立任务和新增实际API浏览器验收。链接对应确切代码版本，结果以该运行记录为准；文档追加不替代代码验证。

集成中修复了校验初次投递继承未来时间、签名实际期限超台账、取消与Complete竞态、放弃扫描重复告警，以及离线任务调度器缺失真实prefork上下文等问题。Linux validator测试使用原生产handler/guard和真实独占PG/Redis，3秒测试硬期限杀死子进程后检查旧用途保留；它不证明源上传UNKNOWN全链故障或进程被杀后的临时磁盘回收。

### 主目录与本地应用更新

代码已快进到实际目录 `projects/tiktok-ad-automation/`，主目录和隔离开发分支的代码提交同为 `153e704`。原15份路径整理文档先单独提交为 `aedeacf` 再合并，未覆盖；原有未跟踪 `docs/design-history/` 仍保留。后续文档提交继续同步两个分支。

对实际开发库做私有备份，先恢复到新建独占 `_test` 库，执行三次新迁移和Alembic check，核对用户/密码哈希/租户/成员/策略/已有连接与素材记录保持一致。之后仅停止已登记且工作目录匹配的API、Worker、单Beat，再做最终备份；实际库升级至 `r2_part_receipts`，原记录再次核对一致。测试恢复库已删除，业务数据库和Redis保留。

从主目录重建前端并重启 `http://127.0.0.1:8011`；健康、静态登录、回调业务错误和未知API边界检查通过。用户可见浏览器确认已有admin会话可读用户管理，新增用户表单无邮箱；进入原租户后，原策略v2仍显示USD120、ROAS1.08、10素材/组、3创意，版权方独立凭据表单及自动恢复提示可用。未创建测试业务数据、未验证版权方凭据、未请求TikTok授权或写广告。

本地配置选择R2，两个部署开关保持false，尚未提供实际桶/凭据或TikTok BC。macOS Worker仍为solo基础诊断，素材生产任务需要Linux prefork。因此当前本地页面可访问、数据已保留，不代表本机可以完成真实视频入库与投放。实际部署条件继续按运行手册处理。

### 2026-09-09交付基线（历史）

2026-09-09：用户指定仓库 `feat/platform-implementation` 的 P01～P06 功能与页面已完成；P07 本地业务、浏览器、容量、故障与备份恢复验收已完成。最终代码包括网眼/嘉书持久取链、共享 Scene 准备、目标封面准备和发送前检查、大批汇总与恢复计数优化。没有执行真实广告操作。服务器、域名、App 与租户授权后的外部验收仍按单独清单推进。

最新测试与截图见[离线验收](acceptance/offline.md)、[功能页面对照](acceptance/functional-delivery.md)及[容量记录](acceptance/capacity.md)。下表及后续按阶段保留历史提交，不能用历史计数替代最新提交的验证。

用户实际使用后补充验收：代码已迁入当前工作区的 `tiktok-ad-automation/`；修正平台菜单点击返回原页，以及已在租户内却提示“尚未接入租户”的问题。`35a033b` 的完整工作区回归 260 项通过，`813e119` 补充空白新建策略回归；实际本地浏览器已完成策略创建、编辑与刷新回读。目录迁移、页面验证和运行限制见[首次使用验收](validation/local-usability.md)。这次反馈说明原自动化验收对首次使用路径的覆盖不足，不应把“本地模块通过”表述为“当前环境可以正常完成真实投放”。

| 范围 | 状态与证据 | 主要集成提交 |
| --- | --- | --- |
| P01 Task1 基线与官方 SDK | 冻结安装/构建通过；11 个离线契约，独立审查 PASS | 10ee3ee, dc4d3b0 |
| P01 Tasks2/3 公共契约与进程配置 | API、安全错误/日志、真实数据库隔离、JSON任务、控制队列；独立审查 PASS | 6221f52, 6a66c24, ea18b79 |
| P01 Tasks4/7 可靠投递与共享准入 | 52 个真实 PostgreSQL/Redis 测试；公平轮次、崩溃重投、并发锁、六项原子配额，独立审查 PASS | a01b13a, 34e151c |
| P01 Task5 登录、工作台、回调入口 | API客户端再生成；16 个工作台浏览器测试；构建/TS通过；1024断点与焦点修复独立复核 PASS | 1f75633, 3097c72, 192eeab, b619f9a, 6b5cb77 |
| P01 日志审查修复 | 删除会采集 OAuth 查询串的继承 Sentry 初始化；独立回归与复审 PASS | 8ef97a8 |
| P01 Task6 运行与部署包 | 实际本地管理员登录、静态页面/健康/回调检查、Beat→Worker no-op均通过；Compose两套配置通过；GitHub CI镜像构建通过；没有公网环境 | f192a30 |
| P02 Tasks1/2 租户模型、管理与审计 | 34 项租户回归独立审查 PASS；初始管理员搜索支持完整 ID | e46930c, 05ad598, 183ab6c, 78274e7 |
| P02 Task3 OAuth、加密、回调 | 一次性 state、候选凭据、失败保留旧连接、子进程硬截止；62 项专项独立审查 PASS；实际 callback 已接通 | 7eb5170, 318dadb |
| P02 Task4 完整账户目录发现 | 41 项独立测试及迁移 roundtrip/check PASS；逐页持久化、失败恢复、完成后才撤销旧关系 | be5e44b, aa8ada2 |
| P02 Tasks5/6 权限、目录、批量解析 | 38 项独立回归 PASS，含实际 PostgreSQL 10,001 账户无重无漏分页；配置与停用接口通过 | 0e7b20f, 5ea7018, 4b7f1bc |
| P02 Task7 租户、成员、账户、连接 UI | 全部页面已接实际 API；目录分页、完整字符串 BC ID、登录恢复、未保存表单保护及发现完成刷新已修复，最终独立21项+2个请求边界探针 PASS | 0367ead, 9495a1b, 4be7820, 6052bbe, 0177a69 |
| P03 Tasks1/2 模型与版权方协议 | 87项独立复核 PASS；租户应用与链接身份、嘉书配置对账、网眼公开前端协议证据；没有真实业务联调 | e46ec86, 509397e, d30a8d3 |
| P03 Tasks3/4 取链恢复与 API | 154项协议/工作流测试、381项相关跨模块测试通过；205行队列积压饥饿已复现并修复，最终独立50项复核 PASS | 1d5bcdb, 16322eb, 405937e |
| P04 Task1 目录与映射 | 26项独立复核 PASS；字面匹配、稳定分页、实际素材账户映射、Unicode空VID约束与迁移修复 | 5ac7b9e, 993cdf7 |
| P03 Task5 版权方工作区 | 19 项版权方浏览器测试及实际本地 API 空态通过；应用/历史/候选/安全配置差异；新增后端目录独立3项 PASS | 8c09e40, be9a497 |
| P04 Tasks2/3 原件与SDK上传 | 独立复核 PASS；修复真实PG发送前/回读落库锁序死锁及首次撤权无法恢复；未知结果不直接重传 | b50282f, fd26d5d, 49c793d, 755f7f4, 62129be, e1e8dc1 |
| P04 Task4 就绪与分发 | 独立99项相关测试 PASS；本地只读、共享唯一操作、目标实际VID、撤权后同消息只读恢复；60秒控制队列修复已注册 | 8581955, 5125ba0, 415a878 |
| P05 Tasks1–3 策略基础 | 45 项及额外并发/迁移独立复核 PASS；重复保存准确返回原版本 | 6d583fd, 60b106c |
| P05 Task4 草稿与API | 24 项通过；全行反馈、分批解析、字面素材匹配、可追溯编辑、只读分页；已修复独立审查3类P2，待最终复核 | 415a878, 24448d5, e0b3d37, 3d369d7 |
| P05 策略页面 | 26 项策略浏览器测试，连同其他工作区共128项通过；保存/冲突/回查/租户切换及三种屏宽 | 9d60a15 |

集成 `d6684c1` 的后端模块回归（不含单独运行的 `tests/acceptance`）为 **1,224 passed、1 skipped**，211.72 秒。跳过项为 macOS 不支持的真实 prefork 测试；该测试已在 Linux CI 实证通过。P06 UI 的生产静态工作区运行 **239 passed**，一个仅在开发服务器提供的输入 fixture 测试另行通过；不能把这两次运行合称一次 240 项全绿。SDK/接口替身不代表真实平台联调通过。

- `1660e86` 的 [Platform CI](https://github.com/yaotingfeng/tiktok-ad-automation/actions/runs/34297735517) 后端、前端和镜像三个任务全部成功。`d6684c1` 的后续 CI 曾发现测试调度器缺少 Beat 推进、合法任务数量超过单轮测试上限等问题，已补入实际控制任务和有界分块；当前精确提交状态见顶部离线验收链接。
- `d6684c1` 的嘉书完整验收成功：真实模块准备、Scene 获取、冻结、提交、138 次目标视频上传与回读、138 次目标封面上传与回读，最终 6 Campaign / 18 Ad Group / 36 Ad，全部 ENABLE，预算 600 USD。外部传输为严格替身，业务模块与 PostgreSQL、Redis、outbox 使用真实实现。
- 100,000 账户目录与能力准备、200 剧 × 1,000 目标账户的完整冻结计划及 10,200,000 执行步骤已实测完成；原汇总查询缺陷及备份副本接续验证过程保留在容量报告中。
- 最新 `0014_recovery_candidates` 数据库已重新完成真实 `pg_dump` / `pg_restore` 演练；合成凭据解密、对象元数据与待发任务保持一致，没有启动消费者。

## 远端与新增验证

- `be9a497` 的 [GitHub Platform CI](https://github.com/yaotingfeng/tiktok-ad-automation/actions/runs/34280164998) 后端、前端及容器镜像构建全部成功。该结果对应这个提交，不自动代表后续提交已过 CI；后续提交继续推送并核对精确 SHA。
- P01 UI独立复审补跑900/1023/1024px三项，通过；P01 Task6部署包独立审查通过。遗留模板部署指南已改为当前手册入口，旧Compose移除。
- 本地实际 API/数据库的浏览器流程已走通管理员登录、新建租户、进入租户工作台、读取初始管理员与角色。没有接入真实 BC，也没有创建广告。
- P02 Tasks1/2、Task3、Task4、Tasks5/6 独立审查全部 PASS。界面初次独立审查发现候选搜索冒泡误提交和长表表头滚走；已用 8 项先失败后通过的回归修复，并由 root 执行全部 42 项页面回归。
- TikTok SDK同步响应实际是已校验的`{data,request_id}`字典，计划示例中的统一`.to_dict()`假设不成立；OAuth实现已用实际SDK传输测试发现并更正，后续适配器复用同一解析契约。
- P03独立负载审查复现205行健康批次1800秒仿真后只完成74行，原因是恢复任务不断替换尚未发布的revision。`405937e`保持当前delivery ID/revision，未发布记录保留时间与退避、已发布丢失记录重新待发；修复后600秒仿真全部完成。没有改变外部写结果未知时先回查的规则。
- P05验证见[策略](validation/p05-strategies.md)与[草稿](validation/p05-drafts.md)；P04见[原件上传](validation/p04-object-uploads.md)与[SDK素材合约](integrations/tiktok-materials-contract.md)。

## 执行安排

- 所有执行与审查 sub agent 使用用户指定 `gpt-6-astra`、`high`；不会因耗时长而打断。
- 当前会话主线程持续负责集成、生成客户端、迁移、验证与提交。独立任务使用隔离 worktree、明确文件所有权；共享文件由主线程整合。
- 执行七份阶段计划，对应五批交付。P02完成后并行P03版权方、P04素材、P05策略逻辑；P06只读场景契约先于预览资源集成；再完成广告执行恢复与P07跨模块验收。
- 阶段提交推送到用户 origin 的功能分支；不强推。原资料目录不修改，不复制运营凭据。

## 已确认的实现裁定

- 用户仓库为空，导入固定官方模板快照并保留许可证，origin保持用户仓库。
- 实际模板为根目录 workspace/锁文件，计划中的旧锁路径按真实结构更正。
- 素材上传不匹配剧目；搭建时按完整剧名包含匹配文件名。所有剧目铺同一批全部账户，SP为相同素材不同文案的N条创意。
- 预览提交后三级广告创建直接ENABLE；实现阶段不发真实广告请求。
- 本地P01诊断Worker使用macOS solo验证消息链路，不能把它当作后续任务硬截止证据；真实调用需硬截止小于共享租约有效期。
- App、对象存储和配额未配置时相关业务明确阻止使用，登录/基本租户管理仍能运行。
- 官方SDK视频multipart在内部整读文件；原文件入库可以分片传输，SDK上传另设明确的工程容量上限，默认256MiB及1个同时上传。它不是TikTok官方文件限额，超过时保留原文件并说明阻塞原因。
- 已交付迁移保持不可变；P05先交付`0005_strategies`，草稿/预览后续追加迁移，不回头改已提交的策略迁移。

## 待外部条件

没有实际部署主机/域名与 TikTok App，公网 HTTPS 回调、真实 OAuth、官方SDK真实账户发现和广告试投尚未验证。没有以示例 URL、账户或测试替身冒充这些结果。缺少这些条件不阻止后续本地模块实现。详见 [部署手册](runbooks/bootstrap-deployment.md)。

## 下一步

本地工程与部署包已形成，后续按真实联调清单接入部署环境和租户授权。发布时使用对应提交通过的 CI 与同版本镜像，外部试投和实际消耗分别记录。以下条目保留各阶段当时的验证记录，以本页顶部当前状态为准。

### P02 Task 4: directory discovery

- Schema contract `596c1ae`: six directory models, tenant composite keys, one active connection generation, external ownership constraints; migration `02c_directory` after `02b_connections`.
- Added page-atomic resumable discovery, candidate promotion after complete BC/account enumeration, per-call admission, durable recovery/outbox, and the bounded prefork resource task. Unknown capabilities and incomplete metadata remain blocked.
- Verification: 189 account/tenant/jobs tests passed, including 41 Task 4 cases; independent 41-test review and migration roundtrip passed. Root added pytest importlib mode (`0befa24`) to resolve duplicate test filenames in the standard command. All TikTok transport was fake; full evidence and P07 boundaries are recorded in [discovery validation](validation/p02-discovery.md).
- `sent_count` is a durable attempted-send counter after admission, not proof the HTTP transport started if the process crashed immediately afterward. Recovery relies on persisted page/claim/version state, not this counter.

### P04 workspace material read APIs

- Added tenant/BC upload-request recovery, lightweight upload batch pagination and on-demand 300-second private original preview URLs. GET routes do not enqueue work; viewers can read, and missing original/configuration/scope are explicit errors.
- Batch file counts use bounded parent-page SQL aggregation; original preview uses local SigV4 GET signing with inline allowlisted video MIME and no-store. No bucket permission change or external SDK write.
- Verification: 9 new regressions plus affected upload/progress/retry tests total 58 passed; Ruff, strict mypy and ty passed. See [material read API contract](validation/p04-material-read-apis.md). Root generates the client and independently reviews integration; no frontend files changed here.

## 2026-09-09：冻结预览与素材工作区集成

- 素材只读补充接口独立审查 PASS（64 项）；素材 UI 已合并，工作区 157 项浏览器测试通过，最后补充权限场景后素材完整 31 项通过。
- Scene/OAuth 独立审查 PASS（118 项）；发现的单页/跨页重复 ID 完整性问题已关闭。官方 SDK 创建编译器 26 条离线契约验证通过。
- P05 草稿独立复核 PASS（25 项）；冻结预览实现与 HTTP 接口已就绪，builds 模块合计 124 项通过。冻结预览独立审查、全工作区 UI 独立审查进行中。
- BC 账户权限引导任务正在开发，用完整只读能力检查把 UNKNOWN 授权事实转为可验证的创建/上传权限，避免依赖广告链接或逐剧重复扫描 BC。
- 后续继续搭建工作区 UI、提交执行、结果核查/恢复及 P07 验证。未进行真实广告创建或真实外部业务联调。

### P06 link-free BC capability bootstrap

- Added isolated capability jobs/request aliases/pages/account-role evidence with migration `0005e_account_capabilities` (schema commit `2c107db`).
- Explicit tenant/BC/connection refresh command, read-only status/evidence APIs, 50-row official current-token BC reads, complete-list publication in 100-grant transactions, durable claim/dispatch recovery. Details and integration registration contract: `docs/validation/p06-account-capabilities.md`.
- Dedicated engineering age setting defaults to four hours from first observation; no per-link BC scan and no capability inferred from account visibility. Root owns Scene/draft/preview integration.

## 2026-09-09：搭建执行与任务查询

- 冻结预览独立审查通过；批量输入、原输入完整分页、分组编辑回执、按剧聚合、预算守恒、预览提交与未知响应回查均已接实际 API，UI 集成 `c29710a`。
- 提交与分批展开 `7761232`：同一预览永久只提交一次，同一草稿的历史组合预留不重复创建；账户×剧目展开及步骤持久化均有界。GET 不触发 SDK 或新任务。
- BC 能力目录版本 `ac9bc53` / `a97a21b`：真实 PostgreSQL 验证 5,000 账户后，能力读取按目录版本主键查询，不再逐次扫描整个 BC；目录插入、移动、删除与并发事务均有版本围栏。
- 执行状态、准入与 SDK 创建 `315fe06` / `b1cfde8` / `a1b8764`：请求意图提交后才调用 SDK，已知 ID 及时持久化；Campaign/Ad Group/Ad 直接 ENABLE，SP 共用目标 VID/封面且文案不同。
- 独立审查修复 `7689b26`：CTA 放在官方 `ad_configuration.call_to_action_id`；已收到远端 ID 但 PostgreSQL commit 失败时，在新的安全事务中保留 `LATE_CREATED` 证据；Redis 暂不可用保留未发送步骤重排。文案 100 字符是明确的应用策略。
- 官方 SDK 回读 `771933a`：分页完整性、同名歧义、实际父级与目标素材、CTA、预算、ROAS、链接、文案与 ENABLE 状态均核对；找不到或无法唯一匹配保持 UNKNOWN。
- 任务查询 `ec483bd` / `9ca6d13`：列表与分层详情均分页，组内素材显示已核实的目标 VID/封面，事件仅暴露白名单；版权方/策略绑定来自冻结预览。
- 队列执行 `7dddbe4`：unit→step 锁序、结果与下一步 outbox 同事务、当前消息修复与业务退避分开；完整成功、远端成功丢响应后回读续建、回读为空停止自动重试已通过 10 条专项测试。
- Linux 进程故障测试 `a842358` 已提交，运行状态独立记录；当前新增 Scene 编排、recovery 与 UI 不在上述已通过范围中。
## P06 recovery backend — 2026-09-09

- Added permanent tenant-scoped retry/reconcile request receipts and separate mutable recovery progress (`0010_submission_recovery`); immutable intent triggers and preview/BC composite foreign keys. Added per-step evidence index (`0011_recovery_evidence_index`).
- Added 100-step durable recovery scanning, caller/original-actor separation, current account gates, dependency-aware definitely-unsent retry, existing readback queue reuse, exact current-generation outbox repair, and SQL-derived recovery counts/buttons. Historical receipt GET remains QUEUED/0 and read-only; job COMPLETED means scan scheduling finished.
- MATERIAL reconciliation now carries strict `read_only` through the original distribution verifier and all continuations. Delayed failure cannot select a new upload; successful original receipts can revalidate their existing VID. Original frozen intent and actual source history remain intact.
- Verified on isolated PostgreSQL and actual Redis with official SDK transport doubles: 476 builds/materials regressions passed; 38 focused recovery/SDK checks passed; final 20 boundary/SDK checks passed after the last strict-read changes. Ruff, mypy, ty, Alembic upgrade/check, and diff whitespace checks passed. No real provider/TikTok/S3 operations.
- Integration: include `builds.recovery_api.router`; include `app.modules.builds.recovery_tasks` in Celery; register periodic `builds.repair_recoveries` on control; map `recovery_no_candidates` to HTTP 409. Root owns bounded terminal material-result synchronization and shared API/client registration. Details: `docs/contracts/submission-recovery.md`.

## 2026-09-09：工作区目录整理

- 应用迁入 `projects/tiktok-ad-automation/`；工作区根目录旧入口保留为兼容符号链接，供现有虚拟环境与运行进程使用。
- 修复并验证 51 个 Git worktree 的访问路径，同步应用文档中的工作区绝对路径和兄弟工具路径。业务实现未改动。
- 根目录旧设计计划整体归档至工作区 `archive/workspace-design-20260909/`；应用内设计与实施进度继续作为当前开发入口。
- 验证：Git worktree 访问、`git diff --check`、Python 虚拟环境可执行文件通过；工作区批处理、VID 接力、骏伯监控离线回归通过。未运行应用全量测试，未调用外部广告接口或发送消息。
## 2026-09-12：真实 MCP BC 分页参数修复

- Sun Browser 复现授权后 BC 弹窗“资源版本或请求标识冲突”；服务端实际回执为官方 bc_get code=40002、page_size 最大 50。候选读取错误地请求 100，改为每页 50；工具 schema 没有声明该上限，不能只凭 tools/list 判断可用。
- BC 分页测试先复现失败，更新为 101 个 BC 分三页并检查实际发送参数；保留分页中途失败时旧连接不被替换的验证。MCP 业务拒绝使用固定脱敏提示，不再显示通用版本冲突，也不回显上游原文。
- 独立 gpt-6-astra high 只读审查确认后续发现使用 50 条分页，未知上传/创建权限不会阻断目录。`e290b1b` 已发布，Sun Browser 真实重新授权、BC 读取及用户确认后的绑定全部通过；连接 ACTIVE、发现 COMPLETE，42 个真实账户已在页面展示，见 [发布记录](validation/2026-09-12-staging-mcp-pagination.md)。上传/搭建权限仍未知，未执行相关写入。

## 2026-09-12：三剧真实页面测试发现合同阻断

- 用户要求在新加坡测试站点已登录的 junbo 租户，通过正常页面上传 69 条素材，并向指定的两个账户搭建三部网眼剧。已通过页面提交准备草稿；三部剧真实取链成功，账户准备持续等待。业务证据与草稿标识只保存在私有 `.runtime/e2e-20260912/`。
- 根因：实际 tools/list 与旧离线声明在五个构建合同上不一致（其中四个进入当前运行网关），包含整数约束及嵌套对象；全目录检查先阻断账户读取，错误随后被包装成网络失败无限重试。已依据实际公开 schema 更新合同及摘要，并保留确定合同错误码，将权限任务置为 BLOCKED。
- 验证：29 个合同全部匹配本次真实目录快照；105 项定向测试通过，扩展账户能力/网关/协议回归 176 passed；真实 PostgreSQL 独立测试库与 Redis 14，外部传输使用替身；Ruff、Python 编译及 diff 检查通过。
- 本节记录代码修复，测试环境发布及页面后续复验另行补记。浏览器扩展尚未允许本地文件访问，素材上传与真实广告搭建尚未完成，不作为端到端通过。
- 上述修复已提交 `a69d050` 并完成测试环境发布，完整备份、五份归档校验及隔离恢复验证通过；三服务同版本，HTTPS/页面登录正常。真实链路仍待用户在 TikTok 登录页完成原连接刷新、开启浏览器本地文件权限后继续；详见 [页面测试与修复记录](validation/2026-09-12-staging-e2e-schema-fix.md)。未推送。

## 2026-09-12：接口升级保留既有授权

- 后续定位修正：账户已获取，升级后的合同摘要与原连接不一致才是管理读取被阻断的原因，不应据此要求用户重新 OAuth。管理目录刷新现在用原凭据重新核验真实工具目录，全部匹配后原子更新接口版本；不改凭据、授权代数或 BC 绑定代数。
- 旧合同仅限协议重观测，业务读取仍要求当前合同；真实目录不匹配时保留原连接版本，不生成通过证据。已有冻结任务不迁移。
- 真实 PostgreSQL 独立测试库及 Redis 14：管理升级、多 BC 管理/发现/新鲜度、运行目录五组回归 45 passed；外部服务使用传输替身。Ruff、编译及差异检查通过，线上复验另行记录。
- R2 页面已创建私有测试桶并核实本站点 CORS；新凭据尚待浏览器要求的提交确认，服务器尚未配置。浏览器本地文件选择权限仍被拒绝；素材上传和广告搭建尚未完成。

- `86fafbd` 已完成完整备份及测试发布，页面使用原授权真实读取 BC 成功，无需重新 OAuth；42 个原账户目录及授权/绑定快照在发布前后保持一致。后续已从页面发起账户同步并重新准备本批输入，素材及广告仍待验，见上述验证记录。

## 2026-09-12：现有桶长期 R2 凭据接入

- 用户确认后通过页面创建现有桶的长期账户密钥，完整备份后配置测试服务器。受控真实分片上传/读取/删除八步探测通过，三服务实际 R2 配置一致；凭据和探测业务明细保存在私有运行目录。
- 导入开启、租户窗口 4 GiB、自动清理暂关闭。浏览器仍拒绝本地文件选择；MCP 写权限事实仍待核实，未报告三剧端到端通过。详见 [验证记录](validation/2026-09-12-staging-e2e-schema-fix.md)。

## 2026-09-12：统一多环境配置入口

- 新增 `config/README.md` 与 `config/environments.json`，登记 local/staging/production 的项目内私有文件、服务器生效路径和运行手册；README、AGENTS 与环境手册均指向统一入口。`.env.example` 为唯一完整模板，补齐 MCP 注册、回调、配额域及 Sentry 参数。
- `scripts/environment.py` 提供 init/path/info，不依赖当前工作目录、不读取凭据、不覆盖已有文件，初始化权限 0600。三环境初始化、重复初始化不覆盖、跨目录调用及安全元数据检查通过；Ruff、编译与 Git 忽略核对通过。
- 现有本地 `.env` 保留；从测试服务器取回生效配置到被忽略的 `.env.staging`，校验和一致、权限 0600；`.env.production` 仅初始化模板，未复制测试凭据。没有修改服务器配置、重启服务或部署代码。实际环境文件不进入本次提交。

## 2026-09-12：上传队列轮询稳定性与入库阻断定位

- 用户手动上传的单文件已完成 R2 接收及原件校验，尚无平台上传尝试。实际阻断为 material_channel_unverified：当前 MCP 视频策略固定未核实，在选择来源账户前即阻断，不是 TikTok 拒绝上传。调整此五项服务能力门禁的实际回读方案已向用户提出，尚未获答复，不伪造能力或授权事实。
- 上传队列和历史队列静默刷新：取消背景轮询提示行，分页不随每次轮询禁用，避免订阅 isFetching 导致单元格重复卸载。首次请求/翻页仍保留加载与错误处理。补充门禁及权限未知的明确说明，已接收原件无需重传。
- 47 项素材/导入前端回归全部通过；专项验证刷新期间按钮节点不被重建、表格位置不动、分页保持可用。TypeScript、Vite 构建、Biome 检查通过。线上发布及复验另记。

- 队列修复 `6eded64` 已完成测试发布与真实页面复验：手动刷新及 10.5 秒轮询期间无提示行、按钮未替换、表格位置不动，详情可直接打开；完整备份和版本记录见同日验证文档。上传门禁尚待设计答复。

## 2026-09-13：MCP 素材 URL 上传接通

- 用户确认 URL 上传已多次实际使用，明确要求修改、推送并部署新加坡测试环境，使用其指定账户验证已保存原件。按服务器现有租户/BC/连接执行，未调用 Codex 的 TikTok MCP。
- 移除永久未知的五项服务内部保证门禁，改为已接入合同与工程容量、实际 token scope/主体/完整目录及账户角色、一次发送和真实 MD5/大小回读。上传和搭建能力分别核实，旧未知上传事实在显式重检时刷新，不改授权或绑定代数。
- 修复完整远端文件名追加关联后缀后超长；指定账户恢复复用原来源选择/槽位/冻结路由，失权或满载不得换账户。原件无需重新上传。
- 定向回归 125 passed；Ruff、改动 Python 编译及 9 个源文件严格 mypy 通过。扩展检查与发布、真实到账结果见 [修复记录](validation/2026-09-13-mcp-material-upload.md)，不以模拟测试代替真实上传。

- `bc538c0` 已推送部署，真实账户上传权限刷新完成，指定账户已收到原件。后续实际回查发现 MCP 素材为 JSON 文本回执，新增严格原生文本解析与上传单条数组规范化；未知上传用持久文件名筛选，避免万条历史库扫描超限。保留原冻结版本/请求，恢复只读，不重发视频。回执/恢复回归分组 244 passed / 1 skipped，最终适配器 68 passed / 1 skipped、SDK 恢复专项通过，5 文件 mypy 通过；最终部署和队列状态见验证记录。

- `b985042` 已推送并完成第二次完整备份部署。实际原件在指定账户回读成功：文件 available、操作 succeeded、账户素材 available，页面同源接口已上传 1 / 已入库 1 / 失败 0；上传尝试共 1 次。三服务及备份 timer 正常，数据库 head 未变，实际 VID/MD5/大小证据留私有目录。

## 2026-09-13：测试环境自动原件清理开启

- 用户明确授权后，完成独立项目、数据库、Redis、配置完整备份及隔离恢复验证，将服务器 `MATERIAL_CLEANUP_ENABLED` 改为 true 并重启三服务；运行代码和数据库 head 不变，备份 timer 已恢复。
- 自动任务完成 24 个原件删除及 HEAD 确认，24 个账户素材保持 available。新批次同源接口确认可用 23、已清理 23、失败 0、占用 0；登录和权限隔离检查通过。备份目录与验收证据见 [自动清理记录](validation/2026-09-13-staging-original-cleanup.md)。

## 2026-09-13：功能开关发布规则

- 按用户要求，在通用部署手册集中列出当前两个布尔开关的范围、默认值、关闭影响、依赖和业务验收；首次部署及新增/语义变化项先准备具体方案并确认，普通升级沿用已确认值，避免模板覆盖导致功能不可用。
- 补齐逐项发布前后值、历史积压影响、服务实际加载、业务结果与配置回退要求；配置入口、首次/测试/生产/R2 手册、模板注释和 AGENTS 均指向同一规则。澄清开关是实例级，不能将小批验收描述为开关按批隔离。本轮仅更新文档和模板注释，不改变默认值或服务器配置。
- 验证：Settings 的两个布尔开关均已纳入清单，模板实际参数值与修改前完全一致，92 个文档本地链接均可解析，`git diff --check` 通过。

## 2026-09-13：移除正常上传后的强制回查

- 用户要求评估并移除非必要逐条查询。核对官方上传接口说明及实际代码：后台队列执行 URL 上传，正常同次业务响应返回 VID；现有保存字段不足以证明 MCP 上传必含大小/MD5，不把查询样本当作上传回执。
- 正常成功且含有效 VID 时保存回执后立即完成来源入库、槽位/用途释放和清理排队；不再固定等待 60 秒后发起第一次回查。客户端关闭异常不会撤销已完成入库。超时、响应丢失、本地发布中断、身份冲突等仍用原账户/连接恢复，不重复上传；两种完成方式分别记录 confirmation_source。
- 无新开关、无迁移，已确认的导入/清理值不变；设计与验收规则见 [成功回执决策](superpowers/specs/2026-09-13-upload-success-receipt.md)。定向完整回归 104 passed / 4 Linux skipped；补充原生文本回执及双通道成功路径 4 passed。Ruff、编译、2 个实现文件 mypy 和文档链接检查通过。Linux 隔离 PostgreSQL/Redis 下 17 passed（含 4 个双 prefork 异常恢复用例）；所有外部请求均为传输边界替身。发布结果另记。
- `73822a1` 已推送并完成测试部署，完整备份及恢复校验通过；三服务加载同一版本，导入/清理均保持 true。登录、隔离及既有队列接口通过；未另传真实测试素材，最新批次此前的 1 个上传失败保留，详见 [发布记录](validation/2026-09-13-upload-success-receipt.md)。

## 2026-09-13：素材完整响应留档

- 用户要求完整保存素材接口返回。为既有 URL 上传与异常恢复查询接入双通道响应观察器，SDK 保存反序列化前正文，MCP 保存完整工具结果与文本原文；不新增正常成功后的回查。
- 新增独立加密压缩归档表，按租户/BC/素材/账户/原操作/冻结连接关联，管理员受控读取并校验长度与 SHA-256；未知字段及错误正文保留，普通状态与日志继续白名单。保存事务本地重试不重新上传，失败后保留 UNKNOWN。历史上传原文不可补回。
- 新迁移 `mat_response_archive`，无新开关，沿用原加密密钥；备份、恢复、容量与禁止随原件删除要求已补入部署手册，详见[设计](superpowers/specs/2026-09-13-material-response-archive.md)。
- 本地 187 passed / 4 Linux skipped；12 项专项覆盖两通道完整还原、未知字段、业务报错、无 VID、HTTP 错误/无法解析、权限/租户/BC/素材隔离、查询分别留档及保存失败不重复上传。Ruff、编译、7 文件 mypy 通过。服务器隔离 PostgreSQL/Redis 下 16 passed，含 4 项 Linux 双 prefork 迟到回执/强制终止恢复；临时测试库已删除。外部请求均为传输边界替身，实际发布与留档验收另记。

- `5fff6d2` 已推送并完成全量备份、恢复校验、迁移和测试部署；三服务及全部 Worker 子进程配置一致、两个开关继续 true，备份 timer 正常。通过服务器自身冻结 MCP 连接只读查询一次既有素材，真实完整工具响应入库并按字节长度/SHA-256 还原一致，含 MD5/大小；未新增上传或重试历史失败项。详见[发布验收](validation/2026-09-13-material-response-archive.md)。

## 2026-09-13：MCP 账户解析搭建权限阻塞

- 用户现有草稿的两个账户已准确匹配，但均因 `account_build_unverified` 阻塞。实际 MCP token 与目录/角色已核实；根因是适配器固定返回未知搭建权限、发布阶段保留旧值及完整能力缓存继续复用。
- 将真实 token 的 `mcp:tt4b` 与真实主体观察一并用于已接入搭建动作，发布本轮全部动作范围；新准备/重检遇到任一未知动作先重新观察，保留租户/BC、冻结连接、角色、失权与接口准入校验。无新开关、无迁移、不直接改账户权限。设计见[修复说明](superpowers/specs/2026-09-13-mcp-build-permission.md)。
- 相关账户目录、MCP 再观察、权限、缓存及草稿准备回归 116 passed；多 BC 新鲜度 9 passed。Ruff、编译、2 文件 mypy 与文档链接检查通过。实际发布另记；线上仅重新准备当前草稿，不提交或创建广告。浏览器连接为空，使用页面同源 API 与服务器任务验证。

### 2026-09-13 实际广告提交链路修复（进行中）

- 用户明确授权沿用当前草稿，解决全部卡点并实际提交/创建广告。账户解析修复已部署，两个目标账户已解析成功，24 个素材就绪。
- 服务器官方 MCP 实际 `minis_get` 成功回执为 JSON TextContent；场景/创建工具沿用严格 envelope 校验接收此载体，冻结工具契约代数保持不变。
- 增加租户管理员 Minis 关联 API 和审计，版权方发现/自动续期不再用空值覆盖配置；实际目标账户已读到与应用对应的 LemonShow Minis，待发版后关联并继续场景、预览及真实提交。
- 验证：关联/权限/续期与响应回归 177 passed；扩大至 TikTok 集成及场景回归时 462 passed、1 skipped，3 个旧预期已修正（JSON 载体、来源上传不再回查），重跑相应用例通过。端到端合成夹具补齐多 BC 目录完成证明；本地测试不作为真实广告成功依据。5 个实现文件 mypy、所有改动 Ruff 与 diff 检查通过。

### 2026-09-13 原生 Minis 场景卡点

- `ea59862` 已推送并部署测试服务器；完整备份 `/var/backups/tt-ada-staging/20260912T191743Z/` 经隔离恢复、素材密文解密摘要、项目/配置和五个归档摘要验证；API、3 个 Worker 进程、Beat 同版本同配置，导入/清理开关仍 true。
- 已通过租户管理 API 保存实际 LemonShow Minis 关联，原草稿修订 5；真实预检发现账户为 TT_USER、地区查询拒绝 MINI_APP 枚举、账户具备 IAA 当日最小 ROAS 而非购买资格。具体修正及边界见[原生场景设计](superpowers/specs/2026-09-13-native-minis-scene.md)。
- 新解析直接读取私有留存的真实身份、地区与 VBO 响应均通过；没有伪造场景/创建证据。实际广告提交尚在继续。
- 本轮回归：原生场景形状、双通道读取、场景任务/地区、请求合同和创建/回读适配器 184 passed；5 个实现文件 mypy、Ruff/diff 检查通过。
- `5c25261` 已推送并部署，备份 `/var/backups/tt-ada-staging/20260912T192735Z/` 全量归档与隔离恢复通过，运行配置一致；草稿修订 6 两个账户场景 COMPLETE。预览发现媒体域名配置为空以及一条旧的发送前失败来源素材，继续配置实际 TikTok CDN 并沿生产恢复服务补传，暂未创建广告。
- 真实提交已受理：批次 LSP7，2 个账户、2 Campaign/2 AdGroup/4 Ad，每条 24 素材，预算与 ROAS 保持原策略。两账户 CTA 已实际创建；跨账户分发进行中。真实不同视频还返回 v19-tt4b CDN，因此将已验证的 v16/v19 精确域名一并配置；不存在 wildcard。旧来源未发送失败项已由生产恢复服务补传成功，原24条均有来源。
- `48b6173` 媒体双主机配置已部署，备份 `/var/backups/tt-ada-staging/20260912T194132Z/` 经完整恢复验证；所有进程均加载 v16/v19，导入/清理仍 true。真实封面上传返回图片 ID 与摘要，但去重后的可变名称和 displayable=false 被旧校验误拒；按官方文档和真实回执修正，保留 ID/摘要/比例和未知 ID 搜索约束。
- 封面回归覆盖 SDK/双通道适配器和持久任务：81 项通过，新加用例修正测试状态名后单独通过；Ruff、mypy 和 diff 检查通过。下一步对已有图片只读核查，复用已创建 CTA 和目标素材继续原提交。

### 2026-09-13 真实创建接口 request_id 修复

- 原批次已完成 48 个目标视频及封面；Campaign 返回 40002，原始私有回执明确为 UUID 无法解析成 int64，且两账户远端名称查询为空。
- 重新读取官方 Campaign 文档 1843312852800706，确认 request_id 必须是 int64 字符串。新请求从持久 attempt UUID 派生合法正整数；安全重排和回读使用同一算法，历史 UUID 仅可只读核查。
- 原批次修复保留原始请求和失败证据，只有明确的参数解析拒绝才允许运维修复；不把空查询或任意非零业务码当作安全重发证据。真实广告验收尚待后续记录。

- 两个 Campaign 已收到真实创建 ID，并完成远端名称读取。Minis Campaign 的原生 GET 即使显式 fields 也不返回 catalog_enabled；移除无用模板字段及对应观察模型，历史明确 false 仅在冻结输入边界移除，不伪造远端事实。

- 追加修复 IAA 的 VBO 窗口：已核实的能力是 Day 0，创建必须显式发送 ZERO_DAY，避免官方默认 SEVEN_DAYS。旧 IAA 快照按其原始 Day 0 能力规范化比较；回读仍要求实际窗口字段。117 项创建/适配器回归通过，追加合同、原生回读及窗口用例 29 项通过，旧预览双通道完整创建链 2 项通过；Ruff 和 5 文件 mypy 通过。首次追加旧预览测试触发冻结保护，已改为冻结前构造旧快照后通过。

### 2026-09-13 原生 MCP 回读金额精度

- `27bc0d2` 已完整备份、恢复验证后部署；两 Campaign 和两 Ad Group 均取得真实 ID。所有进程沿用原开关/配置，迁移 head 不变。
- 实际广告组回执中的 roas_bid 为 JSON 数字 1.05。原生 TextContent 解码曾先转成 float，使严格金额 DTO 拒绝回读。现在只对 Campaign/Ad Group 已校验的原始 JSON 文本金钱字段提取精确十进制文本，其余数据类型和无原文的结构化浮点保持不变。
- 130 项 MCP 解码/创建合同测试通过；不放宽丢失精度、冲突载体、未知结果或缺字段判定。两条 Ads 已真实创建并通过系统回读，其余 Ads 和广告组最终回读仍在继续。实际广告组原始 JSON 经本地新版解码与原冻结请求严格比较为 MATCH；mypy 通过。

### 2026-09-13 广告组原生完整回读

- 四条 Ads 全部创建成功并通过系统回查，远端均为 ENABLE / AD_STATUS_AUDIT，每条 24 个视频。
- 广告组显式 fields 请求原生响应漏掉 targeting_spec 和 operation_status，即使已请求 targeting_spec；无 fields 时两者完整。改为读取完整广告组，保留原父级/ID/分页限制，严格比较所有冻结字段，避免多余的状态补查。缺字段仍不伪造通过。

- 验证：合同和广告组补查回归 30 项通过，Ruff/mypy 通过；使用真实完整广告组回执和原始冻结请求验证严格 MATCH。

### 2026-09-13 新加坡真实广告搭建完成

- 最终运行 `0d33c442151d17c3937223c2ed799124e45c4aeb`；用户原批次的浏览器同源业务接口返回 COMPLETED。2 个账户、2 Campaign、2 Ad Group、4 Ads 全部 SUCCEEDED，8 个三级对象全部完成严格回读，失败/待处理/未知均为 0；48 个素材步骤和 2 个 CTA 完成。
- 独立服务器官方 MCP 读取确认 4 条 Ads 均 ENABLE / AD_STATUS_AUDIT，每条包含 24 个视频。平台审核尚未通过不等于创建失败；本次不声称已经产生消耗。
- 代码已推送原功能分支。每次发布均完整备份、隔离恢复验证后切换；最终备份为 `/var/backups/tt-ada-staging/20260912T202019Z/`。API/全部 Worker/Beat 同版本同配置；导入、自动清理和两个实际媒体主机配置保持原值。
- 最终验收及验证边界见 [真实广告搭建验收](validation/2026-09-13-staging-real-ad-build.md)。本轮通过服务器应用及官方 MCP 完成实际操作，未使用本地广告 MCP，也未把 API 操作描述为浏览器点击。

### 2026-09-13 同 BC 原生共享、Mini 配置入口与素材原名

- 按用户确认统一双通道分发策略：同 BC 调官方素材共享，跨 BC 沿来源自己的冻结授权读取 URL 再上传目标。新增共享合同、官方 SDK/MCP 适配器及真实工具目录证据；同 BC 不依赖原件存储或 URL 白名单，共享结果未知时只核查，不自动重新上传。
- 来源选择按租户/授权及可信 SHA-256、MD5、字节数约束，原始来源优先，查询数量不随批次逐条增加。迁移 `material_source_bc` 拆开来源 BC/素材外键与目标范围，旧关系按原约束回填，不放宽租户边界。
- 版权方连接的已发现应用新增管理员 Mini 关联入口，沿已有接口保存并刷新，操作员不可配置；LemonShow 既有映射保持。上传名称保留可读原名/尾部编号及短关联码，广告视频信息显式携带原名；不改历史冻结请求或既有广告。
- 无新增开关，发布保持已授权的导入/清理开启状态；部署手册补齐共享配额、迁移、MCP 工具合同刷新及 Mini 页面操作。完成验证与实际发布见 [本次验收](validation/2026-09-13-native-material-sharing.md)。

- `b89ad1f` 已推送并经全量备份/隔离恢复及迁移演练部署测试服务器；最终相关后端 493 passed、1 skipped，前端浏览器 22 passed、构建及 12 文件 mypy/Ruff 通过。首轮演练 OS 解释器权限问题在业务迁移前中止并恢复旧服务，改用应用角色恢复库后重新完整发布。
- 服务器实际工具目录/BC 同步及正常角色重检全部完成；用户给定账户的单条原生共享 ready，目标 VID 已取回，无 URL 上传；原广告批次仍 COMPLETED。API、3 Worker 进程、Beat 同版本同配置，导入/清理仍 true，无新开关。最终备份 `/var/backups/tt-ada-staging/20260913T060649Z/`，数据库 head `material_source_bc`。详见上述验收边界。

### 2026-09-13 素材库隐藏 VID 并统一素材id 文案

- 素材详情的账户资产表移除 VID 标签、值及复制入口；原 MID 列改为“素材id”，缺失时显示“尚无素材id”。账户视频预览移除 VID 展示，保留实际账户和视频信息。
- 仅调整前端展示，继续使用原 MID 值；API 字段、上传、分发及预览请求保持原合同。
- 验证：`npx --yes bun run build`、`npx --yes bun x biome check src/features/materials/AssetDetails.tsx tests/materials.spec.ts` 通过；`npx --yes bun x playwright test tests/materials.spec.ts --project=workspace --workers=2 --reporter=line` 17 passed。现有测试同步检查隐藏 VID、素材id 列及远端视频正常预览，均为本地接口替身验证。
- 本轮聚焦提交：`materials: hide VID and label MID as 素材id`；未推送或发布。

## 2026-09-13 自动授权权限与小程序名称选择

- 用户确认按自动权限、取消版权方强关联的方案优化并延续测试部署授权。完整账户目录发布直接复用授权事实和角色页形成权限缓存；API/MCP 均不要求额外试上传、试建或人工能力评估。实际 Analyst、未知授权、撤销及范围变更仍受真实限制。
- 新增按租户/精确推广链接保存的 Mini 确认记录和 `automatic_mini_targets` 迁移。已有有效链接保留历史确认关系，新链接不继承版权方配置；旧手填管理接口和页面入口移除。
- 准备页读取实际账户可用 Mini 名称；明确链接可匹配，否则用户选择批次目标。选择采用草稿版本和原请求幂等记录，未知结果查询后继续准备。账户目录分页缓存可复用，Mini 选择不新增平台请求；后续仍检查每个目标账户可用性。
- 浏览器相关 50 项通过，包含名称选择、保存后自动准备、未知结果恢复和只读成员边界。前端构建、5 个实现文件 mypy 和改动 Ruff 检查通过。后端复核 215 项通过，追加 API 角色/选择/隔离 25 项通过；真实发布证据另记本次验收；不把合成测试当作新广告创建成功。

- 自动权限/小程序代码 `9c23a4c` 已推送并发布到新加坡测试环境。独立发布副本后端 29 项、浏览器 50 项通过；完整备份及隔离恢复/迁移演练通过，备份 `/var/backups/tt-ada-staging/20260913T072535Z/`，运行 head `automatic_mini_targets`。用户指定账户同步后直接获得上传/搭建权限，实际 Mini 返回 LemonShow/NovaShort，场景 ready，原广告提交仍 COMPLETED。API、3 Worker、Beat 同版本同配置，导入/清理保持 true。详见[发布验收](validation/2026-09-13-automatic-authorization-minis.md)。

## 2026-09-13 未完成搭建入口

- 保留保存草稿，在广告搭建页增加“未完成搭建”：按当前租户/BC分页展示剧名摘要、输入及已解析账户数量、策略版本、状态和最近修改时间。
- 继续入口恢复原输入、原准备进度或当前版本预览；不自动发起准备或创建。已提交批次仍从搭建任务处理，切入口保护未保存输入，403隐藏旧行。
- 本次内容独立导出验证：草稿回归21项、页面81项通过；叠加准备页提交 `d27d0e3` 后最终后端23项、页面83项通过，构建、mypy、Ruff、编译、页面Biome及390/1440截图检查通过。无数据库迁移和业务写入，未推送或部署。
- 聚焦提交：`builds: add unfinished draft list and resume entry`；完整口径与验证见[未完成搭建验收](validation/2026-09-13-unfinished-builds.md)。

## 2026-09-13 草稿箱按钮交互

- 按用户纠正，进入广告搭建直接展示新建表单，右上角“草稿箱”按钮展开侧边列表；删除“新建搭建/未完成搭建”标签及view参数。
- 查看和关闭草稿箱保留当前输入，列表仅在打开时查询；选择原草稿/预览时由原离页守卫处理未保存修改。关闭返回按钮焦点，租户/BC切换重置开关和列表。
- 页面回归84项、TypeScript/Vite、Biome和390/1440截图验证通过。仅前端交互调整，无平台写入，未推送或部署。
- 聚焦提交：`builds: open draft box from the new build form`；详见[草稿箱验收](validation/2026-09-13-unfinished-builds.md)。


## 2026-09-13 其他版权方与手动推广链接

- 按用户确认保留三步搭建：第一步增加其他版权方名称及「已有推广链接？批量添加」弹窗，仍用剧名文本框和数量摘要；第二步在原剧目表逐行补充/修改链接并继续准备。没有自动/手动模式选择、重复剧目表或版权方管理前置步骤。
- 本地版权方采用租户内 other/local 范围，无凭据或远端授权；手动链接保留原文与来源，缺外部ID生成本地编号。已知版权方未填写链接的行沿原任务自动获取，补链不重发其他未知任务或被旧任务覆盖。网眼需真实归因名称；Mini、账户、BC、素材与提交边界保持。
- 新迁移 `manual_promotion_links` 保留旧数据与冻结预览；手动数据存在时拒绝破坏性降级。行编辑复用版本和幂等回执，支持响应丢失恢复；并发测试覆盖同外部ID自动发现竞争和逆序多剧准备。
- 验证：相关后端482项、最后补充回归32项（包含新增20项）、分批任务补验8项、完整离线手动创建链1项通过；前端90项和1440/390截图通过。17文件mypy/ty、23文件Ruff/格式/编译、11文件Biome及前端构建通过。旧历史/离线验收夹具按新列与现行接口合同同步，不修改真实配置。
- 聚焦提交：`builds: support other providers and manual promotion links`；仅本地实现与合成验证，未推送、部署或操作真实广告。验收和边界见[手动推广链接验收](validation/2026-09-13-manual-promotion-links.md)。


## 2026-09-13 最新迭代推送与测试部署

- 用户明确要求推送并部署新加坡测试服务器。`7b2ee95` 已推送既有功能分支并发布，包含草稿箱、统一剧目确认和其他版权方/手动推广链接；当前分支无 main，不自动创建或合并分支。
- 本轮复核：前端构建、页面90项、后端专项31项通过；后端包括实际PG迁移、并发及离线完整创建链。
- 完整备份 `/var/backups/tt-ada-staging/20260913T094820Z/`，项目/配置/数据库/Redis校验和、隔离恢复、归档解密及恢复库迁移演练通过；业务 head 为 `manual_promotion_links`。
- API/全部Worker/Beat同版本、同配置，导入/清理保持true；逐进程Settings与调用额度检查、线上双角色登录/隔离、入口检查及Worker ping通过，备份/续期timer正常。本次未新建真实广告。
- 完整版本、备份与验收边界见[测试迭代发布](validation/2026-09-13-staging-iteration-release.md)。

## 2026-09-14 版权方入口、素材调整与小程序选择优化

- 「其他版权方」从选择弹窗内移到第一页常驻按钮，选中后填写名称；保留原三步搭建与链接录入流程。
- 素材侧栏打开即自动加载完整分组并进入编辑，移除二次确认点击；加宽侧栏、文件名换行、组号与短「移除」按钮分开排列。保留未保存修改，后台准备结束后可继续保存，完整读取前禁止覆盖提交。
- 小程序选择与场景校验排队改为同一事务，保留已解析账户/剧目/素材分组，取消前端再次全量准备；页面保留已确认名称，仍校验各目标账户与冻结执行通道。
- 验证：后端109项、页面117项通过；最后保存取消信号修复后准备页53项再次通过。TypeScript/Vite、Biome、mypy、Ruff/格式/编译和1440/390截图检查通过。尚未测量服务器实际耗时。
- 聚焦提交：`builds: simplify provider and material editing and mini selection`；无迁移/新开关，未推送或部署。详见[交互优化验收](validation/2026-09-14-build-interaction-refinement.md)。

## 2026-09-14 版权方选择入口纠正

- 按用户纠正，版权方统一在选择弹窗操作，移除表单常驻「其他版权方」按钮；弹窗顶部使用整行选项和用途说明，使入口不受搜索或分页隐藏。选择后原选择框回显，名称输入沿用现有流程。
- 准备页与版权方页面75项回归、TypeScript/Vite构建、3文件Biome及1440/390px截图检查通过。未修改后端、推送或部署。
- 聚焦提交：`builds: keep other provider selection inside dialog`；以本条替代上轮常驻按钮决定，详见[交互优化验收补充](validation/2026-09-14-build-interaction-refinement.md)。

## 2026-09-14 版权方弹窗视觉统一

- 按用户确认统一已有版权方和其他版权方的整行选项样式，当前选择显示勾选；其他版权方置于弹窗底部分隔线下，列表独立滚动，取消顶部灰底突出设计。
- 准备页与版权方页面75项分批通过，构建、3文件Biome与1440/390截图检查通过；390×640下50条候选滚动到底，其他版权方仍固定可见。详见[最终视觉验收](validation/2026-09-14-build-interaction-refinement.md)。
- 聚焦提交：`builds: unify provider options with fixed alternative footer`；未推送或部署。

## 2026-09-14 数字剧目 ID 与自动链接修改修复

- 网眼长 ID 保留供接口使用，新增独立展示编号，搜索保留 int_id，旧数据根据同范围链接归因补齐。准备页表格、详情和候选显示版权方数字剧目 ID，不展示内部本地编号。
- 已自动获取的链接接入原修改功能，按钮/弹窗显示「修改推广链接」，回填原URL、编号及归因；数字编号复用原剧目，创建独立手动链接记录，保留原技术 ID 和原链接。
- 新迁移 `provider_display_drama_id`，父版本 `manual_promotion_links`；无新开关。并发自动发现与手动补链新增回归先复现错误，使用保存点撤销临时重复插入后通过。
- 后端专项283项、迁移独立1项、草稿/API/预览27项通过；末次并发修改后24项通过。页面102项分批通过，最终准备页54项通过；构建、Biome、mypy、Ruff/格式通过。完整边界见[本次验收](validation/2026-09-14-drama-id-and-link-edit.md)。
- 聚焦提交：`builds: show provider drama numbers and edit acquired links`；仅本地实现与验证，未推送、部署或改动真实广告。

## 2026-09-14 准备阶段等待原因与编辑入口

- 线上只读诊断：最新两部剧的版权方链接已 ready，数字编号正确；账户输入仍 pending，草稿尚未形成 DraftDrama。冻结连接对应 BC 的目录同步停在 ADMISSION_WAIT / mcp_refresh_pending，不能把当前状态解释为素材匹配故障。未触发解析、取链、授权刷新或广告创建。
- 原 UI 对无 DraftDrama 一律显示「等待匹配」，PREPARING 隐藏链接编辑。改为摘要返回当前版本 preparation_phase，页面显示账户/链接/素材及小程序阶段；无素材结果时保留禁用的调整入口并说明等待原因。
- 补充和修改继续共用 ManualLinkSheet 与原 manual-link 接口。准备中可查看预填内容及编辑，保存暂禁用，完成后自动解锁且保留输入；后台仍禁止并发覆盖准备结果，未改变权限及写入边界。
- 验证：实际 PostgreSQL 输入进度4项、准备页55项分批通过（首次54通过，1项因两个「等待解析」定位冲突；素材未开始改为「尚未开始」后单项复测通过）；前端构建、6文件Biome、Ruff及2文件mypy通过。
- 仅本地修正，未推送/部署。线上刷新阻塞仍待后台日志诊断：Browser连接组件加载失败，SSH密钥登录被服务器拒绝，已向用户请求服务器凭据在本机的保存路径或连接别名。不能把本次交互修正报告为线上任务恢复。

## 2026-09-14 MCP 续期被过期授权阻断修复

- 用户授权连接新加坡测试服务器排查并修复。实际数据库保留9月12日12:04 UTC创建、12:14过期的PENDING授权，未兑换、无候选。刷新 `_current` 未判断PENDING有效期，9月14日852次刷新均在发送前变为SUPERSEDED；三个草稿停在accounts阶段。
- 仅有效期内PENDING授权阻止续期；CLAIMED、CANDIDATE_READY、RESULT_UNKNOWN仍阻止，保留原授权版本、历史记录与未知结果保护。无需删除记录、重绑BC或重建草稿。
- 真实PostgreSQL/Redis回归先复现1失败4通过，修复后刷新/并发/授权68项通过；Ruff、mypy及前端构建通过。此前准备阶段与编辑入口修正一并纳入本次测试发布。
- 正在按完整停写、排空、备份和隔离恢复流程发布；真实恢复结果记录于后续验收，不将本地回归视为线上已恢复。

### 实际回执的第二处校验缺陷

- 首次修复发布 `e57dd6c` 后实际兑换成功，仅发送一次；返回字段包含 `refresh_token_expires_in`，被原严格白名单误判，连接进入REAUTH_REQUIRED。回执已完整加密保存，客户端/issuer/resource/scope/claim全部一致。
- 新增刷新令牌寿命的正整数校验；保留未知主体/权限字段的拒绝规则。管理员可复核已保存候选，复用同一完整发布校验，生成带前序关系的新授权事实与审计记录；保留已递增授权版本和历史时间，不回退版本、不恢复旧冻结任务、不重放HTTP。原草稿通过正常prepare命令重新核对。
- 刷新有效期用例先复现2项失败；字段、授权、并发及管理员回执恢复80项通过。恢复覆盖缩权、回执过期、停用、版本变化、非管理员拒绝与重复操作幂等。

### 新加坡最终恢复验收

- 最终版本 `b618aa2` 已部署。两轮完整备份及PG/Redis/项目/配置隔离恢复通过；最后备份为 `/var/backups/tt-ada-staging/20260914T090348Z/`。API/全部Worker/Beat同版本同配置，开关及私有env未变，健康与timer正常。
- 管理员复核回执成功，新凭据生效，真实刷新总计只发送1次；授权版本保持2，未回退。原三个草稿均通过正常prepare恢复到READY；当前两剧31091/31092分别匹配23/22条素材，2个账户通过，链接ready。
- 本轮无广告提交。浏览器插件不可用，真实验收通过认证API/数据库，页面交互由本地回归覆盖。完整证据与运维脚本PYTHONPATH注意事项见[恢复验收](validation/2026-09-14-mcp-refresh-recovery.md)。未推送远端。

## 2026-09-15 任务详情与历史配置展示优化

- 用户确认上一轮页面评估方案。合并任务摘要和重复状态；创建结果改为三项数量摘要，异常显式提示；完整统计、执行连接与编号按需展开。正常任务移除账户授权快捷入口及无恢复候选空提示，未知请求与恢复回执不隐藏。
- 主表集中展示剧目/账户、素材与创建结果，完整广告系列及平台状态移到行详情；操作记录增加业务定位和中文事件说明，技术原码保留在折叠详情。
- 预览摘要返回同租户/BC 的持久化 `submission_id`，已提交配置跨会话只读，移除创建步骤、创建/调整/修正入口，提供返回任务详情。无迁移或新开关，需前后端一起发布。
- 页面回归84项通过；独立 PostgreSQL 预览/任务/查询43项通过，补充字段断言后查询12项复测通过；构建、Biome、Ruff、mypy和1440/390px截图检查通过。详见[本轮验收](validation/2026-09-15-task-detail-refinement.md)。
- 聚焦提交：`1b89b13 builds: simplify task details and make submitted previews read-only`。已随 `cc66201` 推送并发布新加坡测试环境。

## 2026-09-15 搭建批处理与已核实补建闭环

- 用户批准提速方案并明确共享 20 素材 × 10 目标上限：相同冻结范围的既有待执行分发立即拼合法矩形，不新增授权组合、不等凑满；批次保留各原操作及物理请求摘要/回执。超限拆批、不同满批可并行。
- 批量视频/图片读取 1–50 个唯一 ID；已知目标 VID、封面核验按账户合并，900 秒素材有效期不放宽，完整成功回执与已有新鲜映射仍走短路径。
- 原 UNKNOWN 不覆盖，通过内部真实只读验证服务追加独立补建关系，任务/列表/组/广告/恢复入口一致派生完成。已核实的旧对象不再触发重建或恢复。补建服务对超过五秒的真实 HTTP 等待另做回归，保留整体 45 秒与 SQL/锁五秒上限。
- 新迁移 `build_batching` 包含四张新表、不可变回执/补建守卫及批量封面索引；从旧 head 实际 PG 升级通过。资源(prefork 2)、广告(prefork 1)、control(prefork 1)独立模板；根 jobs 69通过/1 Linux项目待验，补建与准入/迁移合跑30通过；前端构建及53页面回归通过。
- 独立审查已关闭并发锁序、回读后身份变化、批次进程中断与单坏成员拖累同伴等边界；批次31、MID发现24、原分发48、最终封面批核验31、读取适配器104/1跳过、原件保护34项通过（分组不重复累加）。100/1000/10000容量仅为本地PG候选分组/SDK HTTP边界测量，不当作平台或真实广告吞吐。
- 实际只读验证三个源 MID 可在目标账户批量找到实际 VID；新共享后按目标账户/MID 合并查询，完整信息20×10共12次业务请求，缺尺寸仍须补严格详情。批次短claim75秒、发送前过期拦截，已发送UNKNOWN不得重放。
- 测试服已发布 `c79465cbce3d16f0a2831d6ab1c1d4f3bf580cae`，数据库head为`build_batching`。最终备份 `/var/backups/tt-ada-staging/20260915T053221Z/` 完成PG/Redis/源码前端/配置恢复及24份加密响应验证；五服务九进程同版本同配置、实际前端167文件匹配、三个Celery节点pong、无异常重启，开关和配额未变。真实只读核实并关联此前两条补建，原六条仍MATCH/ENABLE，原两UNKNOWN历史不改；浏览器任务页8成功0待核实、含2条补建且无待恢复步骤。没有新增真实广告，未推送。完整证据见[本轮验收](validation/2026-09-15-build-batching.md)。
- 首次Linux满批门禁拦截`5ec1c69`，未升级在线库/切版，恢复旧站点。实机证明每callback885次重复SELECT耗尽40秒预算；事务内source合法映射批读及完整route/BC/account/capability去重降至252次，保留逐成员fence/新鲜度且不跨请求缓存，所有生产时间上限不变。46项批次回归及原分发/MID72项通过，共118个不同用例；根官方传输/素材适配器123通过1跳过。第二轮先在旧服务在线时通过Linux满批/并发4项及MID24项，再停写完整备份恢复，通过Linux队列2项后才升级切换。

## 2026-09-15 投放策略名称可修改

- 已有策略名称改为可编辑的显示标签；名称单独保存走租户权限保护的本地 PATCH，去除首尾空白并记录改名前后审计，不创建新的配置版本。
- 预算、ROAS、素材分组、文案及命名配置仍按不可变版本保存；已提交任务继续引用原 `strategy_version_id`。按用户确认，历史草稿和任务目录实时显示修改后的策略名称。
- 前端仅在名称单独变化时显示「保存名称」，配置变化仍显示「保存为新版本」并沿用原校验、冲突与未知结果处理。同步重新生成 OpenAPI 客户端类型；无数据库迁移、新开关或 TikTok 请求。
- 验证：策略后端 69 项、策略页面 40 项通过；后端 Ruff/格式/ty 与前端 TypeScript/Vite 构建通过。
- `cc662011f0429b65949224f883497f0f830172d2` 已随其余本地提交推送并发布新加坡测试环境；完整备份与隔离恢复、登录权限、三队列 ping、五服务同版本和配置不变均通过，详见[发布记录](validation/2026-09-15-staging-strategy-task-ui-release.md)。

## 2026-09-15 广告命名改用版权方数字剧目 ID

- 新加坡测试环境只读核实发现，网眼剧目目录的展示编号为 `31091`、`31092`，但最近 Campaign 名仍拼入 `6a98…` 长技术 ID；取链接口身份与广告命名身份发生混用。未调用 TikTok 或版权方接口，未修改测试环境数据、配置或服务。
- 新预览分别冻结 `external_drama_id` 与 `display_drama_id`：前者继续用于版权方接口，名称模板 `{drama_id}` 只使用后者，效果为类似 `…-32827-…`。缺少可靠展示编号时明确失败，不回退长 ID。
- 新迁移 `preview_display_drama_id` 仅增加冻结字段，不回填或改写历史预览；已生成广告继续读取原冻结名称，带旧快照的未完成预览须重建，避免一批任务混用两套命名规则。
- 独立 PostgreSQL/Redis 下相关预览、链接、迁移、命名策略及版权方回归 163 项通过；Alembic 升级到 head 与 `alembic check` 通过；11 文件 Ruff/格式及3个实现文件 mypy通过。
- `4230f8f` 已推送并发布新加坡测试环境；服务器 Linux 满批/并发4项、MID专项24项和恢复库队列隔离2项通过。完整备份 `/var/backups/tt-ada-staging/20260915T125915Z/` 的五份归档校验、PostgreSQL/Redis/项目/配置隔离恢复及24份加密响应验证通过；线上 head 为 `preview_display_drama_id`，五服务同版本、零重启、开关及调用租约不变。真实目录样例生成 `…-31096-TEST` 且不含长技术 ID；历史广告名称未改写。详见[发布验收](validation/2026-09-15-staging-numeric-drama-id-naming.md)。

## 2026-09-15 分页列表显示数据总数

- 所有通用游标分页响应增加当前权限及筛选范围内、应用游标前的精确 `total`；租户、账户、素材、版权方、策略、草稿、预览和任务目录统一覆盖。
- 公共分页器及 MCP BC、小程序、素材编辑、待上传文件、平台用户等自定义分页显示“共 N 条”；加载时保留上一份已确认总数，不使用当前页长度冒充全量。
- 分页摘要进一步收口为全局通用组件，统一显示“共 N 条 · 第 X / Y 页”；公共游标分页、通用表格及原有本地分页不再各自拼接页数文案，0 条按 1 / 1 页展示。
- 无迁移、新开关或外部平台调用。后端定向 PostgreSQL/Redis 回归119项、原总数浏览器回归230项、总页数组件相关浏览器回归211项、Ruff/编译和前端生产构建通过。
- `9744ccb` 已推送规范集成分支并发布新加坡测试服；完整备份及隔离恢复、9 进程同版本/同配置、三 Worker ping、双管理员登录、前端字节和线上精确 `total` 回读均通过，详见[验收记录](validation/2026-09-15-pagination-total-counts.md)。

## 2026-09-16 主分支与历史分支整理

- 按用户要求将现有集成分支原位更名为 `main`，作为唯一长期维护和后续服务器发布主分支；业务基线仍为 `4f57140`，未重新合入旧实现。
- 核对祖先关系、补丁等价性和后续整合记录后，清理其他 66 个本地分支；完整 Git bundle 和 3 份未提交差异已私有归档并验证。旧工作目录改为 detached HEAD，原提交、未提交修改与环境文件全部保留。
- 发布手册已统一 `main` + 固定 SHA 规则。远端推送、默认分支切换和旧分支删除等待明确授权；未发布服务器。详见[整理记录](validation/2026-09-16-branch-cleanup.md)。

## 2026-09-16 BC 使用授权统一入口

- 按用户批准方案，在“账户与授权 → 广告账户”顶部统一展示及设置当前 BC 的使用授权，复用现有管理员权限；不新增独立页面或成员权限模型。
- 移除账户查询与广告搭建的临时授权下拉框。账户查询读取 BC 配置，新建/编辑保存的草稿提交空授权偏好，由既有后端在准备时冻结；已有准备、预览与任务的冻结授权不变。素材新上传沿用既有 BC 配置机制。
- 授权管理保留接入、同步、重新授权和解绑，详情表移除重复的默认授权设置按钮。关联 BC 详情桌面最大宽度增至 1024px，900/1440px 的同步按钮无须横向滚动，390px 增加滑动提示并防止编号被纵向挤压。
- 验证：最终独立运行账户/授权、搭建通道、搭建准备及素材页面 195 项全部通过；轮询专项连续三次通过，布局专项7项通过，TypeScript/Vite 生产构建、修改文件 Biome 和 `git diff --check` 通过。截图已目视检查，均为 HTTP 边界合成数据。
- 聚焦提交主题：`accounts: centralize BC authorization settings and widen details`。仅本地修改与提交，未推送、未部署；无数据库迁移、新开关或真实平台写入。详细约定见[本轮验收](validation/2026-09-16-bc-authorization-ui.md)。

## 2026-09-16 租户统一素材库与 BC 集中转存

- 按用户要求先使用 Superpowers 完成[设计文档](superpowers/specs/2026-09-16-tenant-material-library-design.md)与[六阶段实施计划](superpowers/plans/2026-09-16-tenant-material-library.md)，先行提交 `76ca073`，再实施并完成独立复审。
- 素材目录、详情、历史记录及自动/手动选材改为租户范围；顶栏 BC 决定新上传与搭建目标。进行中、历史和刷新恢复的上传固定原 BC，切换不重定向。可信内容重复上传在分页前去重，历史文件名与冻结 ID 保留。
- 搭建提交时先复用目标或 B 内来源；否则按租户/内容/B 的唯一持久 seed 向 B 主素材账户转存一次，再原生共享给各目标。并发、多别名及后续新账户复用同一来源；UNKNOWN 不换账户、不重发。封面保留实际 SOURCE 身份并按真实目标回读，同内容别名和主账户自身目标均覆盖。
- 新迁移 `tenant_material_library → material_bc_seed → material_target_scope` 接原 `material_share_receipts`：消费引用按租户，原上传约束不变，实际目标 BC 和操作唯一性继续受约束；旧记录升级保留，不兼容降级拒绝。无新开关。
- 最终根回归46、目录81、网关22、分发广泛191/最终28、封面专项20/原回归176分组通过；API/MCP×同/跨 BC四条完整链路通过。页面四组145通过，最后上传恢复修复相关5通过；Ruff44文件、mypy26模块、前端构建、Biome与 Alembic check 通过。不同组存在重叠，不能相加作为全仓用例总数。
- 独立复审发现的别名 SOURCE 封面与部分勾选后全选问题已补回归关闭；最终没有可确定的未修 P1/P2。验证使用真实本地 PostgreSQL/Redis和HTTP传输替身，详见[本轮验收](validation/2026-09-16-tenant-material-library.md)。
- 实现聚焦提交主题：`materials: unify tenant library and seed each BC once`。仅本地代码、迁移演练和提交，未推送、未发布、未创建真实广告；服务器界面仍需后续授权发布才能生效。
## 2026-09-17：整批恢复继续（218c64f 已发布，业务仍在恢复）

- 12:13 UTC 发布 `218c64f`，完整备份 `20260917T121241Z`、1,246 文件/21 表/948 归档独立恢复、五服务九进程/三消费者/HTTPS/登录隔离及 Linux 3 项通过。2 万条深积压增量 20 项通过。12:16 MATERIAL 成功 61、分发 READY 1,148，但广告仍仅此前 2 条；继续恢复源封面和原批次，未将服务验收当成整批完成。详见[本轮持续验收](validation/2026-09-17-material-queue-recovery.md)。

- 11:50 UTC 原批次仍只有一组广告闭环；后台大量消费旧核验代数，准备任务每次占用约 10～18 秒，结果与封面长期排队。按当前原消息完整身份临时提优先级、短时暂缓准备队列领取，未清消息、未增加并发，结束后恢复两个原消费队列。
- 新发现快照 1 秒预算只索引到约 4,588 个结果消息，而实际有约 20,158 个；尾部抽样 1,000 个中 658 个未索引。原逻辑只补查队首 16 条，修复为 Redis 内按任务 ID 有界筛选候选，再验证完整身份与实时 LPOS。真实 Redis 先复现补投新增重复，修复后深积压去重与实际丢失补投均通过。
- 单项原生共享没有 `share_batch_id`，原批量核实入口将其全部排除。保留原目标冻结边界、20 个 MID 上限和逐成员内容核对，允许两个及以上单项共享结果合并读取；单一非批量成员仍走原路径。API/MCP 两个回归先失败，修复后素材相关 36 项通过。
- jobs 回归 89 passed / 1 Linux skip，Ruff/ty 通过；2 万消息深积压增量和服务器发布验收继续记录。没有迁移、新开关、上游额度或广告内容变更。13 项 URL 转存无回执，按冻结名称真实查询均未找到，已请求用户确认二次核查后的有限补传，尚未补传或改写 UNKNOWN。
# 2026-09-18 有界批处理流水线（已部署、业务验收继续）

- SOURCE未知封面分页修复：同代数以唯一图片ID计数，100/71/53最多三轮补齐；保持总数变化/歧义/不可见上限阻断，不重传。新增两项先失败，最终来源封面、原封面、共享吞吐、租户库105项通过169.64秒，Ruff/格式/ty app通过，待发布。
- `ff5c158146826c01737b3e23986434a76c273fb4` 已部署：191610Z完整备份/1260项目文件/21表/1023响应恢复一致；六服务十进程/双身份隔离及服务器首次共享4项32.07秒通过，测试角色恢复。正式RETRY `308fd147-c7c5-51ee-aabf-7ad9b713c83b` COMPLETED/3；19:23UTC仍2/180，但MATERIAL418、BUILD封面711 READY，继续业务验收，不能当成创建成功。
- 首次IMAGE共享改为本批源MID的完整定向查询，匹配者复用，缺项只发送原未armed账本一次；已发送/已复用/来源自身仍只接受正证据，空或不完整查询保留完整核查。新首次/部分复用两项先失败；新增过期复用无POST账本用例又发现续跑误回PENDING，修复按job证据选择只读队列并在新核查开始后更新批次状态、防止反复清空游标。最终封面/租户库/分页/首次共享84项通过158.92秒，Ruff/格式/ty app通过；双通道完整执行及原封面35项通过，新失败已由最终回归覆盖关闭。待正式发布。
- 19:08UTC原批次仍2/180，MATERIAL329、BUILD封面621 READY，业务未完成。为下一次完整备份已核验184707Z所有归档/manifest/副本字节与无活动会话，仅清理aa016恢复测试库及YXHFLE13解压副本，原业务/备份/release保留，可恢复；余1572368KiB。
- `aa01610f1de3f79343d8f78c9058a0706ca40076` 已推送部署：备份184707Z/五归档/1259文件/21表/1007响应恢复一致，六服务十进程/双身份隔离及服务器新5项21.67秒通过；本地断点10项复跑22.29秒通过。原配置/额度/head/前端不变。正式RETRY `82e641e8-d0ca-5281-a6a7-0a865cef1ec2` COMPLETED/13；18:51UTC仍2/180、MATERIAL249、BUILD封面522 READY。
- 实际活动2成员批次的只读_check耗时0.132秒、62查询累计SQL0.056秒，非当前主要瓶颈。另2个SOURCE UNKNOWN仍走旧单封面扫描，分别在531总数的第4页和118总数的第2页报cover_search_incomplete；均armed、无回执/候选/MID，各引用原批次10个步骤，不能重传。此分支尚未套用共享批次的换边界补齐。
- 同账户连续分页每任务至多4页、剩余预算低于20秒让出、发送前低于15秒续跑；保持跨任务唯一库存核对，图片传输不再提前拒绝可补齐的跨页重叠，视频合同不变。新增连续补齐/页数上限先失败后通过，连同预算、原封面、租户库和双通道适配器166 passed/1 skipped（SDK不适用MCP文本载体）/222.61秒；Ruff/格式/ty app通过。分页断点用例显式按1页运行，新独立用例验证默认4页全链路；仍待正式发布和180组合业务验收。
- 原MID版本RECONCILE `bcce85cd-543d-5ebd-a5d0-3c389a420faa` 实际COMPLETED/15，RETRY返回409无候选。18:43UTC仍2/180、MATERIAL240；当前窗口277项中46成功，其余视频均ready、等待封面。服务器2GiB内存已有990MiB swap，未通过增加Worker并发掩盖逐页开销。
- 为下一完整备份核验183303Z/080601Z全归档和manifest后清理b9d/c85两个恢复测试库及0Q0bAN3A临时副本，备份和业务/release保留可恢复；可用空间1633420KiB。
- `b9d6982f08cc6be6da90fd6d4e9a5fca8375c1b3` 已推送部署，完整备份183303Z、21表/998响应/1259文件恢复一致；六服务十进程/双身份隔离及服务器5项23.34秒通过，测试角色恢复。RETRY无候选，RECONCILE已受理，未伪称创建成功。18:35UTC仍2/180、MATERIAL239、BUILD封面509 READY。真实只读连接计时：建立0.978秒、首次读2.288秒、同连接二次读0.654秒，证实逐页重建会话存在额外开销。
- 共享后/源账户自身目标新增按原MID的正向回读，真实已共享批次4/4内容匹配；空或部分结果继续完整库存核查，不据此重发。原严格匹配方法共用，库存变化不重复探测。新增4项先失败后通过，封面吞吐与租户库完整75项通过144.71秒，Ruff/格式/ty app通过。18:28UTC仍2/180、MATERIAL234、BUILD封面501 READY，不将代码测试视为业务完成；正在发布此提速修复。
- 发布空间清理仅涉及181213Z的临时恢复库及bD3Rf4zD解压副本、091246Z/085826Z的历史恢复测试库；所有归档SHA、manifest、无活动会话及副本字节核验后删除，业务库/备份/release保留，临时副本可重建。
- `c115492f5a754bd32aab5cfe4e6bff7bce220c6b` 已推送部署：备份 `20260917T181213Z` 五归档/1259文件/21表/986响应独立恢复一致；六服务十进程、双身份隔离及服务器8项新回归59.30秒通过，测试角色已恢复。开关/额度/head/前端不变。产品RETRY `6a4fe67c-4cd4-5fba-b80c-f66778c21892` COMPLETED/1；RECONCILE `72abac7b-1912-55fb-b2f2-508a113c3521` COMPLETED/15。18:16UTC整批仍2/180，封面READY496→499、准备队列86，继续业务验收。
- 未知13项目标库存只读10页（声明总数962；第6页首次准入退避后从6继续）均未找到对应MD5；未将此视为可自动重传证明，未修改业务映射。已异步询问是否允许严格核查后的限定补传，尚未得到明确授权。另已成功共享批次只读按4个原MID筛选，目标返回4条，MID、签名及尺寸4/4匹配，可用于后续正向核查优化，但不以筛选空结果断定不存在其它MID内容。
- 根因新增证据：真实图片库存156条，固定100条分页产生3条跨页重叠，唯一153；只读补查第2页/71条取得3条缺项，唯一156完整。不是同image_id对应不同MID；没有放宽唯一计数。正式扫描最多三轮，补查轮用71/53条移动边界，达到同总数完整唯一库存才允许未发送共享，已发送保持只读。SDK/MCP可选页大小严格回读验证，新8项先失败后通过；整体回归进行中。
- 分页修复整组180 passed/1 skipped（SDK不适用MCP文本载体用例）/235.25秒，包含共享完整执行、原封面、SDK/MCP读取和恢复边界；纯图片合同24项通过0.08秒，Ruff/格式/ty app通过。下一发布准备中，不能据此宣称原180组合完成。清理175307Z对应临时恢复库/副本与050329Z对应历史恢复测试库前均核验全归档，业务库/备份/release保留。
- 失败步骤/PENDING封面衔接回归先失败；仅显式RETRY且全部原未发送栅栏成立时恢复步骤，封面不重复投递。与原恢复/容量/封面共76项通过93.26秒；armed、claimed、已dispatch仍拒绝。
- 历史批次准入修复 `59686b7c8d6529d71beb072245441460a5860635` 已推送并部署。备份 `20260917T175307Z` 五归档/1259文件/21表/973加密响应独立恢复一致；六服务十进程同版本同配置、四Worker ping、前端字节及双身份隔离通过。服务器关键5项通过20.65秒（首次测试命令未注入应用环境而启动失败，角色已恢复；补齐实际环境后重新通过）。配置/额度/head不变。产品RETRY返回409 recovery_no_candidates，未伪称调度成功。17:55UTC完整仍2/180，准备队列781→578，持续观察。
- 发版容量门禁曾在停机前退出；校验 `20260917T050329Z` 全归档和解压字节后，仅清理 `EE7amXzz` 历史恢复演练副本，备份/数据库/release均保留。
- `7b0234010ab9eb81097ac02fad7961f894b4ef8f` 已部署：备份 `20260917T172954Z` 五归档/1258文件/21表/960响应独立恢复一致；六服务十进程、四Worker、双身份登录隔离通过，原配置/额度/head/前端不变。实际安装候选SQL执行16.664ms、无JIT；服务器两项回归通过10.15秒。产品RETRY完成9项、RECONCILE完成15项，不等于创建完成。17:45 UTC仍2/180、MATERIAL228成功。
- 新根因：领取准入原先排除所有已分批任务，1261个未发送历史批次成员绕过窗口，持续占用准备槽。领取与修复统一按整个批次检查：任一未完成成员在当前窗口或不绑定搭建即可接续；全未来批次持久等待；已armed批次仍允许核查。不拆改成员及原发送账本。新增future/mixed/armed回归，包括wake本身在未来而另一个成员进入窗口后能自动恢复。
- 本轮执行/万组合容量/封面吞吐及封面创建95项通过249.60秒；Ruff、格式、ty app和差异检查通过。来源查询计数识别补充SOURCE参数，20次上限不变。历史批次准入修复待正式发布；服务器只读SQL19.036ms、无JIT，不把该测量当作实际创建吞吐。
- 上一轮恢复验证临时库 `tt_ada_tenant_library_7b023401_test` 及 `cSq2Lulm`、`3WugkthC` 两份临时解压副本经归档校验后已清理，独立备份及运行release保留，可重建。目标未知操作关联的13份上传归档实际属于来源BC且operation_id不同，不能当目标URL转存成功证据；不重复上传。
- `23e87209cb357d64b41ad000f3b2ab7a5f81baba` 已部署，完整备份 `20260917T171644Z` 五归档/1258文件/21表/954响应独立恢复；六服务十进程实际配置/版本通过，服务器新恢复六项通过/40.49秒。原产品RETRY `2aec1327-1a6d-5f04-bbb5-17c5956a5f9b` COMPLETED/416，仍非广告完成。
- 新根因：完整封面候选SELECT三次内联窗口准入，17时段12次SQL超时。实际EXPLAIN旧8.848秒/JIT8.620秒；物化候选并以排除NULL的tenant/job集合反连接替代相关EXISTS，新执行32.189ms，无JIT，不改配置/超时。单快照完整55行/顺序完全一致，7.22957秒→0.02815秒。此前“窗口本身4ms无JIT”的排除只针对单查询，不适用于完整候选链。
- 本地容量用例增加10000封面与AccountMaterial，完整候选仍10项；容量+完整跨窗口执行15项通过/103.90秒。其它封面回归进行中，查询优化待发布。
- 封面吞吐/租户素材库/创建依赖/冻结恢复98项通过177.56秒；Ruff、格式、ty app通过。17:27UTC原批次仍2/180、MAT225、来源封面881 READY、结果队列9、准备队列1286；不能称已恢复完成。已核实171644Z完整归档后删除本轮恢复测试库和37kcYnLy临时解压副本，备份保留可恢复。
- 新遗漏：PENDING持久等待引用确定未发送的BLOCKED封面，原RETRY只选FAILED，须逐项失败后才可恢复。新SQL在三种明确素材等待码且满足完整COVER_RETRY条件时纳入批次重试；未知视频/armed封面/有回执/活动投递不放行。回归先失败recovery_no_candidates，修复后恢复/权限/原候选容量51项通过（70.43秒），Ruff/格式/ty app通过，待下一发布。
- 84d服务器整组回归360秒超时（未出现断言失败，不算整组通过）；随后单独运行真实Linux prefork和任务交接6项全部通过（19.05秒），测试角色恢复NOLOGIN NOCREATEDB并清理私有bootstrap测试库。万组合与双通道完整通过证据仍为本地，不冒充服务器整组通过。
- 已推送并部署 `84d578e6daeb8d4688911d6d455dbfc1a27f9c6b`。备份 `20260917T170246Z` 五归档、1252项目文件、21表、951加密归档恢复通过；六服务十进程同版本/私有配置、四Worker ping、前端字节与双身份隔离通过。开关/额度/数据库head不变，Linux隔离测试进行中。
- 原产品请求 RETRY `ff62961c-9681-54f9-8a6e-50da010ce392` 实际进度COMPLETED/1；RECONCILE `d1235997-0063-57cd-a34e-751714f2b3dd` COMPLETED/13，仅说明调度。17:06UTC原180仍完成2，MAT169，来源封面822 READY，结果队列1391；10组合已进入窗口。继续处理，不停止目标。

- 根因：准备/结果仅分队列却共用两个执行槽，慢封面仍可占满核验；旧单组合窗口限制吞吐；封面规划与领取准入不一致导致 `cover_claim_lost`。统一准入谓词，按剧优先每提交10个活跃组合，准备/结果各保留1槽，外部额度与冻结意图保持。
- 真实PG/Redis：12组合跨两轮完整创建/回读，无重复写；保留真实限流退避。10000组合容量回归窗口查询0.0147秒，仅投递10条，二次唤醒0条；这不是10000条真实平台创建吞吐。准备积压10000消息时核验3秒内完成（本地threads）；Linux prefork待服务器执行。
- 整体回归131通过/2跳过，两个旧队列迁移断言按新职责调整后，路由故障/消息身份及队列测试25项通过。发布与原180组合真实验收仍未完成，不以测试替代业务。
- 双通道全链路4项通过（77秒）：旧测试遗漏持久来源等待的Beat修复环节，曾以空dispatch_id直接运行目标而失败；测试现在推进时钟后调用正式repair_cover_dispatches，不伪造目标结果或改生产等待逻辑。
- 恢复/未知回读/确定未发送重试61项通过（125.61秒）；Ruff、格式和ty app通过。准备执行完整备份发布。
- 发布空间：核实六份备份所有归档SHA和解压文件后，删除对应临时恢复副本及无活动会话的6fd恢复测试库；所有备份、业务数据和release保留，可重建临时副本。剩余约1.57GB（十进制KiB指标1568876）。
