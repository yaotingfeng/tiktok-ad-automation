# 真实联调记录

2026-09-09：当前没有实际部署主机、域名、开发者 App 和租户业务授权。本轮使用本地数据库、Redis 与外部边界替身开发；以下均未执行。

| 验收 | 状态 | 所需输入 / 验收结果 |
| --- | --- | --- |
| 公网 HTTPS 登录与回调 | 未执行 | 实际主机、DNS、证书；验证 `/api/integrations/tiktok/callback` |
| TikTok OAuth | 未执行 | App ID/secret、门户授权 URL、匹配的回调地址；租户连接成功 |
| 全 BC 账户发现与权限 | 未执行 | 租户授权；完整分页、当前角色和 scope 证据 |
| Minis / Identity / CTA / 地区 | 未执行 | 租户版权方应用与可用广告账户；真实字段与权限匹配 |
| 嘉书链接准备与复用 | 未执行 | 该租户版权方凭据；历史完整核对与配置一致性 |
| 网眼链接准备与复用 | 未执行 | 该租户版权方凭据及可核实的历史查询契约 |
| S3 原件与目标账户素材 | 未执行 | 私有存储配置与 CORS、测试视频、账户权限、实际 VID/封面 |
| 广告创建并启用 | 未执行 | 用户在工具内审阅具体预览并提交；实际三层对象 ID |
| 真实广告回读 | 未执行 | 逐层核对预算、ROAS、链接、文案、目标素材、CTA 与操作状态 |

网眼已实现公开前端可核实的历史分页、创建与按 ID 精确回读协议。首次取链只有在全部历史页完整、范围一致且没有可复用链接时才创建一次；已发送请求的结果未知时，只按原 ID 或稳定名称核查，空结果和歧义均不能触发再次创建。部署前仍需用该租户授权只读验证后端确实遵守日期窗口、总数与 ID 过滤契约，详见[协议证据](../integrations/wangyan-link-protocol.md)及[持久工作流](../integrations/wangyan-workflow.md)。CTA portfolio 未获得 ID 时，官方列表不能证明 CTA 不存在，仍需保留 UNKNOWN。

配置与申请顺序：部署实际 HTTPS origin → 填写回调申请 App → 配置获批 App → 租户授权 → 完整账户发现及能力准备 → 配置该租户版权方应用 → 上传测试素材 → 生成并审阅预览 → 在工具内提交一笔具体试投 → 回读验证。软件创建时直接 ENABLE，提交预览是明确广告写意图。

真实 request ID、账户与对象 ID 留在受控的租户业务记录中；公开仓库只保存脱敏字段形状和兼容结论。离线模拟通过、创建 ENABLE、广告审核通过、发生消耗是不同结果，分别记录。
