# 自动权限与小程序名称选择：新加坡测试验收

用户确认按自动权限同步、取消版权方 Mini 强关联方案优化，并延续本会话的推送和测试部署授权。本轮运行代码为 `9c23a4c639b5a474373d0e6ba7231f404d3571fc`，已推送至 `origin/feat/platform-implementation`；数据库 head 为 `automatic_mini_targets`。站点为 `https://tk-ada.137-220-150-31.sslip.io`。

## 实际行为

- API/MCP 目录完整发布时复用已读取的授权事实和角色页，直接更新上传、搭建权限缓存；不要求人工评估或先有成功操作，也不增加一次全量角色重检。真实 Analyst、未知授权、撤权及账户状态仍受实际限制。
- 准备页按账户展示短剧小程序名称。明确链接可匹配，否则按名称选择；一次选择确认本批次全部链接，随后逐账户验证可用性。不根据唯一候选、包名或不透明短链猜 Mini。
- 对应关系保存在租户内的精确 URL 摘要记录中；旧应用手填接口和入口已移除。迁移保留已有有效链接的历史确认，不将原应用配置传播给新链接。
- 保存采用草稿版本及原请求 ID；响应丢失时查询原修改结果并继续准备。选择本身仅修改本地记录，不调用 TikTok 写接口。

## 回归与构建

真实 PostgreSQL/Redis、外部服务传输边界替身：场景、目录、权限、创建适配器等一轮 `215 passed`；补充 API 角色、Mini 选择和隔离 `25 passed`。范围包括 Admin/Operator/Analyst/缺角色、目录缓存、重复分页、明确链接/短码、错误目标、过期、草稿冲突、租户与角色边界、请求幂等、源目录复用及有效期保持。

共享工作区同时存在另一项准备进度修改，因此仅选取本轮文件及交叉文件中的本轮改动，导出独立构建副本。1025 个源码文件与暂存树逐字节一致；此副本后端关键用例 `29 passed`、浏览器 `50 passed`、前端构建通过。5 个实现文件 mypy、改动 Ruff 和差异检查通过。以上为本轮相关回归，不代表仓库全量测试。

发布包由固定提交归档加独立副本构建产物组成，服务器校验 1107 个源码/构建文件摘要。另一项尚未发布的准备进度改动保留在共享工作区及其独立提交中，未纳入本次服务器版本。

## 备份、迁移与配置

- 原运行版本 `b89ad1f3aa9cb1d94a77d942bf52ace3261baa4d`。
- 完整备份 `/var/backups/tt-ada-staging/20260913T072535Z/`，包含 PostgreSQL、Redis、独立项目归档、私有配置/证书归档；五个归档的 SHA-256 校验通过。
- 正常停写并排空服务，暂停备份 timer。PostgreSQL 恢复到独立临时库，验证素材响应可解密且长度/摘要一致；Redis 在独立 socket 实例加载并 PING；项目及私有配置解压比对通过。
- 在恢复库演练 `material_source_bc → automatic_mini_targets` 后才迁移业务库并切换版本；旧版本和备份保留，备份 timer 已恢复。新 Mini 记录存在时不能强行 downgrade。
- 本次没有新增开关。`MATERIAL_INGEST_ENABLED: true → true`、`MATERIAL_CLEANUP_ENABLED: true → true`，沿用用户原确认；API、3 个 Worker 进程、Beat 均核验为同版本、同配置。
- 调用额度、共享接口租约、MCP 注册配置及媒体主机配置均保留并通过校验。完整素材响应归档仍为 24 条；正常成功上传后仍不逐条回查。

## 真实联调

使用**服务器官方 MCP 骏伯连接** `90d01aa0-ab41-43e2-ad33-11c44b2da73a`，BC `7678608005688066065`，测试用户指定账户 `7680027514155319304`，未使用本地 TikTok MCP 连接。

1. 目录同步 `ea1644e7-98ea-4742-81eb-81262ca7dab0` 为 COMPLETE。账户直接得到 `can_upload=true`、`can_build=true`、VERIFIED；可用本地权限证据为 `4c72dbba-567d-4981-9518-e04afd567003`，没有额外发起能力重检。
2. Mini/场景读取任务 `79e9eaf7-c702-4b14-b041-e7e73b9fc4c1` 完成，实际返回 **LemonShow**、**NovaShort**；原有效链接到 LemonShow 的确认关系已保留。完整场景可用性复核为 ready。
3. HTTPS 健康为 200；线上 OpenAPI 含草稿 Mini GET/POST，已不含旧版权方应用 Mini PATCH。
4. 本轮没有新增广告；用户原广告提交 `b901ff48-24f6-478a-8bce-0909dd640e4c` 仍为 COMPLETED。既有同 BC 原生共享、跨 BC URL 转存和原文件名规则保持此前实现。

私有部署日志、响应和证据保存在服务器 `/root/mcp-upload-fix/` 与本地 `.runtime/auto-minis/`，不提交凭据、原始响应或运行配置。
