# 新加坡 MCP 凭据刷新阻塞修复

目标为新加坡测试服务器，HTTPS 入口 `https://tk-ada.137-220-150-31.sslip.io`；用户授权连接服务器排查并修复。最终运行提交 `b618aa21caec89dd6a1f24e5b45c56281fbf5af8`，数据库仍为 `provider_display_drama_id`，未更改私有配置、功能开关或广告。

## 根因和修复

1. 两天前的未兑换授权入口已过期，但 `_current` 仍将 PENDING 当作正在授权。首次诊断已有852条刷新在发出HTTP前被标记SUPERSEDED；补齐有效期判断，CLAIMED、候选与未知结果仍保持原围栏。
2. 实际刷新返回 `refresh_token_expires_in`，原白名单将寿命元数据误判为授权变化。新增正整数范围校验，其余未知主体/权限字段仍拒绝。真实回执的客户端、issuer/resource、scope和claim对应关系一致。
3. 通过管理员回执复核恢复已加密保存的候选，共用完整发布校验，不重发刷新HTTP。保留递增后的授权版本2，新增前序关系和审计；没有回退授权或绑定代数。原权限事实与观测时间不伪造，随后真实目录同步重新完成。
4. 原三个未完成草稿经正常prepare接口继续准备，新revision为2，旧任务仍保留。此前提交 `3abf1f9` 的实际阶段显示和统一链接编辑入口一并上线。

## 发布和备份

- 首次修复版本 `e57dd6c`，完整备份 `/var/backups/tt-ada-staging/20260914T085607Z/`。
- 最终修复版本 `b618aa2`，完整备份 `/var/backups/tt-ada-staging/20260914T090348Z/`。
- 两次均冻结API/Beat、正常排空Worker、暂停备份timer；包含PG、Redis RDB、独立项目与前端产物、私有配置/证书/全部相关服务配置。校验和、隔离PG恢复、独立Redis装载、项目与配置解压比对、归档响应解密校验通过。
- API、3个Worker进程（主进程及2个子进程）、Beat均核实同版本/配置。导入与清理保持true，环境文件发布前后SHA256一致，数据库head不变，健康和备份/证书timer正常。
- 运维脚本须设置 `PYTHONPATH` 为当前release的backend目录；共享虚拟环境保留旧editable安装路径，外部脚本不能依赖它定位当前源码。恢复脚本最初因未设置该值在导入阶段退出，无业务写入；修正路径后完成恢复。

## 验证结果与边界

- 过期授权用例先复现1失败4通过；寿命字段用例先复现2失败。最终刷新、授权、真实PG/Redis并发与回执恢复共80项通过，Ruff、mypy通过；前端构建通过。
- 前序交互修正：准备页55项及真实PG输入进度4项分批通过。
- 真实线上刷新仅发送1次，最终PUBLISHED；连接ACTIVE，credential_revision=2、authorization_revision=2，恢复审计1条。
- 三个受影响原草稿全部READY。当前两部剧：31091链接ready、23条素材；31092链接ready、22条素材。两个账户均解析成功，场景依赖完成，保留原推广链接。
- 故障处理时间内新增广告提交0条；未创建广告。浏览器插件不可用，真实业务验证来自站点认证API和服务器持久状态，不称为真实浏览器点击验收。素材/链接编辑交互由本地页面回归验证。
- 本轮代码已在既有功能分支提交并部署；用户未要求推送，因此未推送远端。
