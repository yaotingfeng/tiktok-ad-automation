# MCP 账户解析搭建权限：新加坡测试发布

- 用户原草稿的两行账户已识别，但均为 `blocked / account_build_unverified`，有效账户数为 0。实际账户均有 ADMIN 角色及有效目录/授权，权限记录却为上传 true、搭建 null。根因与修正见[设计说明](../superpowers/specs/2026-09-13-mcp-build-permission.md)。
- 发布提交 `fe8981fc3c1d98fc7df49c43bb97e0902db99120`，已推送并部署新加坡测试服务器。无新开关、无数据库迁移，head 保持 `mat_response_archive`；生产未变更。
- 发布前冻结写入、停止 Beat、正常排空 Worker、暂停备份 timer。完整备份 `/var/backups/tt-ada-staging/20260912T185304Z/` 包含 PostgreSQL、Redis、独立项目与前端构建、私有配置和证书；五个归档摘要、隔离数据库/Redis 恢复及配置/项目比对通过。另从恢复库读取现有素材响应密文，用本环境密钥解密，确认长度与 SHA-256 一致；1003 个发布跟踪文件摘要一致。旧版本/备份保留，timer 已恢复。
- 本地账户目录、授权再观察、角色、能力缓存及草稿准备回归 116 passed；多 BC 新鲜度 9 passed。Ruff、编译、两个实现文件 mypy、文档链接检查通过。外部请求是传输边界替身；没有将本地模拟当成线上真实结果。
- 发布后 API、Worker 主进程及两个子进程、Beat 都运行同一版本，数据库/Redis/加密密钥/MCP 注册和配额配置一致；已确认的导入/清理开关继续 true。HTTPS、静态资源、平台/租户真实登录及隔离、回调边界通过；已有完成批次仍为可用 23、清理 23、失败 0、占用 0。
- 通过租户真实登录及页面同源接口重新准备用户原草稿，保留原剧名及两行账户，新准备修订为 4。后台沿原 BC/连接完成真实权限再观察，能力任务 `scope_known=true / scope_build=true`；两行账户均由 blocked 变为 matched，阻塞原因清空。未手工修改 can_build、授权或绑定代数，未提交/创建广告。
- 浏览器插件报告无可连接会话，本次以页面同源 API 和服务器任务验证，不声称完成浏览器点击验收。草稿已 READY，24 个素材就绪；后续场景发现缺少 Minis 关联，用户已进一步授权处理全链路并实际创建广告，继续验证中。
