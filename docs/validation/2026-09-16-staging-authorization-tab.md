# 新加坡测试服：授权管理 Tab 修正

- 发布版本：`6e7045a167509f312609a254bbe733444d6fd67f`；上一版 `6a84fb9c075c1269f5ee2ce8080987eec9d609b8`。已推送 origin/main；本次仅前端和测试/文档，后端文件及冻结依赖与上一版本逐文件一致。
- 当前 BC 的使用授权卡片和更换操作位于“授权管理”Tab；广告账户只保留账户目录。相关搭建链接、缺授权提示和详情说明同步修正。
- 授权名称字段默认为空且授权接入流程未写入名称，原界面因此统一显示“未命名授权”。现由一个共用函数保留非空名称，缺名时使用通道及 ID 后八位。不会猜测授权人的名称，也不以某个 BC 名称代表可关联多个 BC 的授权。

## 验证

- 本地账户/授权 Playwright 104 项通过，其他三个相关文件（build-channels、build-preparation、materials）94 项通过；8 个改动文件 Biome 与 TypeScript/Vite 构建通过。已检查 390px 页面截图。
- 首轮 198 项有 6 项失败：三项旧无 BC 素材库断言不符合已发布的租户库行为，改为检查仍要求 BC 的搭建页；另三项受授权卡片与列表同页影响，限定作用域和轮询计数。补测授权入口位置、不同空名称的区分；卡片在列表授权状态更新后重新读取。
- 已用真实租户登录浏览器读取两个 BC：广告账户 Tab 无授权设置卡片，授权管理显示对应当前连接；`7678608005688066065` 显示 `官方 MCP 授权 · 4b2da73a`，`7683817908149272592` 显示 `官方 MCP 授权 · 500ec75b`。未更换授权、同步账户或执行广告写入。
- 完整备份 `/var/backups/tt-ada-staging/20260916T102006Z/`：PG、Redis、项目源码及前端、私有配置和证书共五份归档校验通过；独立恢复 1219 个项目文件、Redis 读取、18 张历史表及 501 份加密响应/凭据解密均通过。备份仍为同机副本，未配置异地复制。
- Alembic 保持 `material_seed_generations`，无新增迁移操作。配置文件摘要一致；5 个服务/9 个进程同版本同配置，入库/清理开关、存储预算和调用额度保持不变，备份及证书 timer 正常。首次进程检查早于 prefork 子进程启动完成；待 3 个 Worker pong 后复查全部通过。
- 前端 81 个构建文件逐项一致；HTTPS、平台/租户登录、403 隔离、MCP 配置、回调边界通过。租户素材库查询在两个 BC 下仍一致，共 553 项。发布后 error 级服务日志无记录。
- 本次未执行真实 TikTok 上传、共享或广告搭建；用户继续手动搭建测试。
