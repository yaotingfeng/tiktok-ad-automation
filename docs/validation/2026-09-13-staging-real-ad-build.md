# 新加坡测试环境：真实广告搭建验收

## 结果

2026-09-13（北京时间），用户授权自主解决原草稿卡点并完成真实广告创建。使用该草稿冻结的骏伯 BC、连接、账户、剧目、策略和预算，保留原提交，不新建重复批次。最终应用同源 API 返回 `COMPLETED`。

| 项目 | 已计划 | 已提交 | 已成功 |
| --- | ---: | ---: | ---: |
| 广告系列 | 2 | 2 | 2 |
| 广告组 | 2 | 2 | 2 |
| 广告 | 4 | 4 | 4 |

8 个三级广告对象全部严格回读成功，失败、未知、待处理均为 0。48 个目标素材步骤及 2 个 CTA 均成功。独立服务器官方 MCP 读取确认 4 条广告均为 `ENABLE` / `AD_STATUS_AUDIT`，每条包含 24 个视频。创建成功与平台审核通过分开；本次不声称已审核通过或产生消耗。

真实账户、对象 ID、冻结请求、完整回执和应用结果保留在服务器私有诊断目录及应用数据库，不将完整业务报告、签名 URL 或凭据提交至仓库。最终应用检查记录为私有 `final-app-submission-evidence.json`；广告平台独立读取为私有 `final-ads-<advertiser>.json`。

## 修复范围

- 账户解析：正确消费实际 MCP 权限与经过核实的账户授权，避免错误停留在“能力未核实”。
- Minis 与身份：持久关联版权方应用和实际 Minis，保留映射；接入实际可用的 TT_USER 身份、地区、IAA Day 0 能力。
- 素材与封面：配置两个实际观察到的 HTTPS 视频主机；处理封面重复图片 ID、平台改名和视频封面 displayable 语义，按已知 ID、摘要和尺寸校验。
- 创建：Campaign/Ad Group 的 request_id 从本地 UUID 派生为合法 int64 字符串；IAA 显式 ZERO_DAY。旧请求的确定性 int64 参数拒绝保留原始证据，经一次性运维修复创建 Campaign，随后普通回读绑定；不由空查询或一般 UNKNOWN 自动重发。
- 回读：移除 Minis 冗余 catalog 字段；从原始 MCP JSON 数字保留预算/ROAS 精度；广告组读取完整对象，避免 fields 筛选漏掉地区和状态。所有业务字段仍按实际响应严格比较。
- 已确认的自动清理、来源上传完整响应加密留档及部署开关文档继续有效。

## 验证

- 请求参数及双通道创建/适配器回归 117 项通过；补充合同、未知 Campaign 回读及窗口用例 29 项通过；旧预览双通道完整创建链 2 项通过。
- 原生 MCP 解码及创建合同 130 项通过；广告组完整读取与补查回归 30 项通过。
- 相关 Ruff、mypy 检查通过。真实原生 Campaign/Ad Group JSON 与原冻结请求严格比较为 MATCH。
- 最终服务器应用 API 核实全部对象与 8 个回读步骤完成；没有用模拟测试替代真实创建。
- 操作通过测试服务器应用及服务器官方 MCP 通道执行。没有使用本地广告 MCP；本轮未连接浏览器，不声称做过浏览器点击验收。

## 部署与配置

最终代码版本 `0d33c442151d17c3937223c2ed799124e45c4aeb` 已推送原功能分支并部署至 `/opt/tt-ada-staging/current`。数据库 head 保持 `mat_response_archive`。

每次发布都停止接收写入、暂停 Beat、正常排空 Worker、暂停备份 timer 并等待现有备份完成；完整备份 PostgreSQL、Redis、项目/前端产物、私有配置及证书，完成独立恢复、文件校验及响应归档解密/摘要校验后才切换。最终备份 `/var/backups/tt-ada-staging/20260912T202019Z/`，备份 timer 已恢复。

最终 API 1 个进程、Worker 3 个进程（主进程加 2 个 prefork 子进程）、Beat 1 个进程均加载同一发布和配置。`MATERIAL_INGEST_ENABLED=true`、`MATERIAL_CLEANUP_ENABLED=true`；媒体主机仍为 `v16-tt4b.tiktokcdn.com`、`v19-tt4b.tiktokcdn.com`。未新增开关或覆盖原确认值；后续部署继续遵循部署手册的首次/新增开关确认和运行时核实要求。
