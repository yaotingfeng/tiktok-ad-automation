# 全工作区视觉统一验收 · 2026-09-10

用户已确认策略列表试版的布局，并要求“主按钮用原来的黑色，其他都没问题，全部页面都调整一下”。本轮按[实施计划](../superpowers/plans/2026-09-10-workspace-visual-rollout.md)完成推广；回退标记 `ui-before-workspace-rollout-20260910` 指向 `601c181`，已推送 GitHub。

## 交付范围

| 范围 | 最终布局 |
| --- | --- |
| 共享工作区 | 22px正文标题，顶栏仅工作区上下文，浅灰内容底；移动16px/桌面24px外距；主按钮恢复中性黑色，深色模式保留相应中性高对比主题 |
| 租户、成员、用户、账户与授权 | 页头与主操作成组；筛选、表格、分页组合为白色Card；表格与Card外框之间保留内距 |
| 版权方 | 连接、链接记录与取链结果分别分组，保留凭据表单、自动恢复说明和原验证入口 |
| 素材与上传 | 素材目录、R2导入队列、历史批次及只读旧队列采用统一Card；Sheet/详情沿用各自内距，无额外整页卡片套叠 |
| 策略 | 列表取消局部蓝色变量；编辑、版本查看与示例使用统一标题和卡片；固定短列、长数完整换行保留 |
| 搭建与任务 | 输入、准备、冻结预览、任务列表和详情按业务分组；保留固定首列、表内滚动、步骤与底部sticky操作 |
| 设置与辅助页 | 个人资料、密码与危险操作分别成卡；登录、密码协助、404/异常和缺BC引导使用相同基础视觉 |

共享Card水平内距16/24px；表头左右16px，单元格上下12px、左右16px。简单列表指定短列宽度与弹性名称列；复杂业务表保留自动宽度，避免强制列宽遮住长字段。原业务请求、输入/保存/恢复、租户和BC范围、权限及冻结提交行为未改动。

## 代码与验证

- 共享基础 `b9bdeb3`、管理页 `6c8674d`、素材页 `ba89d16`、搭建与任务 `27ea0aa`；回归测试 `47b074a`。
- 整合工作树的 `bun run build` 成功，包含TypeScript和Vite生产构建。最终源码为`27ea0aa`，后续测试及文档提交不改变生产源码。
- 完整workspace浏览器回归 **318 passed，2.0分钟**；原有317项全部保留，新增1项深色主按钮对比检查，更新被本轮已确认设计替代的旧标题/主题断言。
- 在原有真实页面路由与API fixture中增加跨模块几何检查：唯一正文22px标题、灰底、黑色主动作、可见表格属于带内距Card、无整页横向溢出。覆盖平台租户、成员、账户、版权方、素材、输入/准备/预览和任务列表/详情；原390/900/1024/1440/2304px场景继续运行。
- 策略深色主按钮中性RGB、文字对比至少4.5、按钮与页面背景对比至少3通过；预算/ROAS大数文字范围不越出单元格，搜索、游标分页与权限拒绝仍有效。
- scoped Biome与`git diff --check`通过。生成的shadcn组件按项目既有忽略规则不在Biome计数内，已由TypeScript、生产构建和浏览器检查覆盖。
- 独立评审核对管理、搭建及共享基础，无未解决P1/P2；根端另查素材差异、fixture截图及实际页面。部分子代理定向检查在各自工作树完成，最终以根端318项整合回归为准。

测试使用Vite开发服务，保留现有测试对`/src`和harness动态导入的支持。命令：

```sh
bun run dev --host 127.0.0.1 --port 5196 --strictPort
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5196 bunx playwright test --project=workspace --workers=4 --reporter=line
bun run build
```

## 本地应用与边界

实际8011应用已载入相同生产产物。在原有会话和数据上核对策略黑色按钮、成员管理、版权方连接、未连接BC引导、平台租户、用户管理、个人设置与菜单跳转；最后保留在策略页。未保存业务表单、修改数据库、读取/更换凭据或调用外部广告接口。

有BC的完整素材/上传/搭建/预览/任务页面使用现有API传输替身验证，并检查1440px桌面及390px窄屏截图；不把fixture中的账户名称或状态当作实际授权与广告结果。布局验收不替代R2/TikTok真实联调。

主目录与`feat/workspace-visual-rollout`已同步并推送。旧代码可通过上述回退标签查看；需要撤回整轮时对该标签之后的本轮提交执行反向提交并重建前端，不改写既有历史。主目录原有未跟踪`docs/design-history/`保持不变。
