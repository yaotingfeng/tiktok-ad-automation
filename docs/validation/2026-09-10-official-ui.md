# 官方 shadcn 工作台视觉验收

2026-09-10，用户先要求只读对比，确认方案后授权实施。代码基线：`eb613a4`；参考 [dashboard-01 / New York v4](https://ui.shadcn.com/view/new-york-v4/dashboard-01)。

## 交付范围

- 288px inset 侧栏、Neutral 明暗主题、打包到本地的 Geist 字体及中文系统字体回退。
- 页面自身拥有标题，通过 portal 挂入顶栏：唯一 h1，16px/24px/500，随页面卸载自动释放。独立渲染时仍有正常 h1；没有全局残留标题状态。
- 取消工作台1536px限宽，小屏16px/桌面24px边距。保留1024px移动Sheet导航断点、键盘与焦点恢复。
- 管理、账户、版权方、素材、策略、搭建和任务列表统一为工具栏、独立表框、分页。Table基础组件不强制套边框；ServerTable与独立消费者各自拥有一层边框，包括用户列表加载状态。
- 表头和正文14px，表头不再强制大写/加宽字距，单元格8px内边距；表单正常Card内边距24px，策略表单独立限制阅读宽度。
- 原有服务端筛选/分页、权限、租户BC隔离、固定首列、60svh表格内部滚动和提交行为未改。没有后端或API契约变更。

## 自动化结果

- 改动前构建及原21项工作台回归通过，作为基线。
- 整合版本 TypeScript + 生产构建通过；主运行目录通过冻结lockfile安装，并再次构建通过。
- 原有完整 workspace 回归：**310 passed，2.3分钟**。使用独立Vite开发服务5194，真实前端/路由与既有外部API边界fixtures。
- 新增几何回归：旧版5项全部RED；根端在整合版本独立重跑 **5 passed，3.3秒**。覆盖2304px两种侧栏状态、唯一16px顶栏标题与导航切换、390/1024/1440px无整页横滚、表格单边框、筛选与分页间距、窄屏表内实际横滚与翻页。
- 两份含新增用例的工作台/策略文件另有 **56 passed，45.4秒**；与上述计数重叠，不累加成另一组总数。
- 首次全套误用静态preview，273项通过，37项因无法加载开发专用源码/harness入口而失败；改用Vite后上述310项全通过。未删除、跳过或放宽业务断言。
- 相关TSX格式检查及git diff检查通过。两位独立代理只读review通过；发现的PendingUsers加载边框遗漏已修复。

复现命令（在frontend目录，按本机Bun路径设置PATH）：

```sh
bun run dev --host 127.0.0.1 --port 5194 --strictPort
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5194 bunx playwright test --project=workspace --workers=4 --reporter=line
bun run build
```

当前测试集合包含新增5项，共315项；记录中的310与5为先后两次实跑，不冒称一次315项运行。

## 本地页面核对

实际代码目录为 `projects/tiktok-ad-automation/`，已快进到上述代码并更新8011静态资源，无需数据库迁移或重启工作进程。

用户已有会话实际DOM测量（2304px视口）：

| 指标 | 结果 |
| --- | --- |
| 侧栏展开宽度 | 288px |
| h1数量/位置/字号 | 1 / header内 / 16px |
| 展开时表格内部容器宽度 | 1958px |
| 收起时表格内部容器宽度 | 2174px |
| 两种状态左右间距 | 到外表框24px，到内滚动容器25px（含1px边框） |
| 整页横向溢出 | 两种状态均无 |

已核对策略既有记录仍可读、版权方菜单可跳转、素材页在无BC时保留原有“请先选择有效的BC”提示；最后恢复到策略页与展开侧栏。未创建业务记录、验证版权方凭据或调用真实广告写操作。

视觉检查采用测试生成的1440px素材列表、1440px策略表单、390px素材列表截图；实际用户标签页两次截图请求超时，因此该标签页的字号/间距证据来自实时DOM与计算样式，不宣称取得了该标签页的新截图。
