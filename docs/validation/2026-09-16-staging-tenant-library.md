# 新加坡租户素材库发布

用户明确授权：将全部本地应用改动提交推送并部署到 `137.220.150.31:22211` 新加坡测试服；素材来源 BC `7678608005688066065`，后续搭建 BC `7683817908149272592`。仅检查部署和跨 BC 准备前提，真实搭建由用户手动执行。不记录 SSH、登录或平台凭据。

## 发布前核查

- 应用仓库无遗漏的未提交功能改动，已有本地账户授权界面提交 `ea73a27`、统一素材库 `a723f69`、恢复修复 `9d9f913` 全部纳入；已将本地 main 推送到 origin/main，未删除其他远端分支。
- 旧服务器版本 `9744ccb628b3a9a77cfe60afadc504969e0de69d`，数据库 head `material_share_receipts`。五个 systemd 服务正常，磁盘约 4.7 GiB 可用；新版本复用完全相同 uv.lock/pyproject 的冻结依赖，独立打包新前端。
- 两个 BC 均属于同一租户，各有独立 ACTIVE MCP 授权、明确默认路由；A 有 42 个上传/搭建账户，B 有 30 个。没有修改授权、绑定或主账户配置。
- A 的 681 条可用平台副本对应 546 份素材；使用新代码、真实业务数据及只读事务逐批核查，546 份素材全部 `preparable/share_source`，无阻断原因。B 的 30 个账户对样本素材均可准备。
- B 尚未保存主素材账户；按现有规则只读计算的首次候选为 `7683815457845182485`，尚未持久化。它具备当前上传/搭建权限，真实平台素材查询成功；平台已有库存不等于应用已登记 A 内容的 B 副本。
- 随机性不作为证据：按最新验证时间选取 3 份不同源素材，逐份调用正式来源读取路径，VID/MD5/大小和 720×1280 规格校验通过，域名为现有精确白名单 `v16-tt4b.tiktokcdn.com`、`v19-tt4b.tiktokcdn.com`。未调用上传、共享或广告创建。

## Linux 检查与测试修正

专用测试库、Redis 14/15、合成 HTTP 边界。首轮 29 项为 28 passed / 1 failed；包括 API/MCP 硬终止后的原通道读取、真实队列隔离、seed 恢复/并发、封面恢复和迁移。

失败的旧 `test_prefork_deadline` 使用 3 秒硬期限和 1 秒软期限；在服务器重复复现时，任务停在 `PENDING/IDLE/tiktok_local_resources_unavailable`，未到本地 HTTP。与已通过的真实硬终止测试一致，给数据库鉴权预留 10 秒并关闭该测试的软中断，测试仍要求 HTTP 已发、原进程被硬终止、租约保留、未知结果不重发。同步使用现有 `quota_scope` 查询实际准入键，增加不含正文的状态诊断；没有修改生产超时、放宽权限或忽略失败。独立 Linux 复验 1 passed / 20.73 秒。

## 最终发布与验收

- 已推送并部署完整 SHA `6a84fb9c075c1269f5ee2ce8080987eec9d609b8`，前端与后端业务代码与已审查的 `9d9f913` 一致，新增仅上述测试修正及文档。1,138 个跟踪源码文件逐个校验，81 个前端构建文件逐个校验；本地全量后端 Ruff/ty 再次通过。
- 最终固定版本在独立 Linux 测试库运行 29 passed / 184.79 秒，无跳过；包括此前 Mac 跳过的硬终止、API/MCP 迟到结果核查与队列隔离。4 条警告来自测试中多线程进程启动 prefork，实际硬终止/重启/不重发断言通过。测试角色恢复 `NOLOGIN NOCREATEDB`。
- 正常停写、停 Beat 并排空三个 Worker；停止结果均 success。完整备份 `/var/backups/tt-ada-staging/20260916T095817Z/` 包含 PostgreSQL、Redis RDB、私有配置/证书、独立项目/前端和完整运行配置，五个归档的 SHA256SUMS 均通过。
- 独立解压核对 1,194 个旧项目文件，配置与 MCP 注册文件一致；Redis 在独立 Unix socket 实例加载后 PING、DB0 数量与读取通过，未覆盖原 AOF。PostgreSQL 恢复到独立测试库，18 张业务表的行数/摘要和 501 份加密响应的解密/长度/摘要通过，既有授权凭据可解密。
- 先在恢复库演练，再迁移在线库：`material_share_receipts → tenant_material_library → material_bc_seed → material_target_scope → material_seed_generations`。恢复库、在线库均 `alembic check` 无差异；迁移前后 18 表历史一致（分发新增空 seed_id 不作为历史变更），原 681 条 A 可用副本保留，seed 初始为 0，没有预先同步或新建广告。
- 五个服务/9 个进程同版本、同私有配置；resources/builds/control 三节点 ping 通过。原件导入 true、清理 true、租户/全局原件预算 0、媒体白名单及调用额度不变，上传/共享租约通过；备份与证书续期 timer 均 active。未修改服务器密钥、OAuth 绑定或 BC 默认授权。
- HTTPS IP 入口及 sslip.io 入口检查通过；平台管理员和租户管理员登录、平台接口 403 隔离、静态资源、回调错误边界及 API 404 通过。登录后真实素材接口不传 BC、传 A、传 B 均返回相同目录与游标；目录总数 553，不把历史失败/未入库素材当作可用素材。
- 发布后再次执行同一真实只读检查：546 份已有可用素材全部可准备，B 的 30 个账户均通过；3 个来源视频及 B 候选主账户真实读取再次成功。新版本服务启动后未发现 error 级日志；基础健康不作为实际搭建成功证据。

## 用户手动验收

访问 `https://tk-ada.137-220-150-31.sslip.io`（原 IP HTTPS 入口也可用），刷新页面后选择 BC `7683817908149272592`，从统一租户素材库选择 A 已可用素材，正常准备/预览/提交。

首次需要时，程序向 B 的固定主素材账户 URL 转存，然后向 B 内目标账户原生共享；后续其他 B 账户复用该来源。未预先保存主账户、未创建真实转存或共享任务，故本次验收确认的是代码、部署及真实读取/权限前提；真实 URL 转存、图片共享和广告创建仍由用户手动测试完成。可先验证一部剧/一个目标账户，再用 B 的另一个账户检验来源复用。
