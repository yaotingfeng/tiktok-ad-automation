# 搭建批处理验收

## 范围与边界

用户批准整体提速方案，并明确要求共享使用最多 20 个素材 ID × 10 个目标账户。仅处理既有授权任务组成的完整组合，同租户、BC、操作者、源账户和冻结授权才可拼批。未满批立即执行，不为凑 200 个组合等待；超限拆批。素材共享、读取和广告创建均保留共享限额，未将本地限额描述为官方配额。

本次不再创建任何真实广告。原任务的两条既有补建通过实际平台只读核实后关联回原任务；原 UNKNOWN、请求摘要与尝试记录不改写。数据库增加持久批次、成员、物理回执、不可变补建台账和批量封面候选索引。

## 已取得的本地证据

- 真实 Redis 队列隔离：旧部署两个资源槽被占满时广告不能开始；新资源/广告独立消费者时可开始。独立子进程执行，macOS 使用线程池验证队列分配；Linux prefork 项待测试服务器验收，未将本地线程测试冒充 prefork。
- 根代理 `tests/jobs`：69 通过，1 个 Linux prefork 项跳过。
- 旧 `provider_display_drama_id` → `build_batching` 实际 PostgreSQL 迁移通过；最终补建 13 项、迁移与队列合跑 15 通过、1 项 Linux 跳过。
- 前端生产构建通过，实际输出为 `backend/app/frontend`，不是旧构建目录；打包 SHA-256 为 `3cb83bc7fee0de04b8bdf999edb669c1174e8d22f4f724e10a9c123df60bb5e2`。
- 补建域独立后端回归共 75 项、任务页/连接页 Playwright 53 项通过；测试中原两条 UNKNOWN 不变，业务结果已完成含两条补建；原已替代对象不再进入恢复队列。

以下按执行先后保留本地和发布过程证据；当前发布及真实任务最终结果见末节，不能将本地测试等同平台吞吐。

根代理冻结后独立新库集成复测：MID、旧头迁移、补建、队列隔离、封面批核验、两通道批读共 **104 通过、1 个 Linux 项跳过，291.55 秒**。改动 Python 文件 Ruff（仅既有 Alembic env E402 单文件豁免）、九个核心文件 mypy、差异检查通过。

### 最终批次与容量证据

- 批次安全 31 项通过（29 + 最后新增过期发送门禁 2），MID 发现 24 项通过，既有分发/会话/只读恢复 48 项通过。实际 PG/Redis 和 SDK/MCP HTTP 边界验证 20×10 一次共享、21×11 四次共享覆盖 231 个合法组合，以及两满批同步到达 HTTP、同素材跨目标并行核查无死锁。
- MID 发现完整信息场景：源详情 1 + 共享 1 + 十账户搜索 10 = 12 次业务调用，200 条目标映射 READY。缺项仅重读该 MID，重复/越界/分页歧义不发布；尺寸信息不全必须补严格详情，不能承诺所有响应均只需 12 次。
- 批次独立 claim 75 秒，网络请求 40 秒；过期且未发送记录 NOT_SENT，已经发送的 UNKNOWN 不重放。上传长时限与 960000ms 准入租约不变。
- 最终封面批核验 31 项、两通道读取适配器 104 项通过/1 项跳过、旧原件保护 34 项通过。23 个新鲜视频仅 1 次图片查询，过期则视频/图片共 2 次；同组实际连接 checkout 113 → 25。
- 本地容量 3 项通过：100/1000/10000 条分别 2/20/200 组，4/40/400 次 SDK HTTP 边界调用；PG 候选查询约 0.019/0.216/1.472 秒，SDK 本地边界约 0.317/2.081/21.758 秒。万条首/中/尾候选查询扫描均不超过 55 行，无执行 Sort。以上不是整条真实广告或 TikTok 吞吐测试。
- 原件安全测试前置修正：旧 fixture 正常上传完整回执已经成功，后续坏 GET 根本未发生；在基线适配器也复现。改为真实回执后撤销上传权限阻止发布、再恢复权限回查，显式验证 POST/GET 各一次，保留 UNKNOWN/原件保护断言，未改变正常成功短路径。

### 真实只读验证：共享后的目标 MID 查询

从该租户、BC 和目标账户已成功原生共享记录选取三个源 MID，使用任务的原冻结 MCP 连接执行一次 `search_videos(material_ids=...)`。返回三条、完整一页，返回 MID 集合等于源 MID，目标 VID 集合和三条 MD5 均与原已核实目标映射一致；没有任何平台写请求。

这揭示并消除了仅做“批量已知 VID 详情”仍会漏掉的主链路热点：原生共享刚返回时目标 VID 未知，旧流程依然逐素材按名称搜索。新实现须将这一阶段也按同账户/MID 合并，不能把源 VID 直接冒充目标 VID。只读私有脚本为 `.runtime/singapore-staging/probe-shared-mid-read.py`，实际范围和身份不写入公开产物。

第二轮在候选 `c79465c` 的真实应用 gateway 再查同范围三条：实际目标VID、源MID、MD5、源文件名均3/3匹配；按原文件字节数及视频必需属性执行生产 `verified_video` 严格核验，也是3/3完整，因此本次未额外调用详情接口。仅一次真实素材搜索业务请求、零平台写入。私有证据 `/root/live-mid-metadata-c79465c.log`；这证明该真实样本支持快速MID路径，不代表所有未来返回都完整。

## 发布前只读基线

- 新加坡测试环境仍运行 `0cd835525345bd799737f31e6478a2570425e9c9`；数据库 `provider_display_drama_id`。
- 实际页面任务 KDMA：8 个计划/提交广告，已创建 6、待核实 2。原六条与两条补建的实际对象证据见前次提速验收；本次不重复创建。
- 内存 1967 MiB，总可用 469 MiB，swap 已用 109 MiB；系统盘可用 5.7 GiB。拟增加独立广告 prefork 1，资源保留 2，发布后检查余量与异常重启。
- 保留 `MATERIAL_INGEST_ENABLED=true`、`MATERIAL_CLEANUP_ENABLED=true`。共享总并发 4、单操作并发 2、每秒总调用 10/单操作 3、租约 960000ms 均不变；MCP 主体未核实，不拆配额域。
- 只改测试环境，不改骏伯生产，不推送，不做真实压力广告。

## 测试服发布过程

- 固定代码提交 `5ec1c69daac3707b8cc8f8e7b46ad6081b35a913`；源码归档 SHA-256 `367ccff0036e93a2551c398a23f65ae93b419e28c515090fd855592a2cfb62a5`，前端归档同上。
- UTC 04:57:09 开始排空，备份 `/var/backups/tt-ada-staging/20260915T045722Z/`。数据库、Redis、配置、独立源码含前端、完整运行配置五份归档校验通过；项目/前端/私有配置隔离解压逐文件核对，Redis 独立实例恢复 PONG，PG 独立恢复并升级至 `build_batching`，24 份加密响应全部解密/摘要验证通过。尚未配置异地副本。
- 真实 Linux 队列隔离两项通过（线程和 prefork，6.19 秒）；满批/并发和 MID 回读验收进行中，通过后才迁移在线库与切换。

### 首次 Linux 门禁拦截（未切版）

满批门禁 2 通过、2 失败、27 未选择（225.85 秒）：SDK 单满批及同素材跨目标核查通过；MCP 单满批和 SDK 双满批回到 queued、未发送。发布脚本停止在隔离测试阶段，在线库仍为旧 head、current 仍为 `0cd8355`；测试角色已收回 LOGIN/CREATEDB、timer 恢复，原四服务恢复且 HTTPS 健康。

该阶段继续补取结果码并确认重复路径：每个账户 authorize 与 MCP 协议边界都会执行完整批次 callback，不能仅统计主 Engine 而漏掉 bounded_session 的临时 Engine SQL。后续优化保留每次发送前全成员身份和当前权限核实，不以放宽超时或跳过核验掩盖重复工作。

### 实机根因与第二轮修复

- Linux 单项复现：MCP worker 25.994 秒后 NOT_SENT/mcp_call_failed，协议 tools/list 为 network 类别；gateway fixture 已关闭 keepalive，但同步鉴权仍会阻塞协议事件循环，测试 HTTP server 已接受请求的 1 秒读取上限提前断流。仅将测试 server 读取上限调为 30 秒后，网络错误消失，真实业务仍在 worker 41.813 秒处被 40 秒任务预算拒绝（NOT_SENT/tiktok_call_deadline_exceeded，未共享）。SDK 双满批同样产生两个 NOT_SENT/material_deadline，不是平台共享或测试 Barrier 问题。
- 全 Engine 实测每次短 callback 为 885 次 SELECT。本轮通过事务内合法来源批读、按完整冻结路由/BC/账户/能力核验一次，将每 callback 降至 252 次（减少 71.5%）。每个 HTTP callback 新建验证器，逐成员 nonce/digest/路由/VID/内容证据仍检查，能力分别核验，结束前检查时效；不跨事务/请求缓存。
- 本地满200 worker：SDK 2.546 秒/全部5860 SELECT，MCP 4.156 秒/全部9756 SELECT；以上完整统计包含临时 Engine，原仅主 Engine 数字不再用作全链路指标。新增查询预算先两项 RED（885超过300），修复后两通道 GREEN；同事务错 BC 参数另取 RED 并修复 cache key。
- HTTP 替身新增可配置读取上限，通用传输测试默认仍 1 秒、应用 gateway 测试使用 30 秒；不是生产请求超时。该边界修改后根代理官方 MCP 传输/素材适配器 123 项通过、1 项跳过（82.14 秒）。生产 40 秒请求/75 秒claim/5 秒数据库预算全部不变。
- 后续重型 Linux 隔离测试移到停写之前；失败保持旧站点服务，通过后才正常排空、完整备份恢复、实际迁移和切换。
- 第二轮代码冻结前批次专项 **46 项通过**（40 项147.67秒 + 6 项异素材源文件大小/MD5/SHA负向；后者连同错BC项再验7项17.79秒）。根代理三份生产变更 mypy、六份变更 Ruff 及差异检查通过，独立只读审查未发现新的范围/权限遗漏。原分发与 MID 合跑 **72 项通过（204.43秒）**；本轮批次/原分发/MID共118个不重复用例通过。
- 修复候选提交 `c79465cbce3d16f0a2831d6ab1c1d4f3bf580cae`，源码归档 SHA-256 `8f9fb68f9efa5ff82819d3a57519c8d58e5e77d76c81e50c46ee600e384b1616`，前端构建未变。Linux 第二轮预检在旧服务仍运行时进行，SDK/MCP单满批已通过，剩余并发与MID阶段继续验收；尚未据此宣称已发布。

### 修复后 Linux 满批与并发通过

在旧 API/全部 Worker/Beat 仍运行、主机可用内存约438MiB时，四项满批/并发门禁全部通过（174.80秒含建库/种子/断言），未降低批次大小、并发或业务期限。

| 场景 | 实际 worker 本地耗时 | 全 Engine SELECT | 单 callback 最大 |
| --- | ---: | ---: | ---: |
| SDK 20×10，1次共享 | 14.742秒 | 5860 | 252次/0.605秒 |
| MCP 20×10，1次共享 | 22.563秒 | 9756 | 252次/0.609秒 |

SDK 双满批实际同时到达共享 HTTP、400成员互不重叠：全用例69.439秒（含40素材×10账户种子与断言，不当作worker/平台创建耗时）。同素材跨目标核验5.047秒全用例通过。私有JUnit为 `/root/batch-acceptance-c79465cbce3d16f0a2831d6ab1c1d4f3bf580cae.xml`。以上是测试服务器真实PG/Redis＋官方客户端HTTP边界替身，不是向TikTok发400个真实共享/广告请求。

## 最终发布与真实任务验收：已完成

- 实际上线代码为 `c79465cbce3d16f0a2831d6ab1c1d4f3bf580cae`，在线数据库 head 为 `build_batching`。第二轮 Linux 满批/双批/并发核验4项通过（174.80秒），MID发现24项通过（576.21秒）；这些时间包含隔离建库、种子与断言，不是业务创建时间。通过后才停止接收写入并正常排空全部消费者。
- 最终完整备份 `/var/backups/tt-ada-staging/20260915T053221Z/`：五份归档校验通过，PG隔离恢复及迁移、Redis独立实例恢复、源码/实际前端/私有配置恢复核对全部通过，24份加密响应解密及摘要通过；恢复库上的真实 Linux 线程/prefork 队列隔离2项通过（7.32秒）。`RELEASE_COMPLETE` 已落盘，发布日志 `/root/release-build-chain-c79465c.log` 退出码0。旧版本及备份保留，尚无异地副本。
- API、resources、builds、control、Beat五服务/九进程的实际目录和私有环境均匹配新版本；实际前端167个文件摘要全部匹配。初次前端审计差异仅为归档顶层 `._frontend` AppleDouble元数据；审计器验证其固定魔数后仅排除该确切元数据文件，不忽略实际静态资源差异。
- HTTPS健康、登录页面/静态资源、真实平台管理员及租户管理员登录、租户访问与平台接口403隔离通过；三个Celery节点均pong，备份timer恢复。素材导入/清理仍为true，各级共享限额与租约未变。最终复查五服务active、异常重启均0；1967MiB主机可用454MiB、swap已用348MiB，不能据此无限增加prefork。
- 使用实际应用内部服务，对此前两条补建逐条执行新广告、新组内容/启用状态、旧组停用状态的真实平台只读核验，然后追加不可变关联。原六条广告再次读取为MATCH及ENABLE，原两条UNKNOWN请求摘要/尝试/远端ID事实不变；业务任务派生为COMPLETED，成功8、待核实0、补建2，已替代旧项不再进入恢复候选。私有证据保存在服务器纠正批次目录，未重复创建或启用广告。
- 实际浏览器任务页显示「已完成（含2条补建）」、Ad计划8/提交8/已创建8/失败0/待核实0/待处理0，四个剧目账户单元均2/2；页面明确没有需重试或核查的步骤。原用户搭建表单未操作，已验收任务另开标签保留。ENABLE仅为开关状态，不代表通过审核或已经消耗。
- 真实MID三条读取与补建/原广告核实均为只读平台调用，本轮零TikTok写入。没有进行真实20×10共享压力写入或万条广告压测；满批并发的传输边界回归与真实只读样本分别记录，不能承诺外部接口永不超时、限流或失败。
