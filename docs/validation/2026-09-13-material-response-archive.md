# 素材完整响应留档：新加坡测试发布

- 发布提交 `5fff6d2dba4013ad6ab29f691d995ceef37e1572`，已推送功能分支并部署新加坡测试服务器；生产未变更。设计与范围见[响应留档设计](../superpowers/specs/2026-09-13-material-response-archive.md)。正常成功上传不新增查询。
- 新增迁移 `mcp_multibc_runtime -> mat_response_archive` 已通过；API、Worker 主进程及两个 prefork 子进程、Beat 均运行同一发布目录。各进程的数据库、Redis、加密密钥、MCP 注册引用及调用策略一致；导入/原件清理均沿用已确认的 true，没有新开关。
- 发布前停止接收写入、停止 Beat、正常排空 Worker，暂停备份 timer。完整备份 `/var/backups/tt-ada-staging/20260912T181544Z/` 包含 PostgreSQL、Redis、独立项目与前端构建、私有配置及证书，五个归档摘要通过；隔离 PostgreSQL 恢复、独立 Redis socket 装载读取、项目和配置独立解压比对通过。发布前版本尚无归档表；1001 个新发布跟踪文件摘要一致。旧版本及备份保留，备份 timer 已恢复。
- 本地相关回归 187 passed / 4 Linux skipped；Ruff、编译、7 文件 mypy、63 个文档链接检查通过。服务器隔离 PostgreSQL/Redis 下 16 passed，包括 12 项留档专项与 4 项双 prefork 迟到回执/强制终止恢复；临时测试库已删除。Python/Billiard 测试环境产生既有多线程 fork 弃用提示，未出现失败；外部调用均为传输边界替身。
- 发布后 HTTPS、登录页面/静态资源、平台与租户真实登录、权限隔离、回调边界通过。已完成的 23 文件批次同源接口仍为可用 23、清理 23、失败 0、占用 0。没有新上传、重试历史失败素材或创建广告。
- 使用服务器应用自身的原冻结 MCP 连接，对用户指定账户中已存在的首个素材做 **一次** `materials.get_videos` 真实只读查询，通过新观察器保存完整工具结果。落库后管理员受控读取，1908 字节正文的 SHA-256 和长度与收到的结果一致；真实查询包含 MD5 和大小。此次是查询响应留档验收，不声称拿到了历史上传正文，也不把它算成新上传回执。实际归档 ID、摘要及账户等证据仅留服务器私有 `/root/mcp-upload-fix/response-archive-evidence.json`。
- 后续页面新上传将自动保存现有上传及异常恢复查询响应；上游未返回、历史未保存或进程在落库前终止的正文不能补造。首版无按时间自动删除，原件清理不删归档。部署手册已说明加密密钥成套恢复、权限读取、容量监控及归档失败处置。
