# 新加坡草稿素材匹配调度优化

- 用户授权优化、推送并部署新加坡。`1d7ab27001ea5d1a7d7fc439500fce031649e08e` 已推送 main 并部署，前版本 `9a9fea45b54a942733563694fee403368d67f94f`。
- 根因是本地每页选材后固定等待五秒，线上单次处理约 0.04～0.51 秒、任务间隔约六秒。现在每轮最多五页，累计一秒后不开始下一页；有本地工作立即续调度，账户/取链/场景外部等待继续退避。
- 新加坡真实隔离 PostgreSQL 的草稿任务、草稿准备、连接和目录回归 30 项通过（29.36 秒）。覆盖重复投递、批次/时间上限、205 条分页、错误回滚、外部等待。22 剧/550 条合成素材经过五轮，处理 5.255 秒，不含 Broker/Beat 等待，不能解释为真实广告搭建总耗时。Ruff、语法和差异检查通过。
- 正常排空 API/Beat/三个 Worker 后备份 `/var/backups/tt-ada-staging/20260917T025246Z/`。PostgreSQL、Redis、配置证书、独立项目/前端及完整运行配置五份归档 SHA-256 通过；1220 个项目文件恢复摘要一致，Redis 独立实例恢复读取通过，恢复库迁移检查、18 表历史摘要及 501 份加密素材响应验证通过。
- 数据库保持 `material_seed_generations`；前端与依赖未变，入库和清理继续 true、容量保持 0、额度和媒体白名单不变。备份 RELEASE_COMPLETE，保留旧版本与独立恢复目录，无异地副本。
- 首次运行验收落在服务启动期间，子进程尚未全部就绪且入口短暂 502；随后五服务共九进程全部就绪，新版本/实际配置一致，HTTPS 健康返回 true，备份与证书 timer active。未将 systemd active 当作完成验收。
- 未修改用户已有草稿、触发取链或创建广告；新准备和在途续任务自然使用本版本。线上真实批次总耗时尚未单独测量。
