# TK-ADA 名称与品牌排版验证 · 2026-09-10

## 环境口径更正

2026-09-19 用户确认当前没有生产服务器。旧版文件曾把一个远程环境和域名描述为“生产”，该分类及其服务器、入口、容器、备份和回退口径全部撤销，不得作为现网、连接目标或生产发布证据。历史原文仍可通过 Git 历史审计。

当前唯一已部署的远程环境是新加坡测试服务器。未来生产环境必须重新确认服务器、域名、运行方式和授权，具体见[生产环境筹备规则](../runbooks/production-junbo.md)。

## 仍然有效的产品验证

- 产品显示名称统一为 `TK-ADA`。
- 左上角主品牌为 18px、字重 600；副标题为 11px“广告投放工具”。
- 登录按钮静止状态只显示“登录”，登录页标题为“登录 TK-ADA”。
- 代码提交为 `d09c070fca637eb54fa0fe2779d07d04aff6d556`，当时已推送。
- TypeScript/Vite 构建、Biome、`git diff --check` 和 workspace-shell 22 项浏览器回归通过；本地登录页的品牌文字、尺寸和按钮状态完成核对。
- 本次品牌变更没有数据库迁移或业务逻辑变化。

以上仅是代码与界面验证记录，不表示生产环境曾建立或完成发布。
