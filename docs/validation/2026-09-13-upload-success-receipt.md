# 成功上传回执直接入库：测试环境发布

- 用户要求移除非必要的逐条回查，正常 URL 上传成功返回有效 VID 后直接完成入库。决策、同步调用与后台队列的区分、元数据证据边界见 [设计说明](../superpowers/specs/2026-09-13-upload-success-receipt.md)。未知上传恢复继续保留。
- 发布提交 `73822a13f2282274fe6ae6264711a7d9d5be0faa`，已推送功能分支并部署新加坡测试环境。生产未变更，无数据库迁移，head 仍为 `mcp_multibc_runtime`。
- 完整备份 `/var/backups/tt-ada-staging/20260912T174844Z/`：停写、停止 Beat、正常排空 Worker 后保存 PostgreSQL、Redis、独立项目及前端构建、完整私有配置。五个归档 SHA-256 通过，隔离数据库恢复、独立 Redis socket 装载读取、项目和配置隔离解压比对通过；995 个发布跟踪文件摘要一致。
- 功能开关含义及值沿用用户已确认配置：API、Worker、Beat 的运行进程均加载 `MATERIAL_INGEST_ENABLED=true`、`MATERIAL_CLEANUP_ENABLED=true`，同一发布目录及存储/授权/数据库/Redis/配额配置；备份 timer 已恢复。
- 本地相关回归 104 passed / 4 skipped，后补原生文本回执及双通道成功路径 4 passed（包含两个新增用例）；Ruff、编译、mypy 通过。服务器独立测试数据库和 Redis 12/13 补跑 17 passed，包含 4 个 Linux 双 prefork 未知上传/进程终止恢复场景；测试数据库已删除。外部请求均为传输边界替身，不是新真实上传。
- 发布后 HTTPS、登录 HTML/静态资源、平台与租户真实登录及隔离、回调边界通过。已有 23 个素材批次的同源接口仍为可用 23、清理 23、失败 0、占用 0。三批历史合计 46 个已清理原件；最新批次 22 成功、1 个此前的 `mcp_call_failed` 未重试，不将其算作正常成功或本次修复结果。
- 本次没有另传真实测试素材；新版本正常路径少一次素材查询、无固定 60 秒首次回查等待已由 API/MCP 集成测试验证，新的真实批量耗时仍待观察。广告搭建不在本次验收范围。
