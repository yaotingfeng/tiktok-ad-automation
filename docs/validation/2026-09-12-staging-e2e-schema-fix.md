# 三剧页面测试：接口定义阻断修复

## 已实际执行

- 用户要求从测试站点已登录的 junbo 租户，以正常页面完成三部网眼剧的上传、搭建与核验。素材共 69 条（每剧 23 条），约 2.44 GiB；仅使用用户指定两个广告账户。页面选用现有网眼连接、LemonShow Minis、IAA1.05 v2。
- 页面提交“解析并准备”，三剧推广链接均为已就绪，归因剧目编号与用户输入一致；账户能力核验反复失败，草稿持续“准备中”。没有素材上传成功或广告创建成功。
- 真实 tools/list 显示五个内置构建 schema 与上游不同，其中四个纳入当前网关的全目录校验；差异为 integer/int64 和此前不完整的嵌套结构。账户读取在业务发送前被合同校验拦截。异常被包装为 mcp_call_failed，再被账户任务误判为可重试网络错误。

## 修复和验证

- 固定代码提交 `a69d0502b2c8dede8eb9069cc2c03913acee4bbb`：根据实际公开 schema 更新合同及版本摘要；保留 mcp_contract_changed 脱敏错误码；合同变化和工具不可用明确 BLOCKED，保留权限未知，不绕过校验。
- 29 个声明合同与捕获的真实目录全部匹配。独立 PostgreSQL 测试库与 Redis 14，外部调用使用传输替身：定向 105 passed，扩展账户能力、网关、协议测试 176 passed；Ruff、Python 编译和 diff 检查通过。
- 后端修复，frontend、Bun/uv 锁文件与旧线上版本无差异，复用旧版本相同前端构建产物；未改数据库，head 仍为 `mcp_multibc_runtime`。

## 测试发布

- 发布前停止 API、Beat，正常排空 Worker，暂停并恢复备份 timer。完整备份 `/var/backups/tt-ada-staging/20260912T120208Z/`，包括 PostgreSQL、Redis、独立项目归档、原配置归档及补齐全部 Nginx/证书/systemd/私有配置的 config-full 归档。
- PostgreSQL 恢复到新建测试库并读取 migration head；Redis 在独立 socket 实例成功装载、PING/DBSIZE；项目隔离解压核对后端与前端产物；五份归档 SHA-256 均通过，存在 RELEASE_COMPLETE。旧 release、备份和隔离恢复证据保留。
- 983 个跟踪源码文件与固定提交一致；三项服务 cwd 均指向新 release，EnvironmentFile 均为原私有配置。调用策略/租约与 schema 摘要验证通过；HTTPS 健康返回 true，页面真实登录正常，备份和证书 timer active。

## 原授权升级复验

- 后续定位：升级后的合同摘要与既有连接不同导致管理读取被拒绝，不能由此认定用户必须重新登录。`86fafbdaca00115a15c2015b147e09dbdc730c1f` 增加仅协议重观测路径，完整核验真实目录后原子更新接口版本，保留凭据、授权代数、BC 绑定及原冻结任务。
- 管理升级、多 BC 管理/发现/新鲜度及运行目录五组回归 45 passed（真实独立 PostgreSQL/Redis，远端传输替身）；Ruff 和差异检查通过。
- 完整备份批次 `/var/backups/tt-ada-staging/20260912T122445Z/`，同样完成五份归档校验和 PostgreSQL/Redis/项目隔离恢复。源码检查补充符号链接按链接内容校验后，988 个跟踪条目通过；三服务同版本启动，备份 timer active，迁移未变。
- 发布前后连接、授权代数、绑定、默认路由与账户目录快照一致。页面使用原连接“添加 BC”成功读出 1 个已接入 BC，无需重新 OAuth；随后通过“同步账户”发起真实目录刷新。原授权生效时间未改变。
- R2 控制台已创建独立私有测试桶并保存本站点 PUT/GET/HEAD 跨域规则。旧桶未修改；旧用户密钥已过期，新单桶读写密钥待提交确认，尚未写入服务器配置。

## 未完成项与继续入口

- 上游接口版本更新后，需要按正常页面刷新已有连接的核验。平台 admin 代管身份可看到授权按钮，但点击后被权限拒绝；已记录为权限交互问题，未放宽服务端权限。
- 已使用 junbo 租户管理员账号继续测试，原授权升级复验成功；不再以重新登录为前置条件。已重新提交同批三剧两户准备，三剧有效且链接就绪，两户均返回 `capability_scope_unknown`，未生成可搭建账户。代码仍将 MCP 上传/搭建 scope 事实保留为未知，不能由目录读取成功推定写权限，真实素材与广告创建仍待验。
- 浏览器扩展拒绝读取本地视频文件，需要用户启用“允许访问文件网址”后继续素材上传。该浏览器权限错误不是系统素材接口的成功或失败证据。
- 后续：完成 R2 凭据配置及上传开关/容量核验，上传 69 条视频，核对三剧与两户的完整预览、预算、身份、CTA、素材分配，再提交广告并读取实际平台回执。MCP URL 入库仍受独立服务能力证据门禁约束，不因 R2 配好即视为 TikTok 上传可用。不得把本次代码/部署验证称为完整端到端通过。
- 草稿、取链任务、公开目录快照和部署脚本保存在私有 `.runtime/e2e-20260912/`；凭据、视频和业务回执不进 Git。
