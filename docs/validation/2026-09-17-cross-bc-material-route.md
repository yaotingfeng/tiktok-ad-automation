# 跨 BC 素材准备根因与修复

目标为新加坡独立测试服；生产未纳入范围。用户要求排查并修复已提交 22 剧中 18 剧可搭建的批次，18 剧 × 10 账户共 180 组合。

## 根因证据

- API、resources/builds/control Worker、Beat 全部运行；队列持续消费。素材准备失败导致后续 Campaign 等待，排查时 builds 队列约 4,800 条。
- 失败素材步骤未保存 distribution，通用错误 `execution_prepare_failed` 来自执行器捕获非 DomainError。
- 在真实数据上仅调用本地准备函数并回滚事务，无远端请求，得到 PostgreSQL `23514`、`material route connection scope mismatch`。调用链为 `ensure_target_asset → ensure_bc_seed → ensure_target_asset → flush`。
- 数据库 `mcp_material_route_guard` 对 source_route 和 target_route 均使用 `b.bc_id=NEW.bc_id`。来源连接只有来源 BC 授权，合法跨 BC 转存被按目标 BC 误拒绝。
- 历史迁移已把 source_route 的 CHECK 改为 source_bc_id，但漏改连接归属触发器。原跨 BC 测试将同一连接绑定两个 BC，掩盖该差异。

## 修复与验证

前向迁移 `material_route_scope` 仅调整函数中的绑定 BC 比较，使用当前冻结路由的 bc_id。字段 CHECK 继续将 source_route 绑定 source_bc_id、其他路由绑定目标 bc_id，租户、通道、不可变检查保持。迁移不更新历史业务行，不增加开关或依赖；存在跨 BC 冻结记录时拒绝降级。

真实本地 PostgreSQL / Redis：

- 新回归先失败：合法独立来源授权被拒绝；仅绑定目标的连接错误通过来源检查。
- 素材来源/目标独立绑定、错误连接/租户、冻结不可变、跨 BC 转存与并发、失败恢复、远端原件读取、历史路由迁移共 64 项通过。
- 增补历史数据库升级、无跨 BC 冻结记录时往返、存在跨 BC 冻结记录时拒绝回退的专项验证。
- 首次扩展测试 62 passed / 1 failed，因本地应用 Redis 指向未启动端口而返回 503；改用独立本地测试 Redis DB 13/14 后 64 项通过，没有改业务代码规避该失败。

补充历史升级/回退保护后，专项 6 项通过，Ruff、语法及 Alembic check 通过。

## 部署与恢复

用户明确授权后已推送 main 并部署 `9599fd7b2f8915b3d24125a1af6dbbf6733c62e5`。正常停止 API、Beat 并排空三个 Worker 后，创建完整备份 `/var/backups/tt-ada-staging/20260917T065027Z/`，包括数据库、Redis、独立项目归档和私有配置/证书；校验和与独立恢复通过，项目 1,237 个文件一致。21 张业务表及 944 份加密响应在隔离恢复/迁移演练和正式迁移前后一致。head 升至 `material_route_scope`，没有改写历史失败事实。

五服务九进程加载同一版本、同一实际配置；三个 Worker ping、双身份登录及租户隔离、前端资源和旧预览接口通过。功能开关继续为 ingest=true / cleanup=true，额度、对象存储、媒体白名单及容量配置不变；备份与证书 timer 恢复。生产未变更。

原失败步骤使用系统 `RETRY` 恢复流程，沿原冻结预览、账户、连接和请求身份执行；初始 1,339 个安全候选。请求 ID `1d7e8f13-9f0a-4fa6-9234-a6e08891c027`，恢复 ID `a24183b6-7913-4e74-b93d-97a8e31085ee`，仅作用于原批次 `454cfb90-d81d-4c81-b94c-9abbd5e00b42`。没有清空队列，也没有另建提交或直接重建已发送/未知结果步骤。

06:52 UTC 观察：恢复已安排 345 项，旧素材错误由 1,339 降至 994，错误最新时间仍是部署前 06:50:16；新分发成功入库，4 个首次转存进入平台结果核验。暂存 2 个 `mcp_refresh_pending` 的 seed 失败需继续观察，不能仅凭服务正常就宣称全部业务完成。批次仍为 RUNNING。

## 持续监控发现的第二个阻塞

- 06:56 UTC：恢复请求完成，原 1,339 个安全步骤已安排重试；`execution_prepare_failed` 清零且未新增。这不是广告完成数。
- 07:54 UTC：CTA 完成 179/180；素材步骤 4,770 个中 1 个进入 `cover_pending`，9 个 UNKNOWN，其余等待素材。48 个跨 BC seed 和 1 个目标账户分发已核实，180 个 Campaign / 360 个 AD 尚未创建；提交为 NEEDS_REVIEW，不能宣称正常完成。
- 07:51 UTC 素材队列 11,528 条消息中 5,090 条为相同任务 ID 的重复投递，最多 18 份；builds 9,316 条中重复 1,552 条。120 秒未消费补投与慢速 FIFO 消费叠加，放大验证/封面的等待。未清队列或改写原消息。
- 使用原 Worker 的临时消费者抽查少量**已有持久任务**，复用原消息 ID、payload、冻结路由及防重；3 个原失败步骤实际进入第 2 次尝试并创建分发。随后每个目标账户选一个已有只读核实任务抽查。所有临时队列排空并取消消费者，未改变 Worker 数、并发、额度或持久配置。
- 对异常 native-share 分发做一次冻结账户范围的只读平台查询，32 个 MID 返回 `40002 / filtering: material_ids: maximum number of items is 20`。由此确认第二个代码根因：`try_verify_batch` 将 MID 搜索与已知 VID 详情读取共用 50 条上限，导致共享成功后仍反复核实失败。目标上传自身的 8 个 `mcp_business_error` 未保存原始回执，不能据此同归因为查询条数错误，也没有盲目重传。
- 修复仅把 MID 发现批次限制为 20，已知 VID 详情仍为 50；不改变权限、身份核查、写入流程或数据库。新增真实 PG/Redis + MCP HTTP 边界回归：21 个已共享成员在旧代码下重复发出 21 条查询；修复后 20+1 分批并全部 READY，零共享写入。
- 一次私有检查脚本因从外部目录启动而加载共享虚拟环境旧 editable 路径，出现旧配置校验错误；核实 current 和五服务未切换，改用当前 backend 下的 stdin 入口。不是服务器再次发布或实际服务配置失效。

查询条数修复的发布与最终观察另行补记；当前仍有协议阶段短事务超时、未知上传结果与重复排队需要观察。只读本地性能抽查 50 个现有成员执行 1,550 条 SQL / 2.081 秒，无远端调用、事务回滚；不能仅凭这一抽查认定所有超时根因。

补充修复验证：批量分发（含并发、租户/操作者隔离、共享回执保护）与跨 BC 约束共 55 项通过；专项先复现 `[21,21]`，修复后为 `[20,1]` 且 21 个成员全部 READY。Ruff、格式、语法和指定虚拟环境的 ty 检查通过；第一次 ty 调用未指定 Python 环境而未找到依赖，纠正环境后通过，未放宽类型规则。

## 查询批量修复部署

`c85cca1cfd4905e0430ecbf530c2602672269ffd` 已推送并发布。正常排空后完整备份 `/var/backups/tt-ada-staging/20260917T080601Z/`，独立恢复核实 1,240 个文件、21 张业务表、946 份加密响应，数据库 head 仍为 `material_route_scope`，Alembic check 无新操作。五服务九进程加载相同版本和原配置；三个 Worker ping、两种管理员登录及租户隔离、前端和旧预览接口通过，两个 timer 正常。

08:07 UTC 基线：CTA 180/180；分发 READY 49（其中 seed 48），素材 UNKNOWN 15；原 RETRY 请求仍为 COMPLETED / 1,339，未另建恢复请求或批次。目标转存结果不明增至 13 个 seed（12 个仍标记 `mcp_business_error`，1 个已转为 `material_result_pending`），保留原尝试继续只读核实，不能重传。广告创建仍为 0，不把部署成功等同于整批恢复。

08:11 UTC 复核：分发 READY 从 49 增至 112（seed 48、目标账户分发 64），证明本次补充修复后真实映射开始增长。缩小查询条数的只读复查返回 12/12 条，MID、名称、MD5、大小全部匹配，可播放且尺寸信息完整，未再出现条数超限。第二轮抽查仍仅复用原持久只读核实任务；临时消费者再次排空并取消。

**未完成项**：素材步骤仍有 19 个 UNKNOWN，1 个等待封面，其余仍在准备/核实；Campaign 和 AD 创建均为 0，提交仍 NEEDS_REVIEW。resources 14,431 条 / builds 9,389 条消息仍有历史重复积压，部分验证为 `tiktok_call_deadline_exceeded`。不能把旧错误清零或回执增长等同于整批正常推进。后续应针对重复投递的恢复机制与短事务/协议期限另做回归修复，并核实 13 个未知转存的真实平台结果；不得直接清队列、修改冻结素材集合或重发未知上传。

08:12:48 UTC 最终复核：通过历史 `step_evidence` 关联原始失败步骤，确认 **1,339/1,339 已实际执行第 2 次及以上尝试**，当前原错误为 0；不是仅登记恢复请求。进入 `cover_pending` 的素材步骤由 1 增至 15，READY 分发保持 112；素材 UNKNOWN 19、广告创建 0。任务仍在系统原恢复机制下运行，当前只可报告“已重试、部分推进”，不能报告“整批已恢复正常”。
