# 已授权账户持续可用与 R2 原件不限量

用户要求取消账户权限的 24 小时拦截，使已授权账户显示可用，将临时原件总量改为不限制；不处理视频格式失败。范围为新加坡测试环境及对应功能代码，生产不变。

## 实现

- 已核实授权及账户目录不再按年龄失效；账户列表、连接能力展示、实际 route 核验、批量来源验证、广告恢复及历史回读保持一致。
- 已完成角色证据持续复用；保留未完成分页同步的期限，以及真实撤权、原授权/BC 绑定代数、租户权限与账户归属检查。
- R2 全局/租户窗口均以 `0` 表示不限制，默认值、Compose 注入、模板及检查脚本一致；保留原件计数、原文件校验、并发控制与清理。
- 当前站点原有账户已存储 VERIFIED/可上传/可搭建，取消年龄过滤即可恢复展示，不修改历史时间或伪造授权。

## 验证与发布

先运行新增回归，原代码明确失败于 `route_evidence_stale` 与零窗口拒绝预留；修正后基础 20 项通过。10 个修改模块的 mypy、修改后端与测试的 Ruff、全部修改 Python 语法检查、R2 零值/负值配置检查通过。

扩展扫描发现目录发现测试清理外键错误、旧自动权限预期及迁移探针缺列；在未修改的 `4230f8f` 独立源码副本和专用测试库复现同样的 5 项失败及对应清理错误，未修改这些无关用例，不宣称全库回归通过。10001 账户全量分页用例耗时过长，本轮专项排除，未以该用例证明大目录性能。

专项覆盖账户路由/列表/连接、两通道物理请求权限、完整目录、角色证据、R2 配额/探测、批量分发、上传接口、搭建恢复及历史回读。整轮 315 项通过，4 个使用旧日期断言的目录用例失败；修正这些断言后完整目录专项重跑 20 项全过，319 项专项均有通过记录。未完成 10001 账户分页性能验收，不宣称全库测试通过。

## 测试环境实际发布

- 功能提交 `ce2f119e0e0a1810d9007800f8c06052a79346ef`；为避免带入尚未部署的分页功能，将同一补丁干净应用到线上 `4230f8f`，形成独立发布提交 `ca69cc3c7239f9440d4091f81a5b68c8337aa810`。未切换工作分支、未推送。发布源码所有 Git blob 核对一致，独立源码快照的权限/目录/配额回归 40 项通过。
- 完整备份 `/var/backups/tt-ada-staging/20260915T151342Z/`：PG、Redis、独立项目与构建产物、完整配置/证书均有校验和；隔离 PG 恢复、24 份响应归档解密及摘要核对、Redis PING/数据读取、项目与配置解压核对通过。数据库 head 保持 `preview_display_drama_id`，未执行线上结构变更。
- 首次演练发现恢复辅助脚本未初始化素材任务注册，已在切换前中止并恢复旧服务；补齐后重新完整备份及演练。隔离恢复库恢复 115 个原件、再次执行 0 个，证明不重复入队。辅助脚本及私有配置仅保存在私有运行目录。
- API、素材/广告/控制 Worker、Beat 共 9 个进程均为新发布目录；所有进程实际加载全局/租户配额 `0/0`、入库/清理 `true/true`，数据库、Redis、加密与调用策略配置一致。三个 Worker ping 通过，备份 timer 已恢复。
- 真实 HTTPS、前端资源、平台管理员与租户管理员登录及权限隔离通过；42 个账户全部 `AVAILABLE`、可上传且可搭建，每个账户的 read/build/upload 实际后端入口核验均通过。没有伪造授权时间、修改账户事实或绕过租户/BC 隔离。
- 原批次在服务恢复后由旧 outbox 自动继续执行。线上辅助脚本因状态已变化而保护性退出，事务未写入，不再手动重复补队。两次观察从 17 增至 36 个文件真实上传成功且账户素材可用，最新 1 个正在上传，78 个旧阻断状态仍随原队列等待处理；本次并非只有健康检查或模拟上传。
- 366 个文件仍是 `waiting_capacity`，没有已完成原件上传的证据，需要原上传页面继续传输。3 个 `invalid_video` 保持不动。并未宣称整批 484 个文件全部完成。

原 4 GiB 来自应用的租户临时原件预算，不是 R2 购买容量。该预算统计 R2 原件当前占用/预留（非历史累计上传量）；现取消全局和租户上限，正常清理与统计仍保留，不更改 R2 服务本身的计费或单次请求约束。
