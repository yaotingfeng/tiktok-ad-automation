# 原批次正式调度活性修复（持续验收，未完成整批）

- 原提交：`454cfb90-d81d-4c81-b94c-9abbd5e00b42`，18 部可执行剧 × 10 账户 = 180 组合。
- 发布：`f0698e38b763353ebf0824896f060780a7f4a808`，仅新加坡测试服；上一版 `5c16ce11c84b37cd3f86df9a31451a4bdde396b1`。无数据库迁移、配置、额度、并发、前端及新鲜度变化。
- 修复：旧素材消息提前校验代次/领取身份；素材依赖持久等待、结果落定再投递；每提交一个可执行素材组合窗口及后续自动接续。旧消息自然消费，不清队列、搬队列或临时提优先级。
- 本地：107 项相关回归、40 项补充回归（含重叠）和额外 2 项边界通过；真实 PostgreSQL/Redis，外部仅官方传输替身。Ruff/格式/ty app 通过。覆盖两组合全部创建与回读、过期消息、重复投递、窗口交接、视频过期后再复核及未知结果让出窗口。

## 发布与恢复验证

按部署手册正常排空五服务。完整备份 `/var/backups/tt-ada-staging/20260917T154301Z/`：数据库、Redis、配置、独立项目归档和运行配置共五归档 SHA 通过；1248 项目文件、私有配置隔离解压一致；独立 Redis 装载并成功读取，独立数据库恢复及迁移检查、21 表及 950 加密响应历史一致。备份仅同机，尚无异地副本。

五服务九进程的实际 cwd 和完整私有配置一致；三个 Worker ping 成功；两个 timer 保持 active。原前端资源字节一致，HTTPS 健康、平台/租户双身份登录及平台接口 403 隔离通过。服务器隔离真实 PG/Redis 回归 25 项通过（194.68 秒）；测试角色恢复 NOLOGIN/NOCREATEDB，专用空测试库已移除。

## 正式流程观察

- 15:44 UTC：完整仍 2/180，结果队列 10279、builds 6252；159 个未来素材步骤已经由代码转为持久等待。
- 经产品 HTTP 核查接口登记原提交请求 `61c524bc-25cb-57eb-ab98-524840db5cf2`，恢复任务 `20307b6c-c0ed-4119-9362-186f2ee1c15c`；只安排原任务核查，由正式 Worker 执行，不直接运行执行器或修改业务状态。
- 15:47 UTC：持久窗口等待约 1360，builds 降至 4362；当前组合 `00d3f9d2-f58c-44e2-9613-5cb151899b1d` 有 32 素材，其中 5 项已进入依赖等待。完整仍 2/180、素材成功仍 158/4770，不等于广告推进已验收。
- 旧封面另有分页不完整、期限及领取冲突阻断，继续查真实原因；13 项无回执来源上传未盲目重传。目标保持原批次完整创建并回读，尚未完成。
- 16:00 UTC：完整仍 2/180，builds 已清至 0；未来素材 4447 项持久等待，但资源结果队列仍 10807。旧结果中约三分之二是过期 claim/revision，正式消费者自然退出；两个资源 Worker 当时均在执行封面准备。
- 16:05 UTC：当前 32 素材中 14 项已推进至封面等待、18 项仍等待视频；不是组合创建成功。原 RECONCILE 恢复任务已完成 13 项只读调度，不是 13 项上传或搭建成功。
- 为下一轮完整备份保留空间，重新核对五归档 SHA、950 历史及零连接后，移除临时恢复测试库 tt_ada_tenant_library_f0698e38_test（约 192 MiB）；业务库、备份和版本保留，可由对应 dump 重建。

## 封面等待补充修复（尚未发布）

已复现两个额外调度缺陷：目标等待来源封面时每 5 秒重新投递，和旧队列未发送未来封面不受搭建窗口约束。改为持久等待，由既有恢复定时器唤醒；来源任务、已发送任务、显式核查和已绑定共享批次继续原恢复路径。素材库独立操作不受搭建窗口限制。

本地相关 81 项通过（147.85 秒），已发送/双通道/批量核验与 10000 项容量补充 167 项通过（354.04 秒），共 248 项；Ruff、格式和 ty app 通过。全部外部调用使用官方传输替身，不代表真实业务成功。

“重试没有清分页进度”的假设被新增测试排除：现有异常路径已经清理 scan_state，没有修改分页语义。真实只读观察到目标库存由旧 118 增至 124，完整两页去重计数一致；这不能代替任何未知上传未发生的证明。

16:11 UTC 另一个最新阻断批次的真实只读读取：第一页 100 项/总数 121，第二页 22 项/总数 122、与首批重叠 2 项，再次首页 100 项/总数 122。证实读取期间库存变化；后续需为有界重新核查添加测试，不降低完整库存要求或绕过未知发送边界。

原 RETRY 请求 9254ea46-c4ff-5a31-b76d-6a62b05ac67e 的实际进度端点回读为 COMPLETED/scheduled_count=3（静态受理回执仍为 QUEUED/0）。只代表 3 项恢复调度，非 3 个组合成功。
