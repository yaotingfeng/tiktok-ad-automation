# 新加坡网眼标题首尾空白修复

- 用户授权修正未识别剧名问题、推送并部署新加坡测试环境。功能提交 `9a9fea45b54a942733563694fee403368d67f94f` 已推送 `origin/main` 并部署；前版本 `6e7045a167509f312609a254bbe733444d6fd67f`。生产环境未操作。
- 网眼搜索响应标题统一去除首尾空白，完整标题精确匹配规则、内部空白、大小写、标点和 ID 不变。
- 服务器隔离测试库：协议和相关工作流 56 项通过（5.11 秒），包含带制表符标题的完整匹配、历史推广链接复用及命名。扩大工作流运行曾有一项临时数据库清理权限错误（62 passed、1 teardown error），未宣称全套通过；本地测试数据库未运行。Ruff、语法检查和 diff check 通过。
- 发布前正常排空 API/Beat/三个 Worker。完整备份 `/var/backups/tt-ada-staging/20260917T023736Z/` 包含 PostgreSQL、Redis、私有配置证书、独立项目/前端、完整运行配置，五份 SHA-256 校验通过。
- 隔离恢复验证：1219 个项目文件摘要一致，Redis 独立 socket 实例恢复及读取通过；PostgreSQL 恢复、Alembic check、18 表历史摘要及 501 份加密归档解密/摘要通过。数据库 head 保持 `material_seed_generations`。备份已标记 RELEASE_COMPLETE，未配置异地副本。
- 发布后五服务共九个进程均使用新代码目录和相同实际配置；入库/清理开关保持 true，容量值保持 0，调用额度及媒体白名单不变。备份和证书 timer active，HTTPS 健康检查返回 true。前端源码及实际构建与前版本一致。
- 真实只读网眼检索：Throne of the Waves（32808）、A Mistaken Flirtation（32810）、Lies Under One Roof（32811）分页完成后均精确匹配唯一候选。不创建推广链接或广告。
- 既有草稿与版权方准备任务保留历史失败结果；仅刷新准备会复用该任务，不等于重新检索。本次未修改历史任务或补传素材，新建准备任务使用修正后的识别逻辑。
