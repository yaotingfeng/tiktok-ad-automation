# 2026-09-17 新加坡素材队列恢复

仅用户指定的新加坡测试服；生产不部署、不清队列，不重提广告或盲重传 UNKNOWN。

## 12:46 UTC 封面核验补充发布（整批仍进行中）

- 已发布 `b6462b09b8a783755c6bb1573fb0b0a66ad7480a`。线上已有图片的批量核查反复超时；真实 PG 50 项回归捕获 API 920、MCP 2,168 次重复 BC 绑定查询，MCP 修复后为 100 次。
- 共同授权仅在同一次短事务复用；每次真实请求、返回发布均建立新的核查范围，逐项 claim/冻结路由/摘要/VID/图片/映射/回执检查保留。没有改变证据 900 秒有效期、上传规则、额度或配置。
- 本地相关 158 项、增量 5 项通过（有重叠），Ruff/ty/格式通过；服务器隔离恢复库与 Redis 14/15 的同一增量 5 项通过，真实业务 Redis 0 不用于回归。
- 完整备份 `/var/backups/tt-ada-staging/20260917T124524Z/`，五归档校验、1,246 文件、21 表/948 份加密归档独立恢复通过。消息 26,702 → 26,702、搬移 0；数据库 head 不变。五服务九进程、三个 Worker 队列、双身份登录/租户403隔离、HTTPS/前端及 timer 验收通过。
- 12:48 UTC MATERIAL 成功 111，AD 仍 2；分发 READY 聚合 2,017（含历史代数，不等于当前 MATERIAL 成功数）。49 个超时源封面已重新排队只读核验，尚不能把排队状态当成 READY。继续监控原任务，不重提整批。
- 本轮另对 1,798 个明确未发送、源 seed 已成功的等待分发调用既有 `resume_seed_dependency`（2 个并发变化跳过），无直接远端调用。13 个未知 URL 转存无上传响应归档，不能从错误代码推断无副作用；有限补传确认仍未收到。

## 13:29 UTC queued VID复核与队列索引发布

- 已部署 `3ff86a3b6c285a03b90e962944aec37d23307c44`。真实1,236项queued分发已有VID、操作verifying却未纳入批读；现在已知VID路径纳入queued，20MID发现路径不变。API/MCP回归先失败后通过，素材批量53项、outbox/队列45项及调度4项通过；Ruff/format/ty通过。
- 完整备份 `/var/backups/tt-ada-staging/20260917T132835Z/`，五归档、1,246项目文件、21表/948加密响应独立恢复一致，数据库head及配置/额度/前端保持；只有同机备份。消息31,215→31,215，无删除或换队列。
- 五服务九进程、三个Worker消费队列、timer、双身份登录及HTTPS资源通过。服务器隔离恢复库/Redis14和15的新索引测试1 passed、Linux队列3 passed；没有用业务库跑测试。
- 17个可核查步骤通过原提交的持久RECONCILE任务恢复调度，记录ID见实施进度。13项无上传回执仍未补传；40项共享前源读取失败有明确未发送证据，重读一批20源VID匹配，不与未知上传混同。
- 13:41 UTC MATERIAL146/4770、AD2/360、完整1/180。源封面READY504是聚合数字，不等于当前素材步骤完成。只读核对旧消息约19,827条，Worker自然消费的一轮55秒内结果队列25,859→24,090；未执行队列删除或隔离。持续监控，整批仍未完成。

## 13:11 UTC 封面规划与重叠分页发布（业务继续恢复）

- 固定版本 `5d5b6550975e0cc81cc2617d371458b0f14e26f5` 已推送、部署。规划限制20素材×10账户，实际重叠页仅在稳定总数且累计唯一ID完整时才结束负向库存检查，最多三轮；仍不允许重发已armed共享。
- 完整备份 `/var/backups/tt-ada-staging/20260917T131020Z/`：五归档SHA-256一致、1,246旧项目文件及前端字节、21表/948加密响应独立恢复一致；隔离Redis载入读取通过、迁移演练无差异。只有同机备份，没有异地复制。
- 五服务九进程新版本/私有配置一致，功能开关、额度、前端及head保持；备份/续证timer、双身份登录隔离和HTTPS资源通过。队列检查29,658→29,658，无删除或换队列。
- 本地规划相关115项、分页后相关71项通过，Ruff/format/ty通过。服务器仅隔离恢复库与Redis14/15执行新三项，3 passed / 40 deselected，115.62秒；不冒充真实平台写入验收。
- 13:12至13:16 UTC原批次MATERIAL130/4770、完整单元1/180、AD2/360。新增CAMPAIGN失败为 `dependency_failed` 且请求未armed；原调度器在依赖完成后已有安全恢复路径。源封面READY465为目标BC聚合，不是当前4770素材全部成功。
- 104个原批次封面阻断候选，经既有核查/安全retry按共享批次去重，60个锚点PENDING、1个VERIFYING；其他共享成员随原批次锚点恢复。仅对当前到期、身份完全一致的未发布封面outbox有界补发（每账户最多一个），并提升相同消息优先级；已发布但不在队列的消息留给原修复机制，不能强补投。
- 13项旧上传无回执仍未获有限补传确认；不改写UNKNOWN。续页被旧队列延迟，有限运维推进/监控继续，整批未完成。

## 12:13 UTC 后续发布与整批恢复（进行中）

- 用户再次要求整批跑通并主动监控。重新取证发现旧消息空跑、深队列去重漏扫以及单项共享不能合并核验；服务活跃不等于整批恢复。
- 发布 `218c64f5a650027c596495a08614545860eca8b1`：快照漏扫时以 Redis 有界只读筛选候选，完整身份与 LPOS 实时确认不变；多个单项原生共享结果也可按最多 20 个 MID 合并核实，保留单成员原路径和逐项内容/授权检查。
- 回归先失败再通过：单项共享 2 个通道先只完成 1/3；深队列去重原先把 3,001 条变成 3,002 条。素材 36 项、jobs 89 项通过/1 Linux skip，随后含 2 万条深积压的队列增量 20 项通过；Ruff、指定虚拟环境 ty 通过。服务器 Linux 队列隔离 3 项通过。
- 完整备份 `/var/backups/tt-ada-staging/20260917T121241Z/`：五归档校验、1,246 项目文件、21 表/948 份加密响应独立恢复一致；无迁移、配置、开关、额度或前端变更。消息检查 24,778 → 24,778，未删除或搬移消息。五服务九进程/三个消费者、双身份登录隔离、预览接口、HTTPS 及 timer 验收通过。
- 线上只读抽检：旧快照仅覆盖部分结果队列，尾部 1,000 条有 808 条漏扫；修复后的精确候选补查 5/5 找到。真实 20 MID 搜索一次返回 20 条，名称/MD5/尺寸/可用状态全部匹配，耗时 6.46 秒；这只是读取验证，不冒充整批创建完成。
- 原批次有限运维恢复：精确当前消息优先级调整；每次 55 秒暂缓领取 resources，原两个执行槽只处理 resource-results，finally 恢复原消费队列；调用既有依赖恢复函数为 180 个已完成 seed 的未发送等待者绑定来源（无远端调用）；2 个未发送的阻断封面通过既有受限 retry 恢复。未改写冻结广告内容、未扩大账户或重新提交。
- 12:16 UTC MATERIAL 成功 61，分发 READY 1,148；广告仍为此前一组 2 条，继续处理源封面与核查积压。13 个旧 URL 转存未知结果按冻结名称读取均未找到，未将“查不到”当作无副作用证明；已向用户请求二次核查后的有限补传确认，尚未擅自补传。整批未完成。

## 已发布 eef056c

- outbox 对 broker 内同 ID/name/tenant/actor/payload 的消息实时确认，已排队时不重复投递；缓存不能吞掉丢失消息。
- 核实和封面独立 `resource-results`，原 resources Worker 同时消费，仍为两个槽；额度、开关、密钥、数据库 head 和前端不变。
- 批量核验逐成员检查冻结身份，共同账户权限每次物理请求前只查一次；50 条原有 1,550 次查询，真实 PG 用例现不超过 220 次且全部 READY。
- 本地 jobs + 批量核验 131 passed / 1 Linux skip；新增搬移/畸形消息/跨 BC 增量 19 passed；Ruff/ty 通过。服务器 Linux 队列隔离 3 passed，三个消费者队列符合部署模板。
- 完整备份 `/var/backups/tt-ada-staging/20260917T085826Z/`，五份归档 SHA-256、1,240 个项目文件、21 张表和 947 份加密响应恢复一致。仅同机备份，未配置异地复制。
- 停写窗口搬移 17,465 条结果消息，前后均为 23,638 条，消息 SHA-256 多重集合一致；保留重复副本，没有清空任务。五服务九进程同版本同配置，登录隔离、HTTPS、前端和 timer 通过。
- 09:00 → 09:04 UTC：本批素材分发 READY 从 226 → 519。CTA 180/180；Campaign/AD 仍未完成，不能据此称整批搭建成功。

## 后续：恢复扫描拖住数据库

- 实测多个 `repair_material_dispatches` SELECT 连续执行 1～8 分钟，旧 Celery 任务已退出但 PostgreSQL 查询仍占 CPU。仅对已确认超时的该类查询执行取消，未修改业务行或远端状态。
- 原比较把 operation/distribution UUID 主键转成 VARCHAR，相关 EXISTS 不能用主键定位。改为安全转换消息引用为 UUID（非法消息为 NULL 不匹配）；保留原代数、claim、原消息和公平排序。
- 恢复事务设 10 秒 SQL / 2 秒锁期限，短周期扫描关闭本事务 JIT，避免 Worker 硬期限后遗留查询；不改变全局数据库配置。
- 真实 PG 计划回归先失败、后验证主键 Index Cond；增量 9 项通过，覆盖畸形引用、旧代数、活跃领取和初始丢失消息。扩展旧测试 7 项在修复前后同样失败（旧上传配置/原生共享断言），另 9 项通过；未将既有失败计为本轮修复通过。
- 服务器隔离恢复库全部回滚的对照：旧查询触发 SQL 超时，新查询取得 100 候选；关闭 JIT 的观测为 3.840 秒。部署前仍须完整备份、独立恢复及真实工作进程验收。

## 已发布 d22bde3 与持续观察

- `d22bde343ce909d6142b554eec393aaf6d192563` 已推送并部署。完整备份 `/var/backups/tt-ada-staging/20260917T091246Z/`：五归档、1,244 个旧项目文件、21 表/947 份加密响应独立恢复一致；数据库 head 不变。队列检查前后均为 24,817 条，第二次无消息需换队列。
- 五服务九进程同版本同配置，三个 Worker 队列及 Linux 隔离 3 项、登录隔离、HTTPS/前端资源、原预览接口、备份和续证 timer 通过；额度、功能开关、数据库全局配置及前端未改变。
- 仅提升原批次中已核实素材的现存当前封面消息优先级；按 task ID、当前 dispatch/revision、tenant/actor/完整 payload 校验，原子 `LREM + RPUSH` 逐条保证总数不变，没有新建消息或重传。先提升 200 个 PREPARE，再提升 25 个已发送结果的 VERIFY。
- 09:14 UTC outbox 到期未发从数千降为 0、control=0，连续观察无超过 12 秒的遗留恢复查询；09:17 UTC 分发 READY=820、封面 READY=25。Campaign/AD 尚未完成，继续核验完整搭建链路，不宣称整批完成。

## 09:40 UTC 完整链路验收

- 在原提交中选取一个素材已核实的账户/剧目单元，按实际依赖提升原消息优先级：当前视频只读核实 → 实际来源封面 → 封面核实 → MATERIAL 完成 → 原单元调度。包含内容别名时，来源必须同租户/目标 BC，且已验证 SHA-256、MD5、大小一致；未创建新消息或新提交。
- 该单元 MATERIAL 32、CTA 1、CAMPAIGN 1、ADGROUP 1、AD 2、READBACK 4 全部 `SUCCEEDED`；创建的计划/组/广告按原提交为 ENABLE，最终四项回读通过。不将创建成功等同实际消耗或审核通过。
- 原 `execution_prepare_failed` 的 1,339 个步骤全部 `attempt > 1`，当前该错误为 0。09:38 批次总计 MATERIAL 成功 58、分发 READY 832、封面 READY 58；到期待发 5、control=0，builds 从原 9,545 降至 5,376。
- 素材资源两队列合计仍有约 2.5 万历史/后续消息，整批未完成；130 个 MATERIAL 步骤仍为 `material_result_unknown`，保留核实、不自动重复上传。上述一单元闭环不冒充 180 个组合全部成功。
- 优先级操作为有限轮次，最后单元回读成功后停止；没有留下临时消费者、额外 Worker、循环运维脚本或修改配额。原五个服务继续处理剩余提交，后续查询沿当前批次/任务身份，不重新提交。
