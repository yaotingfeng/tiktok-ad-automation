# Loaded automatically only for local Docker development.
services:
  backend:
    ports: ['127.0.0.1:8000:8000']
    environment:
      FASTAPI_ENV: development
  db:
    ports: ['127.0.0.1:55432:5432']
  redis:
    ports: ['127.0.0.1:56379:6379']
